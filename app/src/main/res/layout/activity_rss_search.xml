<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.rss.search.RssSearchActivity">

    <!-- L-RssSearch 订阅源搜索页：顶栏 Compose 化（GlassTopAppBar + 搜索 SettingsSearchBar + 更多菜单） -->
    <androidx.compose.ui.platform.ComposeView
        android:id="@+id/compose_top_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent" />

    <io.legado.app.ui.widget.anima.RefreshProgressBar
        android:id="@+id/refresh_progress_bar"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        app:layout_constraintTop_toBottomOf="@id/compose_top_bar" />

    <io.legado.app.ui.widget.dynamiclayout.DynamicFrameLayout
        android:id="@+id/content_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/refresh_progress_bar">

        <!-- my-compose-full W2.1：结果列表 Compose 化（替代 RecyclerView+RssSearchAdapter） -->
        <androidx.compose.ui.platform.ComposeView
            android:id="@+id/compose_results"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </io.legado.app.ui.widget.dynamiclayout.DynamicFrameLayout>

    <!-- 输入帮助区（搜索历史）Compose 化，背景透明透壁纸（对齐书源搜索样板） -->
    <FrameLayout
        android:id="@+id/ll_input_help"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="@android:color/transparent"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/compose_top_bar">

        <androidx.compose.ui.platform.ComposeView
            android:id="@+id/compose_input_help"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </FrameLayout>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fb_start_stop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:contentDescription="@string/stop"
        android:src="@drawable/ic_stop_black_24dp"
        android:visibility="invisible"
        app:fabSize="mini"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
