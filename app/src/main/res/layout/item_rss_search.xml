<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:scrollbars="none">

    <!-- rss-unified-search: 订阅源搜索结果 item
         参考 item_search.xml 结构（CoverImageView + BadgeView + 标题 + 来源数 + 摘要）
         适配订阅源字段：title/pubDate/description/image/origins/isRead
         删除书源特有的 author/kind（订阅源文章没有这些字段） -->

    <io.legado.app.ui.widget.image.CoverImageView
        android:id="@+id/iv_cover"
        android:layout_width="80dp"
        android:layout_height="110dp"
        android:layout_margin="8dp"
        android:contentDescription="@string/img_cover"
        android:scaleType="centerCrop"
        android:scrollbars="none"
        android:src="@drawable/image_rss_article"
        android:transitionName="img_cover"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="UnusedAttribute" />

    <!-- 已读状态标识（参考 iv_in_bookshelf 用绿点显示） -->
    <io.legado.app.ui.widget.image.CircleImageView
        android:id="@+id/iv_read"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:layout_margin="8dp"
        android:scaleType="centerCrop"
        android:scrollbars="none"
        android:src="@color/md_green_600"
        android:visibility="invisible"
        app:layout_constraintLeft_toRightOf="@id/iv_cover"
        app:layout_constraintTop_toTopOf="@id/tv_title" />

    <!-- 来源数角标（多源聚合后显示 origins.size） -->
    <io.legado.app.ui.widget.text.BadgeView
        android:id="@+id/bv_originCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:scrollbars="none"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:scrollbars="none"
        android:singleLine="true"
        android:text="@string/app_name"
        android:textColor="@color/primaryText"
        android:textSize="16sp"
        app:layout_constraintEnd_toStartOf="@id/bv_originCount"
        app:layout_constraintStart_toEndOf="@+id/iv_read"
        app:layout_constraintTop_toTopOf="parent" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="3dp"
        android:orientation="vertical"
        android:scrollbars="none"
        app:layout_constraintBottom_toBottomOf="@id/iv_cover"
        app:layout_constraintLeft_toRightOf="@+id/iv_cover"
        app:layout_constraintRight_toRightOf="@id/tv_title"
        app:layout_constraintTop_toBottomOf="@+id/tv_title">

        <!-- 发布时间（订阅源文章字段，对应书源的 tv_lasted） -->
        <TextView
            android:id="@+id/tv_pub_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:lines="1"
            android:scrollbars="none"
            android:text="@string/last_read"
            android:textColor="@color/primaryText"
            android:textSize="12sp" />

        <!-- 文章摘要（订阅源文章 description 字段，对应书源的 tv_introduce） -->
        <io.legado.app.ui.widget.text.MultilineTextView
            android:id="@+id/tv_description"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:ellipsize="end"
            android:scrollbars="none"
            android:text="@string/book_intro"
            android:textColor="@color/primaryText"
            android:textSize="12sp" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
