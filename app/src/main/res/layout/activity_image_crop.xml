<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF111111">

    <io.legado.app.ui.widget.image.PhotoView
        android:id="@+id/photo_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FF111111" />

    <io.legado.app.ui.widget.ImageCropOverlayView
        android:id="@+id/crop_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 优化 1a：取景框比例徽标（内容/位置由 Activity 按 cropOverlay.getCropRect() 放置） -->
    <TextView
        android:id="@+id/tv_aspect_badge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_image_crop_aspect_badge"
        android:fontFamily="monospace"
        android:paddingStart="8dp"
        android:paddingTop="3dp"
        android:paddingEnd="8dp"
        android:paddingBottom="3dp"
        android:textColor="@android:color/white"
        android:textSize="11sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <!-- 优化 2：可恢复失败的页内错误条（danger 语义 + 重试；位置在操作栏之上） -->
    <LinearLayout
        android:id="@+id/error_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="76dp"
        android:background="@drawable/bg_image_crop_error_bar"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="14dp"
        android:paddingTop="10dp"
        android:paddingEnd="6dp"
        android:paddingBottom="10dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/tv_error"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="13sp" />

        <TextView
            android:id="@+id/btn_retry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackground"
            android:paddingStart="12dp"
            android:paddingTop="6dp"
            android:paddingEnd="12dp"
            android:paddingBottom="6dp"
            android:text="@string/retry"
            android:textSize="13sp"
            android:textStyle="bold" />

    </LinearLayout>

    <LinearLayout
        android:id="@+id/action_bar"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_gravity="bottom"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/btn_cancel"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="@drawable/bg_image_crop_toolbar"
            android:contentDescription="@string/cancel"
            android:padding="10dp"
            android:src="@drawable/ic_baseline_close"
            app:tint="@android:color/white" />

        <TextView
            android:id="@+id/tv_hint"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/image_crop_hint"
            android:textColor="@android:color/white"
            android:textSize="14sp" />

        <!-- 优化 1b：保存中回执（spinner 与确认键同位，保存期间顶替确认键） -->
        <ProgressBar
            android:id="@+id/progress_save"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:padding="12dp"
            android:indeterminateTint="@android:color/white"
            android:visibility="gone" />

        <androidx.appcompat.widget.AppCompatImageButton
            android:id="@+id/btn_confirm"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="@drawable/bg_image_crop_toolbar"
            android:contentDescription="@string/confirm"
            android:padding="10dp"
            android:src="@drawable/ic_check"
            app:tint="@android:color/white" />

    </LinearLayout>

</FrameLayout>