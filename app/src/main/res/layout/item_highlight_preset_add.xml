<?xml version="1.0" encoding="utf-8"?>
<!-- F-P1-2 高亮预设规则列表项（借鉴蛋蛋Max,适配当前项目） -->
<!-- 适配说明：
     1. shape_highlight_rule_card → shape_card_view
     2. shape_highlight_rule_icon_button → ?attr/selectableItemBackground
     3. shape_highlight_rule_preview_soft → shape_card_view（简化） -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="4dp"
    android:layout_marginBottom="4dp"
    android:background="@drawable/shape_card_view"
    android:elevation="2dp"
    android:orientation="vertical"
    android:padding="8dp">

    <!-- 标题行：标题+描述 + 添加按钮 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/primaryText"
                android:textSize="14sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tv_desc"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:textColor="@color/secondaryText"
                android:textSize="11sp" />
        </LinearLayout>

        <ImageView
            android:id="@+id/iv_add"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/add"
            android:padding="6dp"
            android:src="@drawable/ic_add"
            app:tint="@color/accent" />
    </LinearLayout>

    <!-- 预览区 -->
    <TextView
        android:id="@+id/tv_preview_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/preview"
        android:textColor="@color/secondaryText"
        android:textSize="10sp" />

    <TextView
        android:id="@+id/tv_preview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:background="@drawable/shape_card_view"
        android:lineSpacingExtra="2dp"
        android:minHeight="32dp"
        android:padding="8dp"
        android:textColor="@color/primaryText"
        android:textSize="13sp" />

</LinearLayout>
