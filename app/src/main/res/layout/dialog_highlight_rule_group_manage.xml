<?xml version="1.0" encoding="utf-8"?>
<!-- F-P1-2 高亮规则分组管理 Dialog（借鉴蛋蛋Max,适配当前项目） -->
<!-- 适配说明：
     1. shape_highlight_rule_sheet → shape_card_view（当前项目存在）
     2. shape_highlight_rule_handle → BottomSheetDragHandleView（material 库自带）
     3. shape_highlight_rule_icon_button → ?attr/selectableItemBackground
     4. shape_highlight_rule_save → MaterialButton TonalButton
     5. shape_highlight_rule_card → shape_card_view
     6. 移除 attachBottomSheetDismiss（当前项目无此扩展, 用默认 dismiss）
     7. 移除 observeEvent(EventBus.UP_CONFIG)（非必须主题切换监听）
     8. 移除 skin_textColor 皮肤属性（当前项目无皮肤系统） -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:id="@+id/vw_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:background="@drawable/shape_card_view"
        android:orientation="vertical"
        tools:ignore="UselessParent">

        <com.google.android.material.bottomsheet.BottomSheetDragHandleView
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- 标题栏：返回 + 标题 + 新增 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="12dp"
            android:paddingTop="4dp"
            android:paddingBottom="8dp">

            <ImageView
                android:id="@+id/iv_back"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/back"
                android:padding="8dp"
                android:src="@drawable/ic_arrow_back"
                app:tint="@color/primaryText" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tv_page_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/highlight_rule_group_manage_title"
                    android:textColor="@color/primaryText"
                    android:textSize="@dimen/text_20sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/tv_page_subtitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:gravity="center"
                    android:text="@string/highlight_rule_group_manage_subtitle"
                    android:textColor="@color/secondaryText"
                    android:textSize="@dimen/text_12sp" />
            </LinearLayout>

            <Button
                android:id="@+id/tv_add_group"
                style="?attr/borderlessButtonStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:minWidth="0dp"
                android:paddingHorizontal="12dp"
                android:text="@string/highlight_rule_group_add"
                android:textColor="@color/primaryText"
                tools:ignore="TouchTargetSizeCheck" />
        </LinearLayout>

        <!-- 查看全部分组 -->
        <LinearLayout
            android:id="@+id/ll_view_all"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="12dp"
            android:layout_marginTop="4dp"
            android:background="@drawable/shape_card_view"
            android:foreground="?attr/selectableItemBackground"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:padding="12dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/highlight_rule_group_view_all"
                android:textColor="@color/primaryText"
                android:textSize="@dimen/text_14sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tv_all_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/secondaryText"
                android:textSize="@dimen/text_12sp" />
        </LinearLayout>

        <!-- 列表区域 -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recycler_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingTop="8dp"
                android:paddingBottom="16dp" />

            <TextView
                android:id="@+id/tv_empty_msg"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="@string/highlight_rule_group_empty"
                android:textColor="@color/secondaryText"
                android:textSize="@dimen/text_16sp"
                android:visibility="gone" />
        </FrameLayout>
    </LinearLayout>
</FrameLayout>
