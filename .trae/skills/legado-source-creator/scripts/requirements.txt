# Legado Client - Python 依赖清单
# 安装: pip install -r requirements.txt
# 测试依赖: pip install -r requirements.txt && pip install pytest pytest-cov

# === 核心依赖（必需） ===
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
psutil>=5.9.0
jsonpath-ng>=1.6.0
pyyaml>=6.0
aiosqlite>=0.20.0

# === Web 服务依赖 ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
httpx>=0.25.0
websockets>=12.0
slowapi>=0.1.9

# === 存储层依赖（数据库） ===
python-dotenv>=1.0.0
sqlalchemy[asyncio]>=2.0.0
aiomysql>=0.2.0
pymysql>=1.1.0
alembic>=1.13.0

# === 测试依赖（开发时安装） ===
pytest>=7.0
pytest-cov>=4.0

# === 可选依赖（按需安装） ===
# Playwright: 用于 fetch_html.py/html_fetcher.py 获取需要 JS 渲染的页面
#   安装: pip install playwright && playwright install chromium
# playwright>=1.40.0

# pycryptodome: 用于 crypto_analyzer.py 分析加密规则
#   安装: pip install pycryptodome
# pycryptodome>=3.18.0
