{# ai_tests/templates/ai_prompt_template.j2 — AI 提示词 Jinja2 模板（V4 预留）

当前状态：V3 由 M6 RuleAnalyzer._generate_ai_prompt() 内联实现（固定字符串拼接）
升级路径：M7 report_generator 统一改用本模板渲染（V4）

简化说明：本模板为 V4 预留占位 | 已知上限：M6 当前未使用本模板 | 升级路径：M7 接管 ai-prompt 渲染后启用
#}
# AI Agent 判定提示词

## 用例信息
- TC-ID: {{ tc_id }}
- 标题: {{ title }}
- 模块: {{ module }}
- 关联源码: {{ related_source | join(", ") }}
- 关联 Activity: {{ related_activity | join(", ") }}

## 判定原因
{{ reason }}

## 证据摘要
{% for ev_type, status in evidence_summary.items() %}
- {{ ev_type }}: {{ status }}
{% endfor %}

## 预期结果
{% for expect in expects %}
- [{{ expect.expect_type }}] {{ expect.description }}
{% endfor %}

## 判定指引
1. 基于证据摘要和预期结果，判定 pass/fail/manual
2. fail 判定需给出 failure_pattern（异常类型）
3. manual 判定需说明证据不足的具体原因
4. 将判定回填到 report.json 的 cases[].ai_verdict 字段
