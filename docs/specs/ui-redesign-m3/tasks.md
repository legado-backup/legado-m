# tasks.md — UI 重构设计 任务清单

> 本 spec 交付 **设计文档**（1-7）+ 实施 Phase0-3（8-11）+ 设计文档完善 v2（12，用户评审后补充）+ 实施者深度复盘补证（14，2026-08-11）。设计阶段不写代码；实施阶段动 `ui/widget/components/` 等 src。当前状态：✅ 设计完成 → 🔄 实施 Phase0-3 完成，Phase4-5 待执行；设计 v2 完善中待用户评估（12.10）；实施者复盘补证已并入 11.9/14。

## 1. 需求分析（已完成）
- [x] 1.1 确认需求范围（仅设计，保留暗夜紫，学习开源 fork）
- [x] 1.2 阅读相关源码（ThemeStore/LegadoTheme/ComposeActivitySupport/themeConfig.json）
- [x] 1.3 审核 33 个 fork 的 UI 资产（深度版：HapeLee/325506/legadoT/legados/Suml-1/youfengnoGht/Jingshiro）

## 2. 设计 Token 体系
- [x] 2.1 设计 color 令牌（三内置+暗紫，中心色板表）
- [x] 2.2 设计 spacing/radius/typography 令牌（复用路径）
- [x] 2.3 绘制栅格系统文档（360dp 基准/16dp 边距/档位表）

## 3. 主题系统设计
- [x] 3.1 确认 ThemeSpec（4 内置）与 ThemeConfig.applyConfig 联动
- [x] 3.2 确认阅读器独立配色（ReadBookConfig）不联动全局方案
- [x] 3.3 明确「暗夜紫为默认 dark」保留方案

## 4. Fork 深度学习清单
- [x] 4.1 深挖 HapeLee/legados/Rimchars/legado_NG 四仓前端（架构/组件/弹窗/主题/我的页）
- [x] 4.2 深挖 legado-Harmony 鸿蒙版 MyCenter 三级布局（统计卡→高频卡→低频列表）+ 设置分组
- [x] 4.3 原版痛点清单 8 条（长列表/弹窗深/无搜索/组件零散/角标/阅读设置路径/N过度画色漂移）逐条对齐解法
- [x] 4.4 产出 four-fork-deep-dive.md（组件化清单 + MyCenter 方案 + 风险红线）
- [x] 4.5 design.md 新增 AD-15（组件化优先）与 AD-16（My 页三级布局）
- [x] 4.6 深挖 MoRealm（墨境）：克隆 temp/forks-comparison/morealm-reader，逐源码拆解导航/主题/组件/阅读器/书架（两子代理+父代兜底）
- [x] 4.7 产出 morealm-deep-dive.md（Top10 可搬运组件 + 权衡表）
- [x] 4.8 design.md 新增 AD-17（PillNavigationBar 替代底部导航）与 AD-18（主题 5色→34槽位公式）；File Changes/README 索引同步
- [x] 4.9 新增 frontend-synthesis.md（整体前端思想综合：五支柱/贡献矩阵/不裁剪红线清单/Phase 0~4），design/README/INDEX 引用同步
- [x] 4.10 新增 implementation-spec.md（实现细化：17 组件签名/主题 toM3Scheme 映射/真实锚点/PR+KPI/themeConfig 封口），design/README/INDEX 引用同步

## 5. 页面设计（8 页四要素）
- [x] 5.1 书架（主 Grid）→ P1.md
- [x] 5.2 阅读器页 → P2.md
- [x] 5.3 书籍详情 → P3.md
- [x] **5.4 我的/设置（鸿蒙三级布局 → P4.md 重写，含真实统计+搜索+组件复用）**
- [x] 5.5 书源管理 → P5.md
- [x] 5.6 发现 / 网络书城 → P6.md
- [x] 5.7 RSS / 订阅源 → P7.md
- [x] 5.8 正文内浮层 → P8.md
- [x] 5.9 每页绘图 Prompt 统一前缀策略（写进 pages/_template.md）

## 6. 验证
- [x] 6.1 每页检查五要素齐（布局/交互/Compose 思路/绘图 Prompt）
- [x] 6.2 主题 4 套（含暗紫）在文档中一致
- [x] 6.3 docs/INDEX.md 更新 spec 条目
- [x] 6.4 与现有基础设施引用一致（之已存在代码不加改动）

## 7. 审查
- [x] 7.1 提交检查点1：用户审查设计（AskUserQuestion）
- [x] 7.2 按反馈修订（共三轮：深度不足→四仓鸿蒙→MoRealm→整体综合→实现细化）
- [x] 7.3 标记 README 状态与 INDEX 归位 → ✅ 设计完成

## 8. 实施 Phase 0（组件库建仓，2026/08/10 进入）

> implementation-spec §5 PR 粒度任务。全局思考门禁见 implementation-spec §四（Applicable Checklist 已于实施前填写）。

- [x] 8.1 探针 Compose 基境（BOM 2025.04.01 / material3 可用 / components 目录不存在需新建 / DebugToolsScreen 风格 / Book 实体路径）
- [x] 8.2 PR-0.1: components/ 目录 + ThemeSpec + BadgeDot + ThemedSnackbarHost（❌ 已删除 2026-08-16 孤儿清理）
- [x] 8.3 PR-0.2: 设置三模板（Section/Card/ClickRow/ToggleRow/RowIcon）
- [x] 8.4 PR-0.3: PillNavigationBar + SplicedColumnGroup（❌ 已删除 2026-08-16 孤儿清理） + AppModalBottomSheet
- [x] 8.5 PR-0.4: SwipeActionContainer + VerticalScrollbar（✅ 已接线 DownloadManageScreen，2026-08-16） + ShelfGridSkeleton + GlassTopAppBar + SettingsSearchBar + MetricGrid + SummaryCard（❌ 已删除 2026-08-16 孤儿清理） + BookTocBookmarkSheet
- [x] 8.6 updateLog.md 已更新（编译前强制 ✅ 已完成文本）
- [x] 8.7 ./gradlew assembleAppDebug 编译门禁通过（2m15s，BUILD SUCCESSFUL，APK 51.7MB；修复 4 处编译错误：DragHandle→BottomSheetDefaults、HSL→androidx.core.graphics.ColorUtils、offset import、未命名局部变量）
- [x] 8.8 检查点2：用户审查 Phase 0 → 用户确认「继续 Phase 1 主题 34 槽位 (推荐)」

## 9. 实施 Phase 1（主题 34 槽位推导，2026/08/10）

> implementation-spec §5 Phase 1：PR-1.1（ThemeSpec.toM3Scheme 已在 Phase 0 落盘）+ PR-1.2（LegadoTheme 接线）。

- [x] 9.1 确认 ThemeSpec.toM3Scheme 语义映射等价旧手写（primary=accentColor、secondary=primaryColorValue、tertiary=secondary、surface lerp 4%/10% 同旧）
- [x] 9.2 PR-1.2：LegadoTheme.kt `:40-43` lerp 块 + `:45-85` 手写 ColorScheme → 替换为 `remember { ThemeSpec(...).toM3Scheme() }`（新增 ThemeSpec import；删除 dark/lightColorScheme、lerp 死 import；themeConfig.json 只读）
- [x] 9.3 updateLog.md 追加「主题配色算法统一」条目（面向用户，编译前完成）
- [x] 9.4 ./gradlew assembleAppDebug 编译门禁通过（1m13s，BUILD SUCCESSFUL，77 tasks 9 执行）
- [x] 9.5 检查点3：用户审查 Phase 1 → 用户确认「继续 Phase 2 我的页 (推荐)」

## 10. 实施 Phase 2（我的页 Compose 化，2026/08/10）

> implementation-spec §5 Phase 2：ProfileScreen3Level + MyFragment ComposeView 接线。KPI：高频入口 ≤2 步可达。

- [x] 10.1 探针锚点（MyFragment 190 行全貌 / pref_main 入口表 / fragment_my_config.xml / appDb dao API / 统计 formatDuring / ConfigTag 分发 / WebService/AutoTaskService companion）
- [x] 10.2 新建 ProfileScreen3Level.kt（ui/main/my/）：三级布局（统计卡 MetricGrid：读过/总时长/书签/书源 → 常用 8 行 SettingsClickRow → 服务 SettingsToggleRow ×2 → 其他 6 行）；produceState(IO) 读 appDb；formatDuring 复刻；入口映射复刻 MyPreferenceFragment onPreferenceTreeClick（全保留）
- [x] 10.3 MyFragment.kt 接线：childFragmentManager replace(MyPreferenceFragment) → `binding.preFragment.removeAllViews()` + `addView(ComposeView { setContent { LegadoTheme { ProfileScreen3Level() } } })`；保留 position/菜单/MyPreferenceFragment 类（入口数据源语义不删）
- [x] 10.4 本地化：values/strings.xml + values-zh/strings.xml 各加 read_count/total_read_time/frequent/service
- [x] 10.5 updateLog.md 追加「我的页全面改版（Compose 三级布局）」（编译前强制）
- [x] 10.6 compileAppDebugKotlin 失败 3 处修复（Books→LibraryBooks、ConfigTag→ui.config 包、bookSourceDao.count→allCount()）
- [x] 10.7 ./gradlew assembleAppDebug 编译门禁通过（compileAppDebugKotlin 1m17s + assembleAppDebug 41s；APK legado_miss_app_3.26.081017.apk 54.2MB）
- [x] 10.8 真机验证（MEmu 21503 测试包 io.legado.miss.app.debug）：发现并修复我的页永久 loading（produceState+LaunchedEffect 死锁 → 单 produceState 委托属性改为 if stats==null 显示加载）；三级布局/统计卡真实数据（已读书籍1/时长0秒/书签0/书源0）、Web 服务开关 checked false→true、书源管理入口→BookSourceActivity、logcat 零 FATAL 全通过
- [x] 10.9 检查点5：用户审查 Phase 2 真机验证结果 → 用户确认「进入 Phase 3 书架 (推荐)」

## 11. 实施 Phase 3（书架 Compose 化，2026/08/10）

> implementation-spec §五 Phase 3 书架。用户红线：BaseBookshelfFragment 的 12 菜单/toolbar/导入导出/configBookshelf 对话框**全保留为 View 壳**；多选已迁移 BookshelfManageActivity 无需主列表实现；封面用 `BookCover.load`+AndroidView 免引入 glide-compose。

- [x] 11.1 探针书架契约（子代理全收：View 体系/数据流/渲染矩阵/多选已迁移/封面 Glide 基境）
- [x] 11.2 封面方案定稿：**不引入 glide-compose**——Compose 内 `AndroidView(factory=ImageView) + update={BookCover.load(ctx, book.getDisplayCover()).into(view)}`（BookCover.kt:83-128 已集成 OkHttp/loadOnlyWifi/useDefaultCover/placeholder/error/crossfade/centerCrop）
- [x] 11.3 新建 BookshelfScreen.kt（受控组件）：BookGroupTabs(ScrollableTabRow)/FolderGroupList(groupId==IdRoot)/BookGrid(LazyVerticalGrid 2-6列 含 UnreadBadge+showBookname 1/2+进度条)/BookList(LazyColumn List0 66x90 与 List2 48x64 紧凑)/空态 bookshelf_empty/下拉刷新 PullToRefreshBox(isRefreshing,onRefresh)/topScrollTrigger gotoTop/内部 sortedByBook 排序（1 latest desc/2 name/3 order/4 综合 max(latest,dur) desc/5 author/else dur desc）
- [x] 11.4 改造 BookshelfFragment1（isFolder=false）+ BookshelfFragment2（isFolder=true，currentGroupId 初始 IdRoot，back() 返回根契约 MainActivity.kt:106-107）+ 两 fragment_bookshelf*.xml 容器改 ComposeView + `binding.viewPagerBookshelf/rvBookshelf.setContent{LegadoTheme{BookshelfContent()}}` 纯方案（ViewPager/RecyclerView addView 不测量→改 XML 元素类型解决 0 尺寸；compose-ui 1.9.2 无 setViewTreeLifecycleOwner 弃用）；状态 books/loading/groupId/topScrollTrigger/refreshing + upConnect collect flowByGroup
- [x] 11.5 updateLog.md 追加「书架全面改版（Compose 布局）」条目前置到我的页条目之前
- [x] 11.6 ./gradlew assembleAppDebug 编译门禁通过（编译修复 9 处：Tab 无 onLongClick 用 combinedClickable Modifier、String? 判空、AppConfig import io.legado.app.help.config、R/lifecycleScope import、getBookSortByGroupId suspend 移入 launch、BookExtensions 顶层扩展 import 不带类名前缀 → BUILD SUCCESSFUL 2m29s + 全量 41s，APK 081100/081101 54.2MB）
- [x] 11.7 MEmu 真机验证（全部 ✅）：书架 Compose 渲染（分组 Tab 全部/本地 + b15_test 条目 dur/latest 章节 + 0.0 进度）；Tab 切换正常；点击书→ReadBookActivity 打开阅读器；12 菜单入口全保留；configBookshelf 对话框（视图 列表/紧凑/网格2-6、排序 6 项、间距 SeekBar、开关）弹出；网格二列切换无崩溃；style2 Folder 模式代码保留（默认 Tab 未单测）
- [x] 11.8 检查点6：用户审查 Phase 3 真机验证结果（AskUserQuestion → 用户提出「深度思考 UI 开发是否符合当初设计」，此问题演进为第 14 节复盘）
- [x] 11.9 补证 P0 存量重复收敛：书架 UnreadBadge → 复用公共 BadgeDot、加载态 → ShelfGridSkeleton（AD-21 首批落地）。实施记录（2026-08-11）：BookshelfItems.kt 删 UnreadBadge 私有实现（原 67-91 行）→ BookshelfScreen 网格/列表 2 处改引 BadgeDot（count+contentColor error/primary，>9 error 语义保留）；loading 分支 CircularProgressIndicator → ShelfGridSkeleton（columns=layout 或 2）；空态单行 Text → 新增公共组件 EmptyStatePlaceholder（icon 48dp MenuBook + bookshelf_empty_title 主文案 + bookshelf_empty 次文案）；补错误态 EmptyStatePlaceholder(Error + load_error_retry + retry) 并由两 Fragment upConnect try-catch（CancellationException rethrow、Exception→error=true+AppLog.putDebugWithTag(TAG_DATA)）接线 onRetry；同时归档栅格越档（14.4）。strings.xml 新增 bookshelf_empty_title（en/zh）

### 页面回执：书架 BookshelfScreen（骨架 S2 · 阶段 P0 补/Phase 3）
- 负责人/日期：AI / 2026-08-11
- 【本次复用】公共组件清单：BadgeDot×2（网格+列表未读角标，替换私有 UnreadBadge）、ShelfGridSkeleton（loading）、EmptyStatePlaceholder×2（空态+错误态，本次沉淀）
- 【本次复用】骨架/样式：S2 列表骨架、卡 18dp→shapes.small、间距 4dp 档位（8/12/16）、colorScheme 全量
- 【本次沉淀】新增可复用资产：`EmptyStatePlaceholder`（统一空态/错误占位组件）→ 已登记 components 目录 + ui-standards §3/§3.1/§5
- 【一致性】页面私有组件：0（UnreadBadge 已删）；硬编码色：0（GeneratedCover 已登记豁免）；三态齐全：是（骨架/空态/错误+重试）
- 【对后续页复用贡献】空态/错误态占位与当空态文案模式（bookshelf_empty_title+subtitle）可复用到 BookSource/Rss/搜索/发现列表；BadgeDot>9 用 error 色的阈值语义同 MainActivity Tab
- 【真机覆盖】功能点用例：① 网格双列加载骨架→书列表（BadgeDot 渲染）② 空书架空态占位 ③ style2 Folder ⑤ Tab 切换 ⑥ 下拉刷新。已验（2026-08-11 MEmu）：L1 启动无崩溃、网格 2 列 GeneratedCover+本地徽章渲染、BadgeDot 未读角标数字渲染（改 b15_test durChapterIndex=-2 触发后恢复）、点击封面→ReadBookActivity、分组 Tab 切换、logcat 无 AndroidRuntime/DataLayer 错误。空态/错误态平时不可达未直接触发，待 14.8 复核
- 【遗留】错误态触发路径平时不可达（Room Flow 稳定），真机以 code 注入验证；PillNavigationBar 等 12 孤儿接线留待 Phase4/5

## 12. 设计文档完善 v2（2026-08-11，用户评审 Phase0-3 后要求全面完善）

> 背景：用户审视前几轮改造后要求**深刻反省**——为什么学了 5 个开源前端还是"效果差、杂乱"；明确 goal = 前端全部 Compose + 工程级项目级标准规范 + 页面级统一风格可复用 + 核心功能一个不漏。本阶段**仅完善设计文档，不写代码**，完成后等用户评估。

- [x] 12.1 新增 pages-inventory.md：全量 84 页面类功能点核对表（6 功能域 69 页+15 壳、技术栈/骨架/迁移优先级/入口/手势、§G 路线图 P0→P3+N、§H 变更记录）
- [x] 12.2 新增 ui-standards.md：前端 UI 工程规范权威（§1 设计基石/§2 六类页面骨架 S1-S6/§3 组件六族目录+孤儿接线计划/§4 状态管理范式/§5 三态/§6 无障碍质量/§7 页面改造检查清单/§8 验收 KPI/§9 变更记录）
- [x] 12.3 修订 README.md：状态 →「🔄 开发中（设计 v2 全面修订 + 实施 Phase0-3 已落地，Phase4-5 待执行）」；文档索引补 pages-inventory/ui-standards ⭐；概述补「最终目标前端全部 Compose + 边界约束」
- [x] 12.4 修订 spec.md：Intent 扩 3 条（全 Compose 终态/统一设计思想/功能一个不漏）；Scope 扩至全量 84 页面类；Approach 补工程规范兜底 5 步；新增 FR-8~FR-12（全 Compose P1/统一骨架 P0/组件复用门禁/真机覆盖测试门禁/工程规范交付 P0）；NFR 补状态管理统一范式；Checklist 全量核对✅
- [x] 12.5 修订 design.md：新增 ADR-19（6 类页面骨架）/AD-20（全量 Compose 迁移路线图 P0-P3+N）/AD-21（组件复用强制规范）/AD-22（状态管理范式受控组件+ViewModel）；File Changes 补 v2 文档；AD-18 Status→Accepted
- [x] 12.6 修订 frontend-synthesis.md：五支柱下新增「页面类型骨架」维度（引用 ui-standards S1-S6）与 v2 反思结论
- [x] 12.7 修订 implementation-spec.md：修复 Phase4/Phase5 标号重复（272-275 行）；数据补充 AD-19~22 对应代码锚点
- [x] 12.8 更新 docs/INDEX.md 与 README 状态同步
- [x] 12.9 updateLog 检查：本阶段仅完善 docs/ 设计文档，无 src 代码变更、无编译，按 AGENTS.md「updateLog 面向用户可见变更」规则**不追加**版本日志条目（待后续页面 Compose 化落地时随编译前更新）
- [ ] 12.10 检查点7：用户评估设计文档 v2（AskUserQuestion，等评估后再进入后续页面 Compose 化）
- [ ] 12.11 经用户评估通过后：标记 README 状态与 INDEX 归位 → ✅ 设计完成（v2）

### 12.x 深化迭代（2026-08-11 用户新增要求：回执机制 + 主干→支干→枝叶 + 现状审查）

- [x] 12.12 新增 ui-standards §3.3 实施回执模板（Component Usage Receipt：复用组件/沉淀新资产/私有复制计数/对后续复用贡献/真机覆盖/遗留，回执缺失=未完成）
- [x] 12.13 新增 design.md AD-23（实施回执机制闭环）+ AD-24（主干→支干→枝叶全局构建策略，主干=Phase0-3 公共资产/支干=S1-S6 样板页/枝叶=同类复用）
- [x] 12.14 新增 ui-standards §9 主干→支干→枝叶策略（9.1 三层结构/9.2 支干样板页分配表 S1→MainActivity/S2→BookSource/S3→BookSourceEdit/S4→BookInfo/S5→阅读器浮层/S6→登录导入Dialog/9.3 构建节奏"越来越快"）
- [x] 12.15 改造 pages-inventory §G 为主干→支干→枝叶三阶段路线图（样板页并齐），§H 追加 v2 记录
- [x] 12.16 新增 frontend-synthesis「五·补2 现状 Compose 页面审查报告」（源码核验：我的页达标但 3 待修/书架受控组件范本但 UnreadBadge 违例+加载态不用骨架/LegadoTheme 全局样板✅/组件库 12 孤儿待接线；审查总结：主体符合统一规范、2 处违例+P0 补处理）
- [x] 12.16b 四模块统一性核对（用户追问书架/书源/订阅源/我的 + 搜索 + 布局排序是否全设计 OK）：骨架/搜索已统一（S2 SettingsSearchBar）；**布局切换+排序缺口**（书架 layout1/2、书源三视图+排序6、订阅源三视图+rssSort6、RSS 文章 5 布局、替换/词典拖拽、ReadRecord 排序各页私有）→ 登记统一组件 `ListLayoutMenu`（ui-standards §2 S2/§3 组件表/接线计划 P1 里程碑首接线 BookSourceActivity/§9.2 S2 样板；frontend-synthesis 补缺口盘点；pages-inventory C1/D1 标注 + §H v2.1；ui-standards §10 变更记录 v2.2）
- [x] 12.16c 弹窗统一组件化方案（用户追问"弹框是否有统一组件化设计"）：ui-standards §6 S6 升级为「弹窗统一三层体系」L1 AppModalBottomSheet/L2 语义 Dialog 族（ConfirmDialog/AppEditDialog/AppSelectDialog/AppNumberPickerDialog/AppTextDialog/AppWaitDialog 共 6 个待建，替代全仓几十处 DialogEditText/NumberPicker/TextDialog/WaitDialog 私有弹窗）/L3 透明窗壳；§3 组件表+接线计划登记 Dialog 族；§7 检查清单 6 步补弹窗收敛门禁；§10 变更记录 v2.3；implementation-spec 新增 §2.3.1 Dialog 族签名+头部计数同步（19 落仓 + 6 待建）；修复误删的 VerticalScrollbar 段。
- [x] 12.16d 菜单统一组件化方案（核验延伸：全仓 20+ 私有 PopupMenu 无统一菜单组件）：新增「菜单族」3 组件——`AppMenuSheet`（长按条目 BottomSheet 富操作，替代各 Adapter 长按 PopupMenu）/`AppDropdownMenu`（顶栏/条目更多下拉，均 🆕 待建）/`SelectActionBar`（存量 View 公共批量栏 ui/widget/SelectActionBar.kt，10+ 页在用，登记为公共组件，Compose 化时迁移受控批量栏）；ui-standards §2 S2 手势行/§3 组件表/§3.1 接线计划（P1 里程碑首接线 BookSourceActivity）/§7 检查清单 6 步补菜单收敛门禁（无页面私有 PopupMenu）/§9.2 S2 样板/§10 变更记录 v2.4；Toast 仅 3 处（Config 子页）可控。
- [x] 12.16e 方向与自适应统一策略（核验缺口：设计文档无横竖屏/大屏覆盖，但源码已有完整体系）：ui-standards 新增 §1.4——阅读器方向走 AppConfig.screenOrientation（0-5，BaseReadBookActivity 统一应用）；WebView 内容方向由书源 JS 控制（portrait/landscape/any 字符串→ActivityInfo 映射，WebViewActivity/BottomWebViewDialog/ReadRssActivity）；沉浸页全屏按 VideoPlay.isPortraitVideo 切换；新页面一律跟随系统不写 requestedOrientation；configChanges 已声明 + Compose 用 rememberSaveable 保活；网格列数随宽度自适应（BoxWithConstraints 断点 480/840dp）；§10 变更记录 v2.5。
- [x] 12.16f 核验收尾维度（导航/路由、换肤/多主题、权限、国际化、动画转场）：**已固化不新增**——导航 AD-17 PillNavigationBar 不引 Nav3 保持 startActivity（four-fork-deep-dive:20/151、morealm-deep-dive:99）；换肤 AD-14 不引 zip/Monet 保持 ThemeStore 多 JSON 主题（17 套含暗夜紫）+ AD-18 主题 5 色→34 槽位公式；权限源码已统一 `checkPermissions{}` 扩展封装（HandleFileActivity 等），pages-inventory 逐页记录。**补证缺口**：i18n 无规范 + 源码确有硬编码中文（RegexTestScreen/TimestampConvertScreen/SettingsSearchBar/OpenUrlConfirmDialog/VideoPlayerActivity/VideoFragment/SpeakEngineDialog 等）→ ui-standards 新增 §6.1 国际化（双语 strings.xml 强制/禁硬编码/存量清零清单）；动画转场无统一规范 → §6.2（Hero 默认关 AD-08/浮层零打断/局部 spring 微交互/禁常驻全量换肤动画 AD-18）；§7 检查清单扩至 13 步；§10 变更记录 v2.6。
- [x] 12.16g 书源/订阅源管理页 fork 布局深挖（用户 2026-08-11 追问「有无认真分析书架/书源/订阅源布局并融会贯通」——书架已深挖，书源/订阅源原只有 1 句现状观察 = 真实缺口）：逐源码核验 3 个实现——HapeLee BookSourceScreen（946 行：RuleListScaffold+FastScrollLazyColumn+domain 分组头+ReorderableSelectionItem 拖拽/多选+批量 ActionItem 11+排序 7 维）、HapeLee RssSourceScreen（416 行：同脚手架单列表）、MoRealm BookSourceManageScreen（1305 行：分组 Chips+可折叠 GroupHeader 启用数徽标+卡片 SourceItem 选中描边/评分 N4/登录 AssistChip+统计条+空态 CTA+BackHandler 退多选+referentialEqualityPolicy 陷阱）。**融会贯通落盘**：forks-deep-dive 新增 §7（含启示比照表）；ui-standards S2 骨架新增「分组列表 GroupHeader」+「ListLayoutMenu 排序交互（点同维度翻转）」+「多选列表（BackHandler 优先退多选/选中集 listSaver）」+ §3 组件表登记 GroupHeader + 新增 §4.3 数据类去重陷阱（BookSource.equals 只比 url）→ §10 变更记录 v2.7；结论：fork 均单列表无三视图，按原版红线保留三视图（ListLayoutMenu），但分组态优先走折叠渲染（GroupHeader）。
- [x] 12.16h 阅读页整体框架设计定稿（用户重点要求：看到别的开源阅读项目已实现 Compose 化，期望学习优点结合自身设计规范补强阅读页，推翻早期「Compose 化回归风险大不动」保守结论）：3 个 explore 深挖——HapeLee（ReadBookScreen 641/RouteScreen 827/Controller 1673/MenuBar 族/sheet 族/ReaderViewport 尺寸接缝/单一 activeSheet 三类渲染/配置驱动菜单/滑块拖动 alpha≤30%）、MoRealm（ReaderScreen 2207 单 Box 兄弟浮层栈/BackHandler 优先级链/悬浮药丸底栏/9 控制器拆分/restoreToken 恢复命令语义/全书 Slider conflate+串行预览/沉浸管理集中化/仿真翻页退回 View）、legadoT（纯 View 无 Compose/DialogForm 统一模板/Material Slider/朗读悬浮胶囊/禁用态 ColorStateList/TextActionMenu 可配置）、自身现状（activity_book_read.xml 布局树/read_menu+search_menu View 层/config 12 Dialog 弹窗族/menuLayoutIsVisible 三信号/选区锚点坐标/insets 双轨/PageView 反向依赖）。**落盘**：pages/P2-reader.md 全面重写为权威版（壳-核三层架构 ReadBookRouteScreen 编排壳+ReadBookController 多接口桥接+ReaderViewport 尺寸接缝；叠放布局树；单一 activeSheet 三类渲染+BackHandler 优先级链；顶底栏磨砂降级方案 RenderEffect blur API31+/纯色，不引第三方；滑块 alpha≤30% 实时预览；配置驱动工具按钮；弹窗族收敛表 config 12 Dialog→L2 Dialog 族+Sheet；R0-R4 迁移路径；正文零改动红线 6 条）；ui-standards v2.8（S5 骨架扩写壳-核分离 + §3 组件表新增「阅读器」族 5 组件 ReadMenuGlassButtonSurface/ReadMenuSlider/ReaderBookSheet/ReaderMoreActionsSheet/ReaderViewport + §3.1 接线计划 + §9.2 S5 样板行）；tasks 本条目。→ §10 变更记录 v2.8。
- [x] 12.16i 我的页（ProfileScreen3Level）v2.8 规范符合性复审（用户指令：审查已完成页面是否符合当前设计文档）：逐文件核对 MyFragment.kt（XML 壳+ComposeView 接线，LegadoTheme 包裹）+ ProfileScreen3Level.kt（276 行三级布局）+ fragment_my_config.xml（TitleBar+pre_fragment）+ main_my.xml（menu_help 仅）。**符合项**：组件复用率最高（全页 import 设置族，无私有实现除 formatDuring）；主题接入/无硬编码色/文案 getString/骨架 S2 分类均合规。**违例 4 项新增**：① 实施回执缺失（§3.3/AD-23，KPI 第 5 项——书架有回执我的页无）② formatDuring 硬编码中文「天/小时/分钟/秒/0秒」（§6.1 i18n 违例）③ 三态不全（仅加载态 CircularProgressIndicator 且 stats 查询无 runCatching→Room 异常闪崩/永久 loading，缺错误态）④ 服务开关 `remember{ mutableStateOf }` 仅初始值不观察 EventBus.WEB_SERVICE（§4.2，MyPreferenceFragment:111-120 有 observeEventSticky 但 Compose 版未接——ConfigActivity 子页启停后返回不回读）。**遗留旧项 3**：MyPreferenceFragment 死代码重叠/退出行 as? Activity 硬转/menu_help 在 XML 壳。落盘：pages-inventory A2 条目标注 v2.8 复审四项；修复归入 P1 收尾队列（回执/三态/i18n/事件订阅）。
- [x] 12.16j 书架 v2.8 规范符合性复审（用户指令继续审查其它已 Compose 化页面）：逐文件核对 BookshelfScreen.kt（506 行受控组件）、BookshelfItems.kt（132 行封面族）、BookshelfFragment1/2、BaseBookshelfFragment.kt（300 行 View 壳）。**符合项**：三态齐全（ShelfGridSkeleton/EmptyStatePlaceholder×2，11.9 已修）；BadgeDot 已收敛（11.9）；LazyVerticalGrid/LazyColumn+key；BookCover.load Glide AndroidView；受控组件模式（books/loading 由 Fragment 提供）；主题色全走 colorScheme；GeneratedCover 8 色已登记豁免。**违例 4 项新增**：① **Config 值 `remember{}` 首帧快照**（BookshelfScreen:81-85 五组 AppConfig 读取）——§4.2 违例，改设置后回书架不刷新（configBookshelf 用 `postEvent(RECREATE)` 重建 Fragment 兜底恰好证明快照失效），修复=P1 队列换 Flow/collectAsState/EventBus 观察 ② **网格 `GridCells.Fixed(spanCount)`**（:262）——§1.4 自适应违例，但列数由 AppConfig.bookshelfLayout（1-4 用户显式选择）驱动属用户控制，登记为 §1.4 例外（同 GeneratedCover 8 色豁免范式）或大屏叠加自适应 ③ **i18n 硬编码**（§6.1 存量清零清单新增）：BookshelfItems:125 `"本地"/"在线"` 徽章（直接渲染 UI）+ BookshelfViewModel 业务 toast 5 处（添加网址失败/添加网址出错/导出书籍出错/格式不对/书籍不能为空）+ BaseBookshelfFragment `"添加中..."`×2 ④ **configBookshelf 私有弹窗**（DialogBookshelfConfigBinding）——§7 第 6 步违例，但与 Phase3 用户红线「12 菜单+configBookshelf 对话框全量保留 View 壳」冲突，登记为红线保留例外，Compose 化 L2 Dialog 族时合并。**已知遗留非新增**：style1/2 upConnect 重复订阅（§4.1 Phase4 收敛）、A4 style2 无独立回执。落盘：pages-inventory A3/A4/A5 条目标注 v2.8 复审。
- [x] 12.16k 公共组件库 + debug 工具族 v2.8 规范符合性复审（用户指令继续审查剩余已 Compose 化部分）：① **公共组件库接线实测**（grep 逐组件接线确认）：✅ 已接线 8 个——MetricGrid（ProfileScreen3Level:119）/SettingsSection/ClickRow/ToggleRow（我的页 8+2+6 行）/ShelfGridSkeleton/EmptyStatePlaceholder/BadgeDot（书架+BadgeDot 被 PillNavigationBar 内部用）/RowIcon（被 ClickRow/ToggleRow 内部用）；❌ **孤儿零接线 11 个**——PillNavigationBar/GlassTopAppBar/SettingsSearchBar/AppModalBottomSheet/BookTocBookmarkSheet/VerticalScrollbar/SwipeActionContainer/SplicedColumnGroup/SummaryCard/ThemedSnackbarHost/ListLayoutMenu（其中 ListLayoutMenu.kt 已实现 188 行完全符合 v2.7 交互——首行升降序/点同维翻转/String key/全 stringResource——但零接线，§3.1 P1 首接线 BookSourceActivity 未执行；SettingsSearchBar 设计规划接管四模块搜索但零接线）。② **公共组件库硬编码中文 4 处**（接线页全部继承，优先级最高）：PillNavigationBar Tab label（书架/发现/历史/我的）、SettingsSearchBar「搜索设置」、BookTocBookmarkSheet Tab（目录/书签）、SummaryCard 占位「书」。③ **debug 工具族 7 Screen**：硬编码中文 60 处（EncodeTools 19/HttpDebug 16/TimestampConvert 8/CurlTest 6/PingTest 6/RegexTest 4/DebugTools 1）+ 硬编码色 11 处（CurlTest 2/PingTest 5/RegexTest 4）+ 均无实施回执。**落盘**：ui-standards §6.1 存量清零清单补充（组件库 4 处 + debug 工具族 60/11 处）+ §10 变更记录 v2.10；pages-inventory C19 debug 工具族标注 v2.8 复审 3 违例；孤儿接线维持 §3.1 计划（P1 里程碑前置），debug 工具修复归 P4 一致性巡检。
- [x] 12.16l P1 支干首两页（书源 C1 + 订阅源 D1）View 现状预审（用户指令继续预审 View 层页面，选择「两页深审后落盘」）：2 个 explore 代理并行深审。**C1 BookSourceActivity（7 文件+2 XML）**：骨架判定 S2 列表管理页（规范 §2 S2 样板页指定本页）。已符合 10 项（SelectActionBar 批量栏唯一接线公共组件/DragSelectTouchHelper 滑选/DiffUtil 规避 §4.3/主题 token/返回键层级/方向跟随/文件夹网格自适应/检查源进度反馈/三视图超 fork）。违例 14 项：V1 顶栏 TitleBar 非 GlassTopAppBar；V2 SearchView 非 SettingsSearchBar+搜索词不升 VM；V3 ListLayoutMenu 零接线+**三套排序逻辑并存**（顶栏菜单 7 维/配置对话框 6 维/旧 enum 6 维，menu 项重置 bookSourceSort=0 走 enum，交互非首行升降序+点同维翻转）；V4 文件夹子目录替代 GroupHeader 折叠；V5 VM 零数据流（14 私有状态+4 adapter 散落，Room Flow collect 在 Activity）；V6 多选不持久化+无 BackHandler 退多选；V7 i18n 硬编码 8 处（L545/L864-866/L728/L821/L259/L45/view_search.xml:19+strings.xml 英文文件中文值）；V8 硬编码色 3 处（GREEN/RED/argb 蓝）；V9 8 类弹窗全私有 alert；V10 条目 PopupMenu+OptionsMenu 未下沉；V11 三态缺失；V12 defaultQueryHint 硬编码；V13 网格固定列数非断点；V14 无回执。fork 差距：GroupHeader/分组 Chips/统计条/空态 CTA/卡片 SourceItem/BackHandler/剪贴板嗅探缺失。**D1 RssSourceActivity（7 文件+5 布局+3 菜单）**：骨架判定 S2。已符合 12 项（C1-C12 三视图超 fork/滑选/批量 11 项/拖拽/筛选词全资源/主题 token/DiffUtil+§4.3 规避）。违例 20 项（V1-V20）：V4 排序维度三套不一致（菜单 5/对话框 6/代码 7）；**V5 升降序不持久化（AppConfig.rssSortAscending 已定义但全仓无读写=死配置）**；V6 rssSort Int 非 String key+RssSourceSort enum 死代码；V7 无 GroupHeader/Chips；V8 10 私有状态+VM 零 UI 态；V11-13 i18n（view_search 搜索/ImportRssSourceDialog 新增/更新/已有/英文 strings.xml 中文值 9 key）；V14 网格遮罩 argb 蓝；V15 8 类弹窗私有；V16 PopupMenu；V17 触控目标 <48dp 被 tools:ignore 掩盖；V18 固定列数；V20 无回执。fork 差距：RuleListScaffold/GroupHeader/Chips/统计条/空态 CTA/BackHandler/ListLayoutMenu 缺失。**落盘**：pages-inventory C1/D1 条目标注 v2.8 预审（14+20 违例明细+P1 开工依赖序）；迁移路径=C1 作 S2 支干样板，D1 作枝叶复用。两页均为「规范组件 0 接线」现状，功能面完整超 fork，差距集中在组件接线与状态收敛。
- [x] 12.16m P1 支干次两页（书籍详情 B6 + 发现 A7）View 现状预审（用户指令继续预审 P1 View 页）：2 个 explore 代理并行深审。**B6 BookInfoActivity（4 文件+5 XML+5 Dialog）**：骨架判定 **S4 详情页**（正确）。已符合 8 项（骨架归类正确/功能零遗漏含四渲染+WebFile+16 菜单/Kotlin 零硬编码色/方向跟随+双布局/底部操作 ≥48dp/协程链/封面背景图 contentDescription/简介 textIsSelectable）。违例 13 项（V1-V13）：V1 顶栏 TitleBar themeMode=dark 非 GlassTopAppBar；V2 底部操作栏纯 TextView 非圆角按钮组+换源/分组小 accent 文本未下沉；V3 无共享折叠封面（Hero AD-08 默认关决策未落地）；V4 无多 Tab 信息流/目录预览（目录跳全屏 TocActivity）；V5 状态未收敛（VM 两 LiveData+可变公有字段+Activity 直写 VM 字段 :160/:204-206/:502）；V6 i18n 硬编码 15 处（上传中...../未配置webDav/下载中.../书源不存在×2/源变量注释/Unexpected webFileData/Loading...../未找到书籍/已下载/清理缓存出错+EditVM 2 处）；V7 layout-land #50000000 硬编码色绕过 token；V8 6 类弹窗全私有（删除确认自定义 CheckBox/WebFile selector/ChangeBookSourceDialog/GroupSelectDialog/ChangeCover/Variable/WaitDialog）；V9 顶栏 OptionsMenu 16 项+手写显隐状态机未下沉；V10 三态不齐（加载文字/失败仅 toast/无 EmptyStatePlaceholder）；V11 无障碍错位（ic_book_last/ic_groups/ic_folder_open 复用 read_dur_progress=Origin 语义错误+触控约 20dp<<48dp）；V12 加书架/删除无微交互动画（MoRealm 心跳 spring 未落地）；V13 无回执。fork 差距：MoRealm 心跳/加架过渡/HapeLee 封面 Hero/ReaderBookSheet 三 Tab 全缺失（设计已入文档未落地）。S4 样板首接线：共享折叠封面+底部操作栏+AppModalBottomSheet 换源/分组+GlassTopAppBar+三态+Dialog/菜单族一次走通后冻结为枝叶范本。**A7 ExploreFragment（8 文件）**：骨架判定 **S2**。已符合 7 项（无硬编码色/全 Room Flow/文件夹网格自适应/主文案全 @string/三件套/协程链/返回键层级）。违例 17 项（V1-V17）：V1 SearchView 非 SettingsSearchBar+搜索词不升 StateFlow；V2 私有配置 Dialog（223 行承载 sourceGroupStyle/Mode/sourceLayout/bookSourceSort/sourceMargin）非 ListLayoutMenu；V3 条目 PopupMenu 非 AppMenuSheet；V4 删除私有 alert；V5 TextDialog 非 AppTextDialog；V6 状态未收敛（VM 26 行零数据流+Fragment/Adapter 散落状态含 Adapter 五份运行时态）；V7 英文 strings.xml 中文值（type_* 5 key+source_group_mode*+layout_list_compact）；V8 view_search.xml:19 搜索硬编码（共享 17 页继承）；V9 日志中文；V10 无 GroupHeader；V11 无多选批量（归 P2）；V12 空态裸 TextView 非 EmptyStatePlaceholder；V13 列数像素除法非断点+无骨架屏；V14 触控不足 tools:ignore 掩盖；V15 冗余（rgLayout 改 sourceLayout 但发现页不消费=半无效）；V16 无回执；V17 顶栏 TitleBar。**内核红线**：探索控件 JS 双求值链 evalUiJs/evalButtonClick/InfoMap LruCache(99) 跨模块引用/sourceKinds 缓存/SourceLoginJsExtensions 桥四条**逐字平移**；FlexboxLayout→Compose FlowRow 保留 FlexChildStyle.layout_justifySelf 语义；**ExploreKind.equals 只比 5 字段不含 chars/viewName/style=§4.3 陷阱适用**。P1 接线前置序：V8→V1→V17→V2→V3/V4/V5→V10→V7/V12/V16。**落盘**：pages-inventory B6/A7 条目标注 v2.8 预审（13+17 违例明细）。
- [x] 12.16n P1 支干编辑器对（书源编辑 C2 + 订阅源编辑 D2）View 现状预审（用户指令继续预审 P1 View 页，上一答「继续预审剩余 View 页」）：2 个 explore 代理并行深审。**C2 BookSourceEditActivity（3 kt+3 布局+菜单+关联内核，S3 支干样板页）**：骨架判定 **S3 正确**。已符合 7 项（C1-C7）：功能点一一对应（6 Tab 13+11+10+11+10+11 字段/快捷工具条/自动补全/KeyboardToolPop/全屏编辑 360ms 光标恢复/保存校验+URL 变更书架迁移/未保存拦截）；未保存用 `BookSource.equal` 26 字段全对比（非 equals 只比主键，天然规避 §4.3 陷阱）；保存校验抛 NoStackTraceException；全包 grep 硬编码色 0 命中全走主题 token；无 requestedOrientation+insets 双轨；RuleComplete.autoComplete 调用链完整（6 Tab type 1/2/3 映射 Activity:464-601）+CodeView 三 pattern 每项注册；段评 Tab 已裁剪且布局 gone 一致。违例 13 项（V1-V13）：V1 顶栏私有 TitleBar 非 GlassTopAppBar（xml:9-13）；V2 菜单 15 项全 OptionsMenu+onMenuOpened 手写显隐（source_edit.xml:1-86+Activity:118-127/162-205）未下沉 AppDropdownMenu；V3 **非 ViewPager**（TabLayout+单 RecyclerView onTabSelected 整体换数组 Activity:247-249/280-292），Compose 选型 TabRow(6)+LazyColumn vs HorizontalPager 开工前裁决；V4 状态散落（6 EditEntity 私有数组 Activity:75-80+TextWatcher 直写 editEntity.value Adapter:89-91+VM 无 Flow/StateFlow）；V5 无 rememberSaveable/onSaveInstanceState——旋转/进程死亡丢未保存编辑（Activity:103-109）；V6 6 类弹窗全私有（退出确认 alert/迁移确认/分组 selector/UrlOptionDialog/VariableDialog/KeyboardAssistsConfig）；V7 i18n 硬编码 12 处（L647-663 五 SelectItem/L740,755 日志/L779 源变量注释/VM:91,138/ L327 "jsLib" hint 字面量）；V8 英文 strings.xml 中文值 3 key（:1124 variable_comment/:1469 is_event_listener/:1472 custom_button，values-zh 缺）；V9 无障碍（item_source_edit.xml:14 tools:ignore=TouchTargetSizeCheck+TabLayout SpeakableTextPresentCheck+KeyboardToolPop 图标无 contentDescription）；V10 顶部快捷工具条两行 HorizontalScrollView 未分组（xml:15-107）+cb_is_enable_review 死字段；V11 净化无独立二级页（replaceRegex 仅正文 Tab 内联，范围待产品确认）；V12 **文档冲突**：pages/P5-booksource.md:40「编辑页暂不 Compose 化」vs ui-standards §9.2 指定本页 S3 样板——开工前裁决（倾向 §9.2）；V13 无回执。**内核红线**：RuleComplete.kt 纯函数零 UI 依赖（type 映射表逐行原样搬迁+autoComplete 作 VM 持久态）；CodeView 6 Pattern 用 **AndroidView 桥接原样实例**+每 CodeView 只注册一次+TextWatcher 桥接受控输入；KeyboardToolPop（PopupWindow 5+ 页复用）原样保留仅复刻 insets 接线。**fork 差距**：fork 均无编辑页 Compose 样板，仅管理页外推（MoRealm 校验 N/4+剪贴板嗅探 P2）。**P1 建议序**：裁决（V12/V3/V11）→ 里程碑1 骨架壳（V1→V2→V10 工具条收敛 SettingsCard+删死字段）→ 里程碑2 状态收敛（V4/V5+getSource() 移 VM）→ 里程碑3 弹窗族（V6）→ 里程碑4 i18n/无障碍（V7-V9）→ 回执（V13）。**D2 RssSourceEditActivity（3 kt+3 布局+2 菜单+debug 页+共享内核）**：骨架判定 **S3 正确**。已符合 9 项（C1-C9）：S3 归类/硬编码色 0/方向跟随/字段 label 全 R.string+menu 15 项全资源/菜单动态显隐正确（:136/:137）/未保存拦截语义正确/无违规动画/暗色全资源色/无私有 PopupMenu。违例 20 项（V1-V20）：V1 顶栏 TitleBar 非 GlassTopAppBar（xml:9-15）；V2 无 SettingsCard 分组+无底部保存条（xml:17-144）；V3 :225 `text="WEB_VIEW"` 硬编码；V4 :401 shouldOverrideUrlLoading hint 硬编码中文；V5 :501 源变量注释硬编码；V6 :521-525 helpActions 5 项硬编码中文；V7 VM:93 "格式不对"；V8 :337 "jsLib" 非资源（P2）；V9 英文 strings.xml 中文值（:1425 source_tab_start+arrays.xml:16-28 layout_type/rss_type 仅中文，Spinner 全语言显示中文）；V10 source_parse_concurrency 无 values-zh（P2）；V11-13 关联 debug 页硬编码中文 3 处+布局 xml 7 处+TextDialog/KeyboardToolPop/KeyboardAssistsConfig 共享组件（D3 P3 迁移时清）；V14 状态散落（VM 2 var+Activity 4 ArrayList+Adapter 双态+TextWatcher 写回）；**V15 真实功能缺陷：parseConcurrency 双源冲突**（顶栏 0..32 coerce :418-419 被列表字段旧值覆盖 0..20 :435-436，顶栏编辑值保存时静默丢失，上下限 32/20 不一致）——P1 修复；V16 死菜单项 menu_search（:30-33 无处理分支）；V17 弹窗 6 类全存量私有；V18 触控目标 <48dp tools:ignore 掩盖（xml:36,44,52,60,91,106,125）；V19 debug 页三态不合规（RotateLoading 非骨架+无空/错态 P2）；V20 无回执。**内核红线**：RuleComplete 7 保存调用点（:455-482 type 2/1/1/1/1/3/1）原样搬 Compose VM；KeyboardToolPop **中高风险建议 AndroidView 桥接保留整条 View 栈（IME 高度探测+undo/redo 走 EditText API），P1 不重写 IME 探测**；CodeView R.color.md_* 豁免登记。**fork 差距**：无（与 base 同构，本仓新增仅 Issue-1 并发继承显示+Issue-5 引入 V15）。**P1 必清 13 项**（V1-V7/V9/V14-V18/V20），P2 随行 4 项（V8/V10-V13），完成门禁 V20 回执。**文档修正**：原记「基础17字段」实测 16 项（:322-337）。落盘：pages-inventory C2/D2 条目标注 v2.8 预审+§H 变更 v2.9。两页均为「内核红线明确+状态散落+弹窗全私有」，与 C1/D1 同构——S3 样板页 C2 开工即冻结编辑页范本（D2 作枝叶复用），parseConcurrency 双源冲突建议开工前置修复。
- [x] 12.16o P1 支干导航壳+订阅页（主框架 A1 + 订阅 RssFragment A8）View 现状预审（用户指令继续预审 P1 View 页）：2 个 explore 代理并行深审。**A1 MainActivity（509 行+XML+ThemeBottomNavigationVIew+PillNavigationBar+MainViewModel，S1 支干样板页）**：骨架判定 **S1 主导航壳**（正确）。已符合 12 项（✅1-✅12）：configChanges 声明旋转不重建/pagePosition 天然存活/无 requestedOrientation/沉浸式 VMBaseActivity fullScreen/无硬编码色（grep 0+@color/background 昼夜双值）/可隐藏 Tab upBottomMenu:392-412/Tab 重选手势 onNavigationItemReselected:172-190/Tab 重选手势（书架回顶+发现收起 300ms 双守卫）/返回键优先级链:101-121/换肤走 RECREATE 即时切换（AD-18 合规，EventBus.REPAINT 不存在实际用 RECREATE）/底部 inset:202-206 windowInsetsListener/不引 Nav3 决策一致/功能点 14 项逐一落点。违例 10 项（V1-V10）：V1 **PillNavigationBar 零接线**（孤儿，grep 全仓仅自文件引用，现状用 BottomNavigationView activity_main.xml:16-24）——S1 样板首接线点即本页；V2 硬编码中文 :301 崩溃提示正文未走 R.string；V3 硬编码 hint :279 "password"；V4 **PillNavigationBar 自身硬编码中文+结构失真**（:171,176,181,186 defaultTabs() 写死「书架/发现/历史/我的」且「历史」与真实四 Tab「书架/发现/订阅/我的」不符已失真+:156 fontSize=10.sp 违 §1.1）接线前废弃 defaultTabs() 改接线页显式传 tabs+stringResource；V5 无回执；V6 **Tab 位置跨 recreate 不保活**（onSaveInstanceState:331-336 仅存 isAutoRefreshedBook，recreate:351-356 后 upHomePage:414-427 无条件覆盖——「我的」页切主题被打回默认首页；修复=savedInstanceState 存 pagePosition+recreate 已存跳过，Compose 等价 rememberSaveable）；V7 私有弹窗布局 4 处（setLocalPassword:277-293 DialogEditTextBinding/notifyAppCrash:301/backupSync:321 alert/upVersion:252-263 TextDialog）；V8 角标 View BadgeView:359-364 未用 BadgeDot（PillNavigationBar 接线后 PillNavTab.badgeCount 接管可退役）；V9 S1 壳未包 LegadoTheme（View 走 ThemeStore 旧 5 色+recreate，M3 34 槽位未接入）；V10 弃用 API+勾选同步 hack（@file:Suppress(DEPRECATION)+FragmentStatePagerAdapter+:441 menu[realPositions[position]].isChecked=true 把 ID 当下标+返回 false 监听器与 onPageSelected 二次纠正耦合——受控组件接线后天然消除）。**PillNavigationBar 接线方案**：① activity_main.xml 底栏槽位换 ComposeView+LegadoTheme{PillNavigationBar}（ViewPager 不动）② selectedTab←pagePosition/onTabSelect←setCurrentItem(index,false) ③ tabs 动态按 showDiscovery/showRSS 过滤+badgeCount←onUpBooksLiveData.value ④ BackHandler 双击退出链:101-121 保留 ⑤ insets windowInsetsPadding(navigationBars) 对齐:202-206 ⑥ 前置清理 V4/V5/V6。风险：PillNavigationBar 未包 LegadoTheme（读 MaterialTheme.colorScheme:61-65）调用方必须提供主题；badgeCount=-1 纯圆点语义与 setBadgeCount 计数角标需对齐。fork 差距：PillNavigationBar 简化版缺滑动指示圆点/combinedClickable 长按/tabExtras 插槽/state 读下沉防重组/1dp 顶白高光（MoRealm）；导航缺 Tab 切换连点节流（MoRealm 500ms）。**A8 RssFragment（3 kt+SourceFolderAdapter+4 布局+2 菜单+ReadRecordDialog，含 fork 基线 4c3935cf5 diff）**：骨架判定 **S2 列表管理页**（正确）。已符合 12 项（C1-C12）：文案全 R.string/Kotlin 硬编码色 0/搜索提交跳全局 RssSearchActivity/分组 6 分支 DAO+F-01 解耦/文件夹网格自适应/返回键子目录回退/方向跟随/execute 协程链/触控 ≥48dp/暗色安全/菜单项全资源化。违例 16 项（V1-V16）：V1 顶栏 TitleBar 非 GlassTopAppBar（fragment_rss.xml:9-16）；V2 私有 SearchView 非 SettingsSearchBar+query 未升 VM；V3 布局切换+排序私有（showFolderConfig:313-338+SourceFolderAdapter:74-133 私有 alert）ListLayoutMenu 零接线；**V4 功能性缺陷：rssSort 排序 no-op**（AppConfig:370-374 rssSort:Int，RssSourceDao:62-137 全部 order by customOrder——排序设置本页完全不生效；RssSourceSort enum 死代码:3-5；rssSortAscending 全仓无读写=死配置，与 D1 V5 同款）——P1 优先修复；**V5 功能性缺陷：sourceLayout no-op**（rgLayout 存 AppConfig.sourceLayout 但 applyListView 恒 GridLayoutManager(context,4) :241-245 布局选项保存后不生效）；V6 分组无 GroupHeader（TabLayout:258-292+文件夹卡无数徽标 SourceFolderAdapter:25）；V7 状态管理违规（Fragment 12 私有态:77-105+2 Flow Job:102-103+Room Flow collect 在 Fragment:357-419，RssViewModel 零状态零 StateFlow，旋转丢筛选态）；V8 三态缺失（fragment_rss.xml:37-49 tv_empty_msg 死控件零引用+:360-361,413-414 仅 AppLog）；V9 弹窗族全私有 3 处（del 确认:499-506+配置 alert+ReadRecordDialog:59-68）；V10 菜单族（RssAdapter:64-79 PopupMenu+OptionsMenu:162-186 未下沉）；V11 i18n 硬编码 6 处（:361/:414 日志中文/view_search.xml:19 搜索/strings.xml:1752,1754,1755 英文文件中文值/1761-1763 source_group_mode/RssSourceDao:71,102 SQL `like '%未分组%'` 数据层硬编码需迁移方案/g type_web 与 rss_article_type_web 同义双 key）；V12 XML 硬编码色 3 处（item_source_folder_grid.xml:54 shadowColor #80000000+:58 @android:color/white）；V13 无障碍（view_search.xml:9 height=30dp+dialog_source_folder_config.xml 17 处 tools:ignore）；V14 网格固定 4 列（:243+fragment_rss.xml:33-34 spanCount=4）；V15 无回执；V16 无视图切换轻过渡（P2）。fork 差距：相对 fork 基线已领先 +280 行（统一搜索/文件夹视图/Tab 标签/类型分组/子目录回退）；相对审定 3 fork 缺 RuleListScaffold+FastScroll（VerticalScrollbar 孤儿零接线）/GroupHeader/统计条+空态 CTA/BackHandler 退多选/ListLayoutMenu/左滑 SwipeActionContainer。**P1 开工序**：先修 V4/V5 功能性缺陷（否则 ListLayoutMenu 接线后依然不生效）→ BookSourceActivity S2 样板冻结后照抄 V1-V3/V6-V15 → V16 入 P2。六公共组件本页全零接线。落盘：pages-inventory A1/A8 条目标注 v2.8 预审+§H 变更 v2.10。**修正**：恢复此前误删的 12.17 行（12.16n edit 时丢失）。
- [x] 12.16p P1 支干目录+搜索+主题配置（B2 TocActivity + B11 Search 页族 + E2 ThemeConfigFragment）View 现状预审（用户指令继续预审 P1 View 页）：3 个 explore 代理并行深审。**B2 TocActivity（13 kt+BookTocBookmarkSheet+6 布局+菜单+4 Dialog）**：骨架判定 **S2 列表管理页（现状）+S5 浮层（蓝图目标）**——短期按 S2 治理（S2 样板确立后枝叶），中期迁入阅读器浮层 ReaderBookSheet 三 Tab（P2-reader R2），双归类并行。已符合 14 项（C1-C14）：功能点齐全（目录搜索/反转/替换标题/字数/拆分长章/正则/导出书签/MD/Obsidian/日志 TocActivity:128-174）/双 Tab Room Flow 实时订阅/协程 execute 链/菜单全 @string/Tab 标题 getString/EventBus.SAVE_CONTENT 缓存云精确更新（ChapterListFragment:102-119）/DiffUtil 逐字段规避 §4.3/长列表性能 payload+ConcurrentHashMap/Kotlin 硬编码色 0/返回键层级/HandleFileContract 导出/高亮 Tab 复用布局/全 @color token/目录功能超越 fork。违例 14 项（V1-V14）：V1 顶栏 TitleBar 非 GlassTopAppBar（activity_chapter_list.xml:9-14）；V2 appcompat SearchView 非 SettingsSearchBar+searchKey 未升 StateFlow+menu_search 无 queryHint；V3 目录/书签 TabLayout+ViewPager 与 BookTocBookmarkSheet 零接线（中期随 R2 收敛）；V4 状态未收敛（VM 仅 MutableLiveData<Book>+3 回调接口，Fragment 散落 4 私有态无 onSaveInstanceState）；V5 搜索词驱动链非受控；V6 i18n 硬编码 9 处（TocViewModel:97,99,121,123+BookmarkFragment:64+HighlightFragment:77+TxtTocRuleActivity:114+TxtTocRuleDialog:119+TxtTocRuleEditDialog:81,88,161,164）；V7 占位符拼接非 %1$s（ChapterListFragment:88）；V8 硬编码色 item_highlight.xml:16 #80FFF176；V9 弹窗族全私有（WaitDialog/TxtTocRuleDialog 0.9/0.8/BookmarkDialog/HighlightNoteDialog 全屏/alert+DialogEditTextBinding）；V10 菜单族 OptionsMenu 手写显隐:109-126+PopupMenu 未下沉；V11 三态全缺（无骨架屏/空态裸空白/错误仅 AppLog）；V12 无障碍 36dp<48dp（fragment_chapter_list.xml:32,45,56）；V13 无回执；**V14 高亮 Tab 未接线**（HighlightFragment 已建 108 行但 getCount()==2 不含 :210-212；highlightCallBack 定义但搜索分发无高亮分支 :90-98；updateLog:484 声称已上线与现状不符=功能回退）——P1 高优先止血。**BookTocBookmarkSheet 接线评估**：组件 99 行双 Tab 纯 String 列表能力覆盖不足 20%**不能直接接线否则功能回归**；正确路径=定位为阅读器浮层双 Tab 雏形 R2 增强演进 ReaderBookSheet 三 Tab（HorizontalPager 72%+CardTabRow+当前章态，from HapeLee），TocActivity P1 先治理 S2 枝叶，功能点随后作 ReaderBookSheet 数据源迁移，最终独立 Activity 收敛进浮层。组件硬编码中文「目录/书签」:58,64 接线前清。fork 差距：fork 新增高亮系统 F-P1-2/Obsidian 导出/TxtTocRule 独立页/4 增强配置属正向；蓝图缺 ReaderBookSheet 三 Tab/高亮 Tab 接线/GlassTopAppBar+三态+回执/当前章进度条。**P1 开工序**：V14→V1/V2→V11/V6/V7/V8→V4/V5→V9/V10/V12（P2）→V13 回执。**B11 Search 页族（18 kt+12 布局+5 菜单+2 实体，含 SearchContent/RssSearch/RssArticleInfo）**：骨架判定 **S2**（B11 一致；**归类疑点 D5 将 RssArticleInfoActivity 混归 S2 实际为 S4 详情页** RssArticleInfoActivity:39-45 自述仿书源需裁决）。已符合 14 项（C1-C14）：硬编码色 0/方向跟随/暗色安全（applyThemeColors :118-136）/DiffUtil payload 规避 §4.3（areContentsTheSame 恒 false 绕开 equals 只比主键）/日志规范/execute 链/菜单全资源/历史类型隔离 type0/1/防抖 ConflateLiveData(1000)/搜索范围持久化/空结果引导闭环 :438-459/缓存安全 SearchBookStoragePolicy 512KB/触控 ≥48dp/订阅源搜索为 fork 正向资产。违例 21 项（V1-V21）：V1 三页顶栏全 TitleBar 非 GlassTopAppBar；V2 androidx SearchView 私有子类（SearchView.kt 110 行）搜索词不升 VM（view_search.xml 17 页共享）；V3 历史 FlexboxLayoutManager 非 Compose FlowRow；V4 状态未收敛（Activity 6 私有态+2 Flow Job+VM 5 LiveData+可变公有字段，Room Flow collect 在 Activity）；V5 SearchScope stateLiveData 非 StateFlow；**V6 全文搜索配置存 companion object 静态可变**（SearchContentViewModel:17-27+Activity 直写 companion:87-101 进程内全局共享，三搜索页均无 onSaveInstanceState 旋转丢全丢）；V7 三态全缺（2dp 线性条/空结果 alert 非占位/无重试）；V8 4 处私有弹窗（SearchScopeDialog 114 行/alertClearHistory/空结果 alert/ChangeRssArticleSourceDialog）；V9 OptionsMenu+手写动态分组状态机（onMenuOpened+menu.transaction 每开必重建）非 AppDropdownMenu；V10 用户可见硬编码中文 7 处；V11 日志硬编码 6 处；V12 view_search.xml:19 共享 17 页接线页继承（M1 前置）；V13 公共组件 SettingsSearchBar:53 搜索设置（M1 前置）；V14 英文 strings.xml 中文值 :1447 search_result；V15 占位符拼接非 %1$s；V16 私有 BadgeView 复制 BadgeDot 能力+FAB 停止图标手写显隐；V17 无障碍（chip 27dp<48dp item_fillet_text.xml:6-18+tools:ignore 掩盖）；V18 无 GroupHeader；V19 无多选批量（P2）；V20 未包 LegadoTheme；V21 无回执。fork 差距：功能面完整超 fork（流式聚合/书架模糊搜索/双类型隔离/精准搜索/分页/rss 统一搜索 fork 独有）；缺 MoRealm SearchResultCard 加架动画+字数/状态/收藏心/HapeLee RuleListScaffold 壳/MoRealm GroupModeChips+统计条。**P1 开工序**：M1（V12/V13 i18n 清零）→ M2（GlassTopAppBar+SettingsSearchBar+Dialog 族+AppDropdownMenu+EmptyStatePlaceholder 接线）→ M3（StateFlow 收敛 V4/V5/V6）→ M4（i18n/无障碍/BadgeDot/回执）。**E2 ThemeConfigFragment（+ConfigActivity 壳+ColorPreference+ThemeListDialog+ThemeConfig/ThemeSpec/ThemeStore）**：骨架判定 **S2 配置列表页**（E2 一致，无搜索需求豁免 SettingsSearchBar）。已符合 12 项（C1-C12）：骨架归类/**主题权威源迁移红线完全合规零越界**（34 槽位只在运行时推导 LegadoTheme:41-49→ThemeSpec:34-93 无写入；themeConfig.json 格式不改 Config 9 历史字段 save GSON 原格式；Pref 旧 key 不改 applyTheme:427-473 直读 cPrimary 等）/换肤 RECREATE 即时切换无全量动画/明暗校验正确 :89-108/页面层硬编码 UI 色 0/无私有 PopupMenu（仅 menu_theme_mode 1 项）/Pref listener 配对+runCatching/方向跟随/无障碍部分达标（48dp+contentDescription）/资源层双语主体合规（18 title/summary 全 @string）/阅读器红线未越界（仅 Pref+EventBus 不碰数据库）/背景图下载逻辑正确。违例 15 项（V1-V15）：V1 顶栏 TitleBar 非 GlassTopAppBar（activity_config.xml:8-13）；V2 页面零 Compose 未包 LegadoTheme；V3 状态管理无 VM+Flow（Pref 直读直写，ConfigViewModel 占位未被使用）；V4 themeList 私有全屏 Dialog（ThemeListDialog:23-120 0.9f 全屏，AppSelectDialog 明确替代）；V5 4 类私有弹窗（保存主题 DialogEditTextBinding/背景图三选 selector/模糊 SeekBar DialogImageBlurringBinding/删除确认 alert）；V6 NumberPicker 非 L2 族；V7 i18n 硬编码中文 7 处（:351 下载背景图片中.../:383 设定成功/ThemeListDialog:65,85/theme_list.xml:7/ThemeConfig.kt:120,278,290）；V8 硬编码英文字面量 :226 hint="name"；V9 主题子系统日志硬编码 4 处（ThemeConfig.kt:120,278,290,321）；V10 硬编码色 ColorPreference.kt:28,135,261 取色器默认值建议登记豁免（巡检）；V11 ThemeListDialog 三态不齐（RotateLoading 私有/空态裸 tv_msg）；V12 无回执；**V13 AD-01/AD-04 内置 4 套 ThemeSpec 未落地**（ThemeSpec.kt 仅 data class 无预设表，grep 米白|暖黄|纯黑 0 命中，themeConfig.json 仅暗夜紫）——功能范围问题**需用户裁决归 P1/P2**；V14 选主题双重重建隐患（applyConfig RECREATE+onSharedPreferenceChanged 再 upTheme 两次 :150-156 recreate 幂等 P1 优化）；V15 无障碍缺口（tools:ignore=SpeakableTextPresentCheck dialog_recycler_view.xml:27+selector 触控未确认）。fork 差距：MoRealm Room themes 表+6 内置**不引（生态/风险）**补 4 套预设待裁决；readerBackground 随主题实体**不学**（每书配色红线）；已采纳背景锚定中性面+RECREATE；HCT 不引。**P1 五件套**：V1→V2→V3→V4-V6→V7-V9+裁决 V13。落盘：pages-inventory B2/B11/E2 条目标注 v2.8 预审+§H 变更 v2.11。
- [x] 12.17 验收 KPI 补「回执完成率 100%」
- [x] 12.18 检查点8：用户审核深化迭代文档（回执+支干+审查报告，AskUserQuestion）——2026-08-11 用户答复「暂停本轮任务」，文档迭代已收尾（ui-standards v2.6/13 步检查清单），2026-08-16 用户指令「快速推进后续设计文档任务，编译通过即下一个」隐含放行，P1 支干实施已并行推进（12.19）
- [x] 12.19 **Phase P1 支干样板页实施全部落地（2026-08-16 核验封口）**——S2→BookSourceActivity 起，每页按 §3.3 回执 + FR-11 真机门禁。**S1-S6 六样板页代码落地核验**：S1 MainActivity（PillNavigationBar 接线，12.20/12.21 ✅）；S2 BookSource（BookSourceScreen 1006 行+Items 430 行已接线，12.16q/r R1-R5 全修，硬编码字号 0 残留 ✅）；S3 BookSourceEdit（composeTopBar/composeTabBar/composeQuickToolbar/composeFields/composeBottomBar 5 处 Compose 接线 ✅）；S4 BookInfo（composeTopBar+composeBottomBar 接线，12.23 ✅）；S5 阅读器浮层（ReaderBookSheet/ReaderViewport/TextActionSelectionMenu 等 12.2A-12.2F 组件已建+12.24 Sheet 化 ✅）；S6 Dialog 族（ImportSourceSheet+ComposeDialog 家族 8 类+BaseComposeDialogFragment+AppConfirmDialog+MultiSelectDialog 12.32-12.38 已建 ✅）。**FR-11 门禁**：S1 已真机验证（12.21），S2-S6 交 V-7 统一真机（用户指令暂缓真机，编译通过即推进）。
- [x] 12.20 **S1 支干样板页 A1 MainActivity PillNavigationBar 接线完成**（2026-08-12，本窗口）——按 12.16o 接线方案①②③④⑤⑥ 全量落地。**已修 V1-V6/V8/V10**：V1 孤儿接线=activity_main.xml 底栏 ThemeBottomNavigationVIew→ComposeView+bottomNavigationView.setContent{LegadoTheme{PillNavigationBar(...)}}（LegadoTheme.kt:22-24 单参确认，M3 34 槽位接入本壳 V9 一并覆盖）；V4 defaultTabs() 废弃删除（原写死「书架/发现/历史/我的」失真）+fontSize 10.sp→MaterialTheme.typography.labelSmall（§1.1 禁写死字号）；V6 保活=savedInstanceState 存 SAVED_PAGE_POSITION+"pagePosition"+onActivityCreated 已存则跳过 upHomePage；V8 BadgeView 退役=onUpBooksLiveData.observe{upBottomMenu()} 直接驱动 PillNavTab.badgeCount（原 addBadgeView/setBadgeCount 删除）；V10 勾选 hack 删除=onPageSelected 只 pagePosition/selectedTabState.value 赋值；V2/V3 i18n=strings.xml 新增 password（en/zh）+crash_detected_prompt（en/zh）；V5 回执=本条。**保留**：双击退出链（:101-121）、Tab 重选 300ms 双守卫（gotoTop/compressExplore）→ onBottomTabSelect(index) 重构。**新状态管理**：selectedTabState/tabsState mutableStateOf 驱动 PillNavigationBar；tabs 动态按 showDiscovery/showRSS 过滤（MenuBook/Explore/RssFeed/Person 图标，RssFeed 非 AutoMirrored 经 icons-extended 1.7.8 枚举确认）；insets=Modifier.windowInsetsPadding(WindowInsets.navigationBars)（原 windowInsetsListener bottomPadding 删除）；NOTIFY_MAIN 简化。**组件改动**：PillNavigationBar.kt 清 material-icons imports+defaultTabs+fillMaxSize 残留，现 158 行。**updateLog**：2 条 2026/08/12（优化=悬浮胶囊 Compose 底栏四Tab 动态显隐/书架角标/弹性动画/沉浸式/双击退出与重选回顶保留/Tab 保活；修复=文案合并）。**FR-11 门禁**：✅ 真机功能点覆盖完成（见 12.21）
- [x] 12.21 **S1 MainActivity FR-11 真机功能点覆盖验证通过**（2026-08-12，本窗口，MEmu 127.0.0.1:21503 + ai_tests\venv，包 io.legado.miss.app.debug）：✅ PillNavigationBar 渲染（四 Tab 正确文字 书架/发现/订阅/我的，胶囊 bounds[24,1169][776,1256]）；✅ V4 修复生效（真实 Tab「订阅」非旧 defaultTabs「历史」）；✅ Tab 切换双向（书架↔我的内容正确渲染 ProfileScreen3Level/书列表 b15_test）；✅ V6 recreate Tab 保活（「我的」页→切「黑白」主题→EventBus RECREATE→回 MainActivity 仍在「我的」页，未被 upHomePage 打回默认首页）；✅ 返回键双击退出（2×BACK→com.microvirt.launcher2）；✅ Tab 重选回顶分支（书架重选 gotoTop 无崩溃）；✅ 无 FATAL（多次 recreate/切换/双击）；⚠️ Tab 选中态高亮（uiautomator 不暴露 Compose capsule selected，截图目测为唯一缺口，代码受控组件 selectedTab 驱动正确）；⚠️ 书架角标 badgeCount（模拟器无等待更新书，核实 MainViewModel.kt:58/237-247 语义=showWaitUpCount 且有待更新书才 postValue，与旧 addBadgeView 一致，代码正确）。**实施回执（§3.3 AD-23）已在本条目 + 12.20 双写完成。**
- [x] 12.22 **主干公共组件库第二期建仓完成**（2026-08-12，本窗口，用户经 AskUserQuestion 选定「先补主干公共组件」）——`ui/widget/components/` 新增 **9 个组件**：**Dialog 族 6**（ConfirmDialog：M3 AlertDialog+destructive 确认钮 error 色；AppEditDialog：EditField(label/initial/hint/singleLine/isPassword) 多字段 OutlinedTextField 列表 onConfirm(List<String>)；AppSelectDialog：SelectOption(label/value) 单选 RadioButton 列表 selected 高亮 onSelect；AppNumberPickerDialog：IntRange Slider+数字输入双联动 confirm 钳制；AppTextDialog：isMarkdown→私有 MarkdownView(Markwon+Glide+HTML+Table) 否则纯文本；AppWaitDialog：Dialog+Surface+CircularProgressIndicator 阻塞等待 show=false 不渲染）+**菜单族 2**（AppMenuSheet：复用 AppModalBottomSheet 数据驱动 MenuAction(icon/title/tint/onClick) 长按富操作 BottomSheet；AppDropdownMenu：M3 DropdownMenu 封装 actions 列表）+**GroupHeader**（折叠箭头 AutoMirrored+组名 titleSmall Bold+启用数/总数徽标+MoreVert→AppDropdownMenu 组操作；onMenuActions 懒加载）。**签名对齐 implementation-spec §2.3.1 + fork 参考（AppConfirmDialog destructive 色/BaseComposeDialogFragment 教训）**；confirm/cancel 文案由调用方传 stringResource（§6.1 组件内零硬编码中文）；全走 MaterialTheme.colorScheme 零 Color(0x)；间距 4dp 档位、触控≥48dp。**编译验证**：首轮 5 处错误已修（KeyboardOptions 包名/AppMenuSheet 缺 @OptIn/AppNumberPickerDialog textAlign→textStyle.copy/AppTextDialog setMarkdown+GlideImagesPlugin import 包名/GroupHeader content ColumnScope→BoxScope），`assembleAppDebug` BUILD SUCCESSFUL。**updateLog**：cronet版本: 后顶部追加「公共组件库第二期：对话框族6+菜单族2+分组列表头」。**接线计划**：供 A8 订阅/A7 发现页及 S2 枝叶 BookSource 接线使用（随各页 P1 实施）。
- [x] 12.23 **S4 支干样板页 B6 BookInfoActivity 壳层接线完成**（2026-08-12，本窗口）——按 12.16m 预审「S4 样板首接线」采用「View 主体 + Compose 壳层桥接」范式（同另一 AI BookSourceActivity），简介四渲染（WebView 池/setHtml/Markwon/纯文本）依赖未动。**已修 V1/V2/V6/V7/V9/V11/V13**：V1 顶栏=activity_book_info.xml（+land）TitleBar→ComposeView(compose_top_bar)+GlassTopAppBar（title=book_info/ArrowBack 返回/MoreVert→AppDropdownMenu 16 项数据驱动 buildMenuActions()，onCompatCreateOptionsMenu/onMenuOpened/onCompatOptionsItemSelected 三方法全删，显隐状态机收敛进 buildMenuActions 按状态过滤：自定义按钮 hasCustomBtn/编辑 inBookshelf/登录 loginUrl 非空/源变量+书籍变量+允许更新 source!=null/拆分长章 isLocalTxt 勾选/上传 isLocal/删除提醒+允许更新+拆分长章带 checked 勾选态）；V2 底部栏=fl_action（tv_shelf+tv_read 50dp）→ComposeView(compose_bottom_bar)+Row 内 OutlinedButton（加/删书架，shelfBtnText mutableStateOf 由 upTvBookshelf 驱动）+Button（阅读）双 12dp 圆角 48dp 高（原 tvRead/tvShelf.setOnClickListener 迁移 Compose onClick）；V6 i18n=Activity 9 处+VM 7 处+EditVM 2 处共 18 处硬编码→strings.xml en/zh 新增 16 key（source_variable_comment/book_variable_comment/book_source_not_found/uploading/webdav_not_configured/downloading_dots/unexpected_web_file_data/loading_dots/book_not_found/download_remote_book_fail %1$s/downloaded/clear_cache_error/webdav_not_configured_alarm/load_toc_error %1$s/book_info_save_fail_duplicate \n%1$s/book_info_save_fail）；V7 layout-land #50000000→@color/transparent50；V9 菜单 16 项全下沉 AppDropdownMenu（MenuAction 扩展 checked:Boolean?=null，AppDropdownMenu 渲染 Icons.Filled.Check primary）；V11 无障碍=竖版+land 换源/分组/目录 3 个 AccentBgTextView 加 minHeight=48dp+gravity=center_vertical（6 处，触控从 ~20dp→48dp+）；V13 回执=本条。**组件改动**：MenuAction.checked+AppDropdownMenu 勾选渲染（向后兼容默认 null）。**编译验证**：首轮 3 错已修（Icons.Filled.Copy 不存在→ContentCopy；BookInfoEditViewModel getApplication() 泛型需显式 `<Application>`），`compileAppDebugKotlin` BUILD SUCCESSFUL（5m2s）+`assembleAppDebug` BUILD SUCCESSFUL（36s）。**updateLog**：cronet版本: 后顶部追加「书籍详情页顶栏磨砂 Compose 版+底部圆角按钮组+无障碍 48dp+i18n 双语」。**FR-11 门禁（MEmu 127.0.0.1:21503 + ai_tests\venv，包 io.legado.miss.app.debug）**：✅ 顶栏渲染（compose_top_bar+标题「书籍信息」+返回+MoreVert）；✅ AppDropdownMenu 弹出（分享/上传 WebDav/刷新/置顶/日志/删除提醒/清理缓存 按当前书状态过滤正确）；✅ 底部按钮组（两按钮 72dp 高=48dp+达标，含「阅读」）；✅ V11 触控 48dp+（换源 72dp/设置分组 72dp/查看目录 72dp）；✅ loading 态「目录：加载中…」；✅ 无 FATAL；⚠️ 菜单具体项点击（编辑/分享/上传/置顶/源变量/书籍变量）未逐项真机（均为原业务逻辑内联迁移）；⚠️ 阅读按钮点击结果受模拟器 UI 栈干扰（网络书 startReadActivity 分发，非本次改动缺陷）。**标注后续（本次未做，留枝叶/后续窗口）**：V3 共享折叠封面（AD-08 默认关）、V4 多 Tab 信息流、V5 状态收敛（VM 两 LiveData+公有字段）、V8 6 类业务弹窗（删除确认/WebFile selector/ChangeSource/GroupSelect/ChangeCover/Variable 均复杂业务 Dialog，L2 族 12.22 已备待枝叶接线）、V10 三态（loading 文字+toast 现状）、V12 微交互动画。**实施回执（§3.3 AD-23）本条目已双写（12.16m 预审 + 本回执）。**
- [x] 12.16q P1 支干首个实施（另一 AI 窗口 BookSourceActivity Compose 化）DeepReview（2026-08-11，用户入睡前指令「审查另一 AI 标记完成项代码，不符合则补任务项/设计思路，循环直到符合」）：另一 AI 采用 ComposeView 桥接双轨过渡（View 顶栏/菜单/批量栏保留 + 列表区 Compose 化，BookSourceScreen 607 行 + BookSourceItems 430 行 + activity_book_source.xml 双视图 + LegadoTheme 收敛 toM3Scheme ✅ 正向）。**已符合**：GlassTopAppBar/ListLayoutMenu/SettingsSearchBar/EmptyStatePlaceholder/SwipeActionContainer 公共组件接线（showTopBar=false 下 ListLayoutMenu 隐于 View 顶栏=过渡期可接受）；BookSourcePart 自定义 equals 仅比主键（data class :54 只比 bookSourceUrl）+LazyColumn key={bookSourceUrl} 规避 §4.3 ✅；三视图断点自适应（400/600/800dp）✅；左滑 SwipeActionContainer（编辑/调试/复制URL）复用公共组件 ✅；i18n 快捷筛选词已资源化由 BookSourceMoreMenu 传参 ✅；检查源状态实时回传 ✅。**新实施违例 5 项（须修）**：① **多选批量被架空**——Compose 侧 `isSelecting=false` 写死（BookSourceActivity:208）+`onItemLongClick` 改弹 PopupMenu（:259，原 View 长按=DragSelectTouchHelper 滑选入口进多选）+recyclerView 已 gone 但 ItemTouchHelper/DragSelectTouchHelper/SelectActionBar 仍挂死列表上→滑选多选+批量操作全失效（updateLog 声称「批量操作栏保留」与实际不符）；② **硬编码色 2 处未登记豁免**——BookSourceItems.kt:294-303 sourceCoverColorPalette 8 色 Color(0xFF)（与 BookshelfItems GeneratedCover 同款但未在 ui-standards §1.1 例外登记）+ :363-368 SourceTypeBadge 类型 4 色（#4CAF50/#2196F3/#FF9800/#E53935，语义色应走 ThemeSpec token 或登记）；③ i18n 硬编码中文「源」兜底（BookSourceItems.kt:335 `ifEmpty { "源" }`）未走 stringResource；④ 空态文案语义错位（BookSourceScreen.kt:225 EmptyStatePlaceholder title 用 bookshelf_empty 书架文案，应为书源专用空态文案）；⑤ 搜索词未升 StateFlow 双轨（Activity composeSearchQuery + Compose 本地 searchVisible，搜索词状态未收敛 VM）。**设计思路补充（反馈另一 AI）**：多选修复路径=Activity 维护 composeIsSelecting/selectedIds 状态→Compose 侧 SelectActionBarCompose 接线+onLongClick 进入多选+选中高亮；硬编码色统一走 ThemeSpec 或登记豁免；空态文案新增 book_source_empty_title。**落盘**：审查结论已入 pages-inventory C1 条目（v2.11 审查增量）。
- [x] 12.16r **P1 首实施修正门禁（给另一 AI 窗口的行动清单，用户 2026-08-11 确认「补任务项给另一 AI 修正」）**：完成 12.16q 五违例修正后方可勾选。**进度 2026-08-12 复审**：R1 已修（另一 AI 9:06 提交）——Activity 升 composeIsSelecting/composeSelectedUrls(:137)+composeSelection 计算属性，onItemClick 多选时 toggleSelect(:269)、onItemLongClick=enterSelect(:271)，onSelectAll/onRevertSelection/onDeleteSelection/onBatchAction 全接线(:277-284)，SelectActionBarCompose 完整实现（12 项 BookSourceBatchAction enum+批量菜单），View selectActionBar visibility=GONE(:937)，onBackPressed 双分支(:870-892)，exitSelecting/checkComposeSelectedInterval/exportSelection/shareSelection 齐全；BookSourceScreen.kt 已扩至 1006 行，isChecked=selectedUrls.contains(:321/342/367/576/587) 选中高亮接线。**R1a-R5 全部完成（2026-08-12 本窗口）**，详见下方子项。**后续**：交真机 FR-11 统一验证（V-7 统一验证子任务，用户指令不逐项验证）。
  - [x] R1a 滑选死接线清理（2026-08-12 完成）：`initRecyclerView()` 移除 DragSelectTouchHelper+ItemTouchHelper attach（recyclerView 已 gone 纯死接线），并同步删除 Activity 侧 `DragSelectTouchHelper` 死导入；updateLog 已声明「长按进多选、滑选降级为逐项勾选」
- [x] R2 硬编码色收敛（2026-08-12 完成）：`sourceCoverColorPalette` 8 色（BookSourceItems.kt:312-321）+ `SourceTypeBadge` 4 语义色（:381-387）已在 ui-standards §1.1 例外登记（③④ 条，同 GeneratedCover 封面范式登记豁免）
- [x] R3 i18n（2026-08-12 完成）：「源」兜底（:353 `ifEmpty { "源" }`）改 `stringResource(R.string.book_source_cover_fallback)`，新增 en/zh 双语 key
- [x] R4 空态文案（2026-08-12 完成）：BookSourceScreen.kt 3 处空态（groupSourcesByDomain/isFolder/chips 平铺）标题 `bookshelf_empty` → 新增 `book_source_empty_title`/`book_source_empty_subtitle` 书源专用文案（3 处全改）
- [x] R5 搜索态收敛（2026-08-12 复核判定满足）：桥接架构 showTopBar=false 下搜索由 View SearchView 唯一驱动，`onQueryTextChange` 同步 `composeSearchQuery` 作 Compose 只读镜像，`onSearchQueryChange` 回转 `searchView.setQuery` 幂等收敛，单源成立；Compose 本地 `searchVisible` 仅 showTopBar=true（独立壳）时渲染，桥接态不激活，无双源漂移
- [x] 12.16s **阅读 Archive（legado-archive，Rimchars 私有仓）深挖补证**（用户 2026-08-12 指令「继续学习其他开源阅读的细节，补充到我们的设计文档中呀！」；3 个 explore 子代理并行深挖）——**成果已入库**：① forks-deep-dive.md 新增 §9（6 小节：主题引擎/书架/我的页/书源管理/阅读器浮层/结论）；② ui-standards.md 升级 v2.11（新增 §4.4 多选状态收敛「isSelectMode 由选择集派生+数据回流剪除脏选中+批量栏 AnimatedVisibility」、§4.5 主题撞色守卫「toM3Scheme 后处理对比度 pass」、§4.6 书架快照缓存；§3 组件表新增 ListCard/ModernActionPopup/ComposeDialog 家族 8 类）；③ 收录我的页 Preference XML 深度搜索深链+区块级 ComposeView 渐进式浸润、书源智能导出（<30% 仅选中）。**关键采纳决策**：fork 的「快照缓存秒显+Set 派生多选态+底部批量栏+XML 搜索深链+撞色守卫」五件套直接指导书源/书架/我的三页改造；fork 缺空态/骨架屏/下拉刷新恰为目标项目已具备，互补不冲突。AI/角色卡/朗读三模式/云书库为前瞻参考不搬运。审计记录齐（tasks/forks-deep-dive/ui-standards 三处同步）。
- [x] 12.16t **Legado_Max（Suml-1/Legado_Max，Compose 128 文件）深挖补证**（续 12.16s 同指令；3 个 explore 子代理并行）——**成果已入库**：① forks-deep-dive.md 新增 §10（5 小节：首页模块化系统/Dialog 体系+设置页/阅读记录+热力图/规则执行流程可视化/结论）；② ui-standards.md §3 组件表新增 8 行：`BaseComposeDialogFragment` 基类（Dialog 族 6 统一宿主，优先于自造骨架）、`AppConfirmDialog`（destructive 色，14 处实战）、`MultiSelectDialog` 内容模型（覆盖多选类缺口）、Sheet 内多级导航模式（S6 多级弹窗方案）、`HeatmapCalendar`（★ 自包含纯 Compose 661 行+双模式+二次方强度配色，强烈建议搬运）、`SummaryCard+BookStackView`（成就卡）、`CommonPageColors` 深色自适应规范（luminance<0.18 判暗+lerp 抬升，MetricTile 需对齐）、拖拽排序模式（ReorderableItem+LaunchedEffect 持久化）。**关键采纳决策**：① 热力图搬运性价比最高——纯 Compose 自包含、无第三方、仅需 1 条 DAO GROUP BY date SQL（fork getDailyStats），作为统计卡页高价值低成本增量；② BaseComposeDialogFragment+AppConfirmDialog 直接指导 Dialog 族 6 落地；③ 模块化首页作为发现/书架页交互模型参考（S1 同 ViewPager 框架）；④ FlowLog 规则可视化价值高但需 41 处埋点侵入规则引擎核心，仅增量接入（开关默认关+书源自测回归）；⑤ 教训：fork 的 AppDialogScaffold 骨架全仓零引用=「抽象骨架不接线即负债」，Dialog 族以实际共性为准。审计记录齐（tasks/forks-deep-dive/ui-standards 三处同步）。
- [x] 12.16u **legado-with-MD3-DIY（325506，kt=1106 compose≈126 全量 Compose 仓）深挖补证**（用户经 AskUserQuestion 选定目标；3 个 explore 子代理并行：路由体系+关键功能页/主题引擎/Dialog 体系）——**成果已入库**：① forks-deep-dive.md 新增 §11（4 小节：导航路由体系/主题引擎/Dialog 体系/结论）；② ui-standards.md 升级 v2.13，§3 组件表新增 5 行：**Nav3 全局路由+Route 壳层模式**（单 Activity+Navigation3 14 sealed route+backStack 手动栈管理，壳层=副作用编排与渲染分离，底部 Tab HorizontalPager 不嵌套 NavHost 避免双栈）、`ListScaffold<T>`+`ListUiState<T>` 泛型列表模板（DynamicTopAppBar 搜索/下拉+选中 SelectionBottomBar+FAB）、`AppAlertDialog`/`AppModalBottomSheet` 双引擎封装（Miuix vs M3 按引擎切换+data 重载退出动画）、`AlertBuilder<D>` DSL（Confirm/Select 统一入口）、7 导入家族三处分叉模式（泛型 BatchImportDialog<T>）。**关键采纳决策**：① BookInfoRoute 壳层+ListScaffold 泛型模板为 P1 书源/书架改造直接范本；② AppAlertDialog 双引擎+AppModalBottomSheet 是 Dialog 族可直接搬范本；③ 7 导入家族三处分叉=目标书源/订阅源/净化/词典 4 个导入对话框统一化样板；④ 主题引擎 14 模式预置定义化+种子色/风格/对比度三维覆写供 AD-18 参考但不全量搬（48×2 硬编码维护成本高，保留 AD-18 公式+撞色守卫）。审计记录齐（tasks/forks-deep-dive/ui-standards 三处同步）。
- [x] 12.16v **huajideshutiao/legado 深挖补证**（净化规则源三件套为全 fork 独有；子代理通道多次中断改前台直读源码）——**成果已入库**：① forks-deep-dive.md 新增 §12（5 小节：数据模型/生效引擎/UI 三件套/IBottomDialog/结论）；② ui-standards.md 升级 v2.14，§3 组件表新增 2 行：`SearchBookFilter` 快照过滤引擎（★ @Volatile 快照+惰性预编译 Regex+@Synchronized ensure，apply 返回丢弃数，rulesInScope 列生效规则）+ `SourceFilterRule` 数据模型（Field enum 5 种 NAME/AUTHOR/INTRO/KIND/WORD_COUNT，Scope sealed 4 态，规则字段 OR+多规则黑名单，scope 范围协议 空=全部/含::=单源/CSV=分组）。**关键采纳决策**：① 目标项目暂无「搜索/发现结果过滤」能力，huajideshutiao 给出完整闭环（数据模型+快照引擎+Activity/EditDialog/ImportDialog 三件套）可完整借鉴——引擎性能快照模式与 §4.6 书架快照缓存同思路；② 导入三态判定（新增/更新/已有 比较 pattern/fields/scope）复用 Import 家族模式；③ 编辑弹窗作用范围复用 SearchScopeDialog（与搜索范围选择同源）=「弹窗复用既有选择器」范例。审计记录齐（tasks/forks-deep-dive/ui-standards 三处同步）。

- [x] 12.24 **Phase4 阅读器浮层 Sheet 化完成**（2026-08-12，本窗口）——PR-4.1 目录/书签浮层 + PR-4.2 高亮选色评估。**PR-4.1 实现**：activity_book_read.xml 新增 `compose_sheet_host` ComposeView（match_parent，默认 gone）；ReadBookActivity 新增 `showTocSheet/tocChapterTitles/tocBookmarkTitles` 状态 + `loadTocSheetData`（BookChapterDao.getChapterList→章节标题、BookmarkDao.getByBook→书签标题）+ `dismissTocSheet` + `setupTocSheet`（onPostCreate 调用，LegadoTheme{AppModalBottomSheet{BookTocBookmarkSheet 双 Tab + 章节列表 TextButton 保留「章节列表」完整目录入口启动 TocActivity}}）；`openChapterList()` 改为优先展示 Compose 浮层（快速跳转章节/书签），完整目录全功能入口保留（无回归）。**PR-4.2 评估**：高亮选色面板 HighlightStyleDialog 本身已是 BottomSheetDialogFragment 且含 6 色预设色板 + 下划线样式（kind），满足设计「高亮选色面板 BottomSheet 化 2 行 6 色」，无需再改造；选中文字工具条 TextActionMenu 属正文引擎交互（AD-02 N 不迁移）。**修复 3 错**：R.string.toc_rule_title 不存在→改用现有 chapter_list；AppModalBottomSheet 调用需 @OptIn(ExperimentalMaterial3Api::class)（setupTocSheet 加注解+import）；setupTocSheet 未接线→onPostCreate 调用。**编译验证**：`assembleAppDebug` BUILD SUCCESSFUL（3m24s 后修，EXIT=0）。**updateLog**：cronet版本: 后顶部追加「阅读器目录/书签浮层升级为底部抽屉（Compose 版）+完整目录入口保留」。**FR-11 门禁**：交 V-6 统一真机验证（用户指令不逐项验证）。**实施回执（§3.3 AD-23）本条已双写。**

- [x] 12.25 **S6 登录/导入 Dialog 族评估与实施前置分析**（2026-08-12，本窗口）——**SourceLoginDialog**（`ui/login/SourceLoginDialog.kt`）评估：**属 N 不迁移类**——它是**动态 JS 表单引擎**（据书源 loginUi 配置运行时生成 text/password/select/button/toggle 五类控件，flexbox.addView 动态建树 + `findViewById(index+1000)` 反向索引 + runScriptWithContext JS 执行 + 长按 666ms 触摸判定 + onDismiss 落库 loginInfo），Compose 化需重写整个动态表单引擎、收益低风险极高，建议标记 N 不迁移（类比正文引擎 AD-02）。**Import 系列 8 个 Dialog**（`ui/association/` 下 ImportBookSourceDialog/ImportRssSourceDialog/ImportDictRuleDialog/ImportReplaceRuleDialog/ImportTxtTocRuleDialog/ImportThemeDialog/ImportHttpTtsDialog）评估：结构高度同构（**BaseDialogFragment(R.layout.dialog_recycler_view) 共用布局** + ViewModel 解析源文本→allSources/checkSources/selectStatus + RecyclerAdapter 列表（勾选/备注展开/新增-更新-已有状态/编辑 CodeDialog）+ 底部全选/导入/取消 + Toolbar 菜单（新建分组/保持原名/保持分组/保持启用/显示备注））。**Compose 化方案**：新建通用组件 `ImportSourceSheet.kt`（数据驱动 `ImportItem(name/comment/state)` + selected 列表，复用 AppModalBottomSheet + stringResource i18n），8 个 Import Dialog 共用；接线需**新建独立布局**（勿改共用 dialog_recycler_view.xml，避免影响其余 7 个）、保留各 ViewModel 逻辑、菜单/分组子 Dialog 收敛。**风险与工作量**：中-大（共用布局隔离 + 多选状态桥接 + 菜单/分组子 Dialog + 8 个同构页复制），且无法真机验证，建议作为独立会话/窗口推进（复用 V-8 验证子任务统一真机）。**S3 BookSourceEditActivity 另评**：6 Tab + CodeView 编辑器 + KeyboardToolPop + JSON 字段编辑，CodeEdit 内核属 N 不迁移，全量 Compose 风险高，建议分阶段或局部（待定）。

- [x] 12.27 **页面设计文档 8 册全部升级 v2 模板（10 节）+ 84 子页面归属登记**（2026-08-13，本窗口，用户 m0088/m0171 指示「每页单独详细设计文档 + 主文档引用 + task 对应」）——**8 册全部 ✅ v2**：P1 书架（对齐 BookshelfScreen 已接线，BadgeDot/ShelfGridSkeleton/EmptyStatePlaceholder ✅，登记 V1 Config remember 帧固定 :81-85+V3 i18n）、P2 阅读器（壳-核分离三层架构+正文零改动 6 红线+activeSheet 单态+R0-R4 迁移）、P3 详情（对齐 12.23 已接线，登记 V3-V10 后续队列）、P4 我的配置（ProfileScreen3Level 混血现状，登记 V1-V4）、P5 书源（v2 样板 12.16 已立）、P6 发现（V1-V17+内核红线 JS 双求值）、P7 订阅（V1-V16+**双功能性缺陷 rssSort/sourceLayout no-op P1 优先**）、P8 浮层（S6 三层体系 L1 Sheet/L2 Dialog 族/L3 透明窗）。**84 子页面三级覆盖规范**（ui-standards §10 新增）：族文档规格 + pages-inventory 条目差异点 + tasks.md task 对应，不重复建 92 行完整文档。ui-standards §11 变更记录 v2.15。**文档索引**：README pages/ 行已更新列出 8 册。

- [x] 12.26 **S6 Import 系列样板落地：ImportSourceSheet 通用组件 + ImportRssSourceDialog 接线**（2026-08-13，本窗口，子代理实现+父代编译核验）——按 12.25 评估方案「新建通用组件 `ImportSourceSheet.kt` + 样板页优先」。**新建**：`ui/widget/components/ImportSourceSheet.kt`（数据驱动 `ImportItem(name/comment/state)` + `ImportState{NEW/UPDATE/EXIST}`，内部 `AppModalBottomSheet` 包裹：顶部标题+MoreVert→AppDropdownMenu、中部 LazyColumn 列表项（Checkbox+名称+状态徽标+Edit 按钮+备注展开 heightIn max440dp）、底部全选-取消/取消/导入 12dp 圆角 48dp 高；全 stringResource + colorScheme，禁硬编码色/中文）+ `res/layout/dialog_import_sheet.xml`（独立 ComposeView 宿主，**未改共享 dialog_recycler_view.xml**，其余 7 个 Import Dialog 零回归）。**改造**：`ImportRssSourceDialog.kt` 布局→dialog_import_sheet，保留 ViewModel 全部业务（importSource/importSelect/comparisonSource）；LiveData→Compose 桥接=Fragment 级 mutableStateOf + observe(viewLifecycleOwner)，普通 ArrayList 用 `toMutableStateList()` 镜像 + remember(successCount, editTick) 派生 items（规避 produceState 与 LaunchedEffect 双写死锁教训）；CodeDialog 编辑回写 + editTick 驱动重组。**i18n**：所需 key 全已存在，零新增。**编译验证**：`assembleAppDebug` BUILD SUCCESSFUL（EXIT=0，父代复核）。**updateLog**：cronet版本: 后顶部追加「订阅源导入弹窗升级为 Compose 底部抽屉」。**FR-11 门禁**：交 V-12 统一真机验证（用户指令不逐项验证）。**实施回执（§3.3 AD-23）本条已双写。** 其余 7 个 Import Dialog 待作为枝叶照此样板复用（同 ImportItem 数据模型 + 各自 VM 桥接）。
- [x] 12.26b **S6 Import 系列枝叶改造完成：其余 6 个 Import Dialog 复用 ImportSourceSheet**（2026-08-13，本窗口）——按 12.26 样板照抄：`ImportBookSourceDialog`（完整菜单族 7 项：新建分组/选择新增/选择更新/保留原名/保留分组/保留启用/显示备注，checked 勾选态驱动，`toggleSelectionBy` lambda 捕获 selectFlags）+ `ImportReplaceRuleDialog`（新建分组/保留原名 2 菜单）+ `ImportDictRuleDialog`（无菜单）+ `ImportTxtTocRuleDialog`（showComment=true 常显 example 备注）+ `ImportThemeDialog`（无菜单，Edit 仅预览）+ `ImportHttpTtsDialog`（无菜单）。**共性模式**：`BaseDialogFragment(dialog_import_sheet.xml)` + `onFragmentCreated` 内 `findViewById<ComposeView>(R.id.compose_view).setContent{LegadoTheme{...}}` + LiveData→Fragment 级 mutableStateOf 桥接 + `selectStatus.toMutableStateList()` 镜像 + remember(successCount, editTick) 派生 items（ImportState 新增/更新/已有判定各 VM 业务字段比较）+ CodeDialog 编辑回写 editTick 驱动重组；各 VM 业务（importSource/importSelect/comparisonSource）零改动，纯 UI 桥接。**残留检查**：grep 确认无 `setContentView`/`recyclerView`/Adapter 等旧 View 逻辑残留，仅 `findViewById<ComposeView>(R.id.compose_view)` 宿主桥接（正常）。**编译验证**：`assembleAppDebug` BUILD SUCCESSFUL（EXIT=0）。**updateLog**：cronet版本: 后顶部已追加「导入弹窗统一为 Compose 底部面板（书源/替换净化/净化词/目录规则/主题/在线朗读 6 个导入弹窗）」。**FR-11 门禁**：交 V-12 统一真机验证（用户指令不逐项验证）。**实施回执（§3.3 AD-23）本条已双写。** Import 系列 7 个（RssSource 样板+本 6 枝叶）全部完成，SourceLoginDialog 属 N 不迁移（12.25 已评估）。

- [x] 12.26c **S3 BookSourceEditActivity 阶段1：Tab 区 Compose 化（里程碑1 骨架壳先行）**（2026-08-13，本窗口）——按 12.16n 预审「里程碑1 骨架壳（V1→V2→V10）」+ 用户「全量渐进 Compose」决策推进首步。**已落地**：`activity_book_source_edit.xml` 顶栏 TitleBar→`compose_top_bar`+`GlassTopAppBar`（title=编辑书源/ArrowBack 返回/MoreVert→AppDropdownMenu 15 项数据驱动 `buildMenuActions()`，onMenuOpened/onOptionsItemSelected 显隐状态机收敛：登录项仅 loginUrl 非空显示、自动补全带 checked 勾选）；TabLayout→`compose_tab_bar`+`PrimaryScrollableTabRow`（6 Tab 基本/搜索/发现/详情/目录/正文，`selectedTabIndex` mutableIntStateOf 驱动，onClick 切数组 setEditEntities，**当前版本无 onTabSelected 参数**见修复）；字段 RecyclerView + 顶部快捷工具条 CheckBox/Spinner 行保留 View 内核（AD-02 渐进）。**编译修复 4 处**：① `Icons.Filled.Variable` 不存在→`Icons.Filled.Tune`（原 XML 菜单无图标，Compose 需自选）；② L235 `actions += MenuAction(...).let{...}` 类型不匹配（let 返回 MenuAction? vs MutableList<MenuAction>）→改 `if(!loginUrl.isNullOrBlank()){ actions += MenuAction(...) }` 条件块；③ `PrimaryScrollableTabRow(onTabSelected=...)` 参数不存在→移除（Tab 自带 onClick，selectedTabIndex 已驱动）；④ `PrimaryScrollableTabRow` 需 `@OptIn(ExperimentalMaterial3Api::class)`+import。**门禁检查**：`assembleAppDebug` BUILD SUCCESSFUL（EXIT=0）；grep 无 `android.util.Log.d/e`/`printStackTrace` 残留；无硬编码色/中文。**updateLog**：已追加（见本节）。**FR-11 门禁**：交 V-7 统一真机验证（S3 属 V-7 支干样板页）。**实施回执（§3.3 AD-23）本条已双写。** **待办（阶段2+）**：里程碑2 状态收敛（V4/V5 + getSource() 移 VM + rememberSaveable 旋转保活）→ 里程碑3 字段容器 SettingsCard + 底部保存栏 → 里程碑4 弹窗族/i18n/无障碍（V6-V9）→ 回执（V13）。

- [x] 12.26c-2 **S3 BookSourceEditActivity 阶段2：V5 旋转保活（状态收敛先行）**（2026-08-13，本窗口）——按 12.16n V5「无 rememberSaveable/onSaveInstanceState——旋转/进程死亡丢未保存编辑」修复。**已落地**：`onSaveInstanceState` 保存 6 个 EditEntity 数组（source/search/explore/info/toc/content，GSON 序列化 `GSON.toJson(list)`）+ `selectedTabIndex`；`restoreState(savedInstanceState)` 在 `viewModel.initData` 回调 `upSourceView` 之后调用，按 key 匹配恢复 value（`GSON.fromJson(json, Array<EditEntity>::class.java)` + runCatching 容错，尺寸不匹配时保留 upSourceView 填充值防 key 错位）。**编译修复 2 次**：① `io.legado.app.utils.fromJson` 扩展不存在（误 import）→移除，改 `GSON.fromJson(json, Array<EditEntity>::class.java)`；② `GSON.fromJson` 泛型推断失败（`val saved = runCatching{...}.getOrNull()`）→显式类型 `val saved: Array<EditEntity>?`。**门禁检查**：`compileAppDebugKotlin` BUILD SUCCESSFUL + `assembleAppDebug` BUILD SUCCESSFUL（EXIT=0）；grep 无 Log.d/e/printStackTrace 残留。**updateLog**：已追加「编辑状态旋转/进程重建保活」。**FR-11 门禁**：交 V-7 统一真机验证。**实施回执（§3.3 AD-23）本条已双写。** **V4 状态收敛决策**：6 EditEntity 数组/getSource() 暂留 Activity（View 内核现状合理），随阶段3 SettingsCard+LazyColumn 化时收敛到 VM StateFlow，避免 View 内核阶段无收益搬移。**待办**：阶段3 SettingsCard 字段容器 + 底部保存栏 → 阶段4 弹窗族/i18n/无障碍（V6-V9）→ 回执（V13）。

- [x] 12.26c-3 **S3 BookSourceEditActivity 阶段3：字段容器 SettingsCard 包裹 + 顶部快捷工具条 SettingsCard 分组 + 底部保存栏**（2026-08-13，本窗口）——按 P10 §2 布局框图（快捷工具条 SettingsCard 分组 + 字段分组 SettingsCard + 底部保存/取消 12dp 圆角 48dp）+ 用户决策「字段容器也 Compose 化」。**已落地**：`activity_book_source_edit.xml` 重写为 5 个 ComposeView（compose_top_bar/compose_quick_toolbar/compose_tab_bar/compose_fields/compose_bottom_bar，LinearLayout vertical 纵向排布，compose_fields 权重 1）；新增 Compose 状态变量（`typeIndex`/`isEnable`/`isEnableExplore`/`isEnableCookie`/`isEventListener`/`isCustomButton` 全部 mutableStateOf/mutableIntStateOf）；`initComposeQuickToolbar()`=SettingsCard 分组（类型 SettingsClickRow+DropdownMenu 6 档 book_type + 5 个 SettingsToggleRow 启用/发现/保存Cookie/事件监听/自定义按钮）；`initComposeFields()`=SettingsCard 包裹 AndroidView(recyclerView)（保留 EditText 行内编辑 + CodeView/KeyboardToolPop 内核，AD-02 红线）；`initComposeBottomBar()`=Row 内 OutlinedButton 取消 + Button 保存（各 weight1/48dp/RoundedCornerShape(12.dp)）。**删除 View 内核引用**：`upSourceView` 的 `binding.spType.setSelection`/`binding.cbIsEnable.isChecked` 等 6 处 → 改写 Compose 状态变量；`getSource` 的 `binding.spType.selectedItemPosition`/`binding.cbIsEnable.isChecked` 等 6 处 → 改读 Compose 状态变量；`setEditEntities`/`sendText` 中 `binding.recyclerView`（5 处）→ `recyclerView`（by lazy 实例）。**编译修复 1 处**：`RoundedCornerShape` 误 import `androidx.compose.material3.RoundedCornerShape`（不存在）→`androidx.compose.foundation.shape.RoundedCornerShape`。**门禁检查**：`assembleAppDebug` BUILD SUCCESSFUL（EXIT=0）；grep 无 `android.util.Log.d/e`/`printStackTrace` 残留；无硬编码色/中文（全 stringResource/colorScheme）。**updateLog**：已追加「书源编辑页布局升级为 Compose 卡片式」。**FR-11 门禁**：交 V-7 统一真机验证。**实施回执（§3.3 AD-23）本条已双写。** **待办**：阶段4 弹窗族/i18n/无障碍（V6-V9）→ 回执（V13）。

- [x] 12.26c-4 **S3 BookSourceEditActivity 阶段4：i18n 收敛（V6）**（2026-08-13，本窗口）——按 P10 §7「全部文案 strings.xml 双语；无硬编码中文」收敛。**已落地**：`helpActions` 键盘补全菜单 6 项硬编码中文 → stringResource（插入URL参数 `insert_url_param`/书源教程 `book_source_tutorial`/js教程 `js_tutorial`/正则教程 `regex_tutorial`/插入分组 `insert_group`/选择文件 `select_file`）；`setSourceVariable` 源变量说明硬编码中文 → `source_variable_comment`（复用既有 key）。**新增 key 5 个**（insert_url_param/insert_group/book_source_tutorial/js_tutorial/regex_tutorial，en+zh 双写）；复用 select_file/source_variable_comment/set_source_variable 既有 key。**弹窗族评估**：本页所用弹窗（VariableDialog/UrlOptionDialog/alert/selector/AppLogDialog）均为项目既有标准弹窗组件，无私有表单布局，无需替换。**无障碍评估**：底部保存/取消 48dp（阶段3已做）、快捷工具条 SettingsClickRow/SettingsToggleRow 为公共组件自带触控高度、CodeView/KeyboardToolPop 属 N 不迁移内核，V11 满足。**V7 评估**：本页无硬编码色（grep 0 命中），不适用。**门禁检查**：`assembleAppDebug` BUILD SUCCESSFUL（EXIT=0）；grep 无 `android.util.Log.d/e`/`printStackTrace` 残留；UI 硬编码中文 0 残留（仅 AppLog 内部日志保留，非 UI 文本）。**updateLog**：已追加「键盘补全菜单文案国际化」。**FR-11 门禁**：交 V-7 统一真机验证。**实施回执（§3.3 AD-23）本条已双写。** **S3 骨架样板 1-4 阶段全部完成**：待办=回执（V13）核对 + 真机统一验证（V-7）。

## 13. 后续实施门禁（设计文档 v2 评估通过后启动）
> 每个页面 Compose 化 = pages-inventory 功能点核对 + ui-standards 骨架归类 + 组件复用 + 主题接入 + 三态 + **真机功能点覆盖测试（FR-11 门禁，MEmu + ai_tests\venv）** + **§3.3 实施回执（AD-23）** 全过才 ✅。逐页任务在评估后按「支干样板页 → 枝叶复用」顺序（AD-24）追加。

### 13.0 代码改造 vs 设计文档一致性审查新增子任务（audit-code-vs-design.md，2026-08-15）

> 审查结论：另一 AI 待提交代码总体符合设计文档。下列 D-1~D-14 为审查发现需另一 AI 执行/确认的子任务。详细偏差分析见 `audit-code-vs-design.md`。

- [x] D-1 **VideoPlayerActivity 存量 Log.d 清理**（P1；**✅ 已完成 2026-08-16 核码确认**）：`VideoPlayerActivity.kt:1251,1272` 存量 `Log.d("VideoFS",...)` 改造时未清理（仅改写 message），收敛为 `AppLog.put()` 或删除。门禁：grep 无 `android.util.Log.d/e` 残留。**核码确认**：VideoPlayerActivity 全文件 Log.d/e 零残留，日志均 AppLog.put 收敛（含降级决策日志）。
- [x] D-2 **核实并修复 rssSort 排序偏好 no-op——已精确定位为 RssFragment 主列表层**（P1，P7 V4 关闭条件，2026-08-15 二阶段核码定案；**✅ 已完成 2026-08-16**）：管理页 RssSourceActivity 已消费（菜单 :212-235 写 AppConfig.rssSort + `sortSources():587-597` 排序生效）；**RssFragment 主列表仍 no-op**（数据源 :379-419 六分支 flow 全走 RssSourceDao 恒 `order by customOrder`，RssViewModel 无 rssSort 消费，文件夹配置对话框写 rssSort 但主列表不读）。修复路径：upRssFlowJob 各分支按 AppConfig.rssSort 内存排序（1 名称/2 启用/3 类型/4 分组/5 URL/6 更新时间，sortAscending 反转，0=手动跳过）或传 DAO 查询参数。**修复落地**：RssFragment.kt 新增 `sortSources():435-445`（六分支 + rssSortAscending 反转），:429 `adapter.setItems(sortSources(it))` 接入，与管理页行为一致。
- [x] D-3 **核实并修复 sourceLayout no-op——已精确定位为 RssFragment 主列表层**（P1，P7 V5 关闭条件，2026-08-15 二阶段核码定案；**✅ 已完成 2026-08-16**）：管理页 BookSourceActivity/RssSourceActivity 已完整消费（applyListView 按 0-6 切 layoutManager）；**RssFragment `applyListView():241-245` 仍恒 `GridLayoutManager(context,4)` 不读 AppConfig.sourceLayout**。修复路径：applyListView 读 sourceLayout 三态（列表→LinearLayoutManager / 紧凑→Grid(2) / 网格→复用 applyFolderView 的 calculateSpanCount 逻辑）。**修复落地**：RssFragment.kt `applyListView():244-267` 读 AppConfig.sourceLayout 三态切换 layoutManager，:321 onResume 时应用。
- [x] D-7 **SourceLoginDialog 壳层 Compose 化已定案**（2026-08-15 二阶段核码；**✅ 已完成 2026-08-16 收尾勾选**）：12.25 判定「SourceLoginDialog 属 N 不迁移」需更新——顶栏 Toolbar 已迁 `initComposeTopBar()`（GlassTopAppBar+登录/菜单 3 项），**表单引擎内核（rowUiBuilder/loginUi/动态 JS 表单）保留 View**→属 AD-02 壳层 Compose 化合规，复核已通过，无新增偏差。**核码确认**：SourceLoginActivity 顶栏合规（audit 第 17 条），D-7 已定案落地。
- [x] D-8 **HighlightRuleScreen/ReplaceRuleScreen 顶栏菜单替换为 AppDropdownMenu**（2026-08-15 二阶段核码新增；**✅ 已完成 2026-08-16 核码确认**）：两新文件裸 `material3.DropdownMenu`（HighlightRuleScreen:115-139 顶栏 5 项 / ReplaceRuleScreen:85-103 分组 + :121-146 顶栏 MoreVert）手写，违反 §3.4 菜单族统一门禁；替换为 AppDropdownMenu（MenuAction 数据驱动，48dp/checked 态，功能等价）。同批 BookInfoActivity/ReadRssActivity 已正确用 AppDropdownMenu，属遗漏。**核码确认**：HighlightRuleScreen:119 / ReplaceRuleScreen:91/:142 均已 AppDropdownMenu，D-8 已落地。
- [x] D-9 **书架角标实现与 P1 文档对齐 + 硬编码色收敛**（2026-08-15 二阶段核码新增；**色槽已定案=主题色 primary，用户 2026-08-15 决策响应 b6 需求；✅ 已完成 2026-08-16**）：BookshelfScreen 用私有 ShelfUnreadBadge（primary 底+Color.White 硬编码），P1 文档却声明 BadgeDot 已接线（BookshelfItems.kt:323/:449 实际不存在，文件仅 124 行）。修复：①P1 回填真实实现+BadgeDot 接线改回仅 PillNavigationBar；②ShelfUnreadBadge 硬编码色收敛 colorScheme/badgeTextBright 自适应（保持 primary 色槽）。**核码确认**：BookshelfScreen:333 ShelfUnreadBadge + :346 badgeTextBright 自适应均已落地。
- [x] D-4 **App 图标改版（已授权）**（P1；**✅ 已完成 2026-08-16**）：`ic_launcher.xml` foreground 改 `@mipmap/ic_launcher_foreground` + 新增 5 个 png。**✅ 用户 2026-08-15 已授权保留新图标**。设计依据已补 `app-icon-design.md`（adaptive icon 现代化 + M3 对齐 + monochrome + 资源清单）；D-14 ③ launcherIcon 代码侧残留一并收尾（PreferKey/BackupConfig 两处清理，Grep 零残留）。
- [x] D-5 **P2-reader §8 验收补登记 P2 收尾待办**（P2，2026-08-15；**✅ 已完成 2026-08-16**）：3s 无操作自动淡出 + 滑块 alpha≤30% 预览 + conflate 串行提交，已在 P2-reader.md §8 补登记为待办（:109），与 reader-overlay tasks 2.4/4.2 对齐。
- [x] D-6 **16 份 light 轻量文档回填精确 task 号**（P3；**✅ 已完成 2026-08-16**）：Grep 全量核验 `12.xx` / `tasks.md 12.` 占位符在 pages/light/ 零残留——16 份清单（L-B14/B15/B16/C3/C4/C5/C6/C9/C10/C11/C12/C13/C15/C16/C17/C20）均已回填精确 task 号（B14→12.52、B15→12.53、B16→12.54、C6→12.55、C9→12.56、C10→12.57、C11→12.58、C12→12.59、C16→12.5A、C17→12.5B、C20→12.5C 等，见 tasks.md 矩阵）。
- [x] D-10 **MenuLayer 换源/更多菜单裸 DropdownMenu→AppDropdownMenu**（P2，2026-08-15 四阶段核码新增；**形态已定案=下拉菜单，用户 2026-08-15 决策；✅ 已完成 2026-08-16 核码确认**）：MenuLayer.kt 换源菜单（:318）与更多菜单（:355）均已改 AppDropdownMenu（MenuAction 数据驱动+action.checked），私有 MenuMoreItem 已删除。**P2 spec §3 表「更多操作→AppMenuSheet」描述同步更新为下拉菜单。**
- [x] D-11 **BookSourceScreen/BookSourceItems 裸 DropdownMenu + i18n 硬编码**（P2，2026-08-15 四阶段核码新增；**✅ 已完成 2026-08-16**）：①BookSourceScreen:699 BookSourceMoreMenu + :915 SelectActionBarCompose 批量菜单裸 DropdownMenu→AppDropdownMenu（MenuAction 数据驱动，12 项批量操作全配图标）；②:402-403 QuickFilterWords 硬编码英文关键词 `enabled/disabled/need_login/no_group/enabled_explore/disabled_explore`——View 侧走 getString 本地化，中文环境筛选失效（且 showTopBar=false 死代码），改 getString 或删；③BookSourceItems:287 `checkMessage.contains("成功")` 硬编码中文收敛为 SUCCESS_MARK 常量。编译 BUILD SUCCESSFUL。
- [x] D-12 **MyFragment 内部类 MyPreferenceFragment 死代码清理**（P3，2026-08-15 四阶段核码新增；**✅ 已完成 2026-08-16**）：onFragmentCreated 已改 Compose 桥接不再实例化，类体 L92-202 完整保留（含存量中文 L103+未迁移 observeEventSticky）。清理前确认无反射/无 R.id 引用。**清理结果**：MyFragment.kt 精简为 View 壳+Compose 桥接（删除整类 MyPreferenceFragment + 17 个无用 import），确认 ProfileScreen3Level 已完整覆盖 webService/autoTaskService 开关与全部入口；pref_main.xml 孤儿资源一并删除。编译 BUILD SUCCESSFUL。
- [x] D-13 **PillNavigationBar 规格或文档回填**（P2，2026-08-15 四阶段核码新增；**✅ 已完成 2026-08-16**）：组件 08-14 已简化（:95-104 tween(200) 无 spring、:66 padding vertical 4dp、无选中胶囊底色，注释 :41 自述），但 §3.4:220 与 P9 §3 仍写「spring+α0.12 胶囊 36×30+垂直 6dp」——**决策取「回填文档匹配代码」**：ui-standards §3.4:220 与 P9-main §3 均回填真实实现（tween(200) 变色、Row 4dp/Tab 2dp、无胶囊底、图标 22dp）。
- [x] D-14 **残留清理批次**（P3，2026-08-15 六阶段核码新增；**①②④ 已完成，③ 待 D-4 图标授权联动收尾**）：①`activity_rss_artivles.xml:23` 残留 `@id/title_bar` 死引用（✅ 已改 compose_top_bar）；②`menu/rss_articles.xml`+`menu/rss_read.xml` 孤儿资源删除（✅ 已删，onCompatCreateOptionsMenu 零引用）；③`pref_config_theme.xml` 删除 launcherIcon 后代码侧残留确认（LauncherIconHelp:15/IconListPreference:138/BackupConfig:63 仍引用 change_icon，与 D-4 图标授权决策联动，**待 D-4 另一 AI 收尾**）；④双语缺失 2 key（✅ `about_description_sigma`/`contributors_summary_sigma` values-zh 已补中文）。
- [x] D-15 **ConfigActivity 启动 NPE 崩溃修复**（**P0**，2026-08-15 真机验证新增）：`ConfigActivity.kt:32` `by mutableStateOf(getString(R.string.setting))` 在 Activity 构造阶段（Context 未 attach）调 getString 抛 NPE，**阻断设置/主题/备份/封面/欢迎/精准管理全部子页入口**（真机 am start 直接崩溃退回 launcher，crash 栈指向 ConfigActivity.<init>）。修复：改 `mutableStateOf("")` 与其余 6 处一致（Cache:110/AudioPlay:100/RssSort:78/VideoPlayer:148/ReadRss:146/BookshelfManage:102）+ setTitle() 兜底赋真实值。门禁：am start ConfigActivity 正常进入设置页。**✅ 已修复（2026-08-15 模拟器验证 4 入口正常，见 ai_memory_main 决策记录；2026-08-16 核码确认 ConfigActivity.kt:33 composeTitle 已 `mutableStateOf("")`，收尾勾选）**
- [x] D-16 **书源管理页分组视图下 sourceLayout 网格 no-op**（**P1**，2026-08-15 横向扩展真机验证新增；**✅ 已完成 2026-08-16**）：GroupedSourceList 恒 LazyColumn 忽略 layout>=2。修复：GroupedSourceList 增加 `layout >= 2` 分支走 LazyVerticalGrid（BoxWithConstraints 自适应 2/3/4/6 列，分组 header 用 GridItemSpan(maxLineSpan) 占满整行，组内书源网格卡片 BookSourceGridItem），折叠态逻辑与列表分支一致。编译 BUILD SUCCESSFUL。

### 待建组件接线 task 清单（ui-standards §3.4 🔵 待建 → task 挂接，2026-08-13 方向④）

> 原则：§3.4 待建组件表标 🆕/🔵 = 待建，**规格已在 §3.4 精确化（2026-08-13 收敛专项二：四维圆角/间距/字号/色槽+高度/触控全部给精确值，含 task 号）**，实现前按 §3.2 复核源码/fork 转 ✅ 后，由下表对应接线页落地。编号 12.28 起（接 12.27 页面文档），字母后缀避开 12.2x 数字序列。**依赖关系**：本表每个 task 是「接线页」开发的前置交付物（先建组件 → 接线页引用），页面文档 §3 引用该组件时以本表 task 号为前置门禁；实现后回填 §3.4 状态列 🔵→✅ 并登记 §3 组件目录接线页。

| task | 待建组件 | 接线页 | 阶段/职责 | 规格来源 |
|------|---------|--------|-----------|----------|
| ✅ 12.28 | `ReadMenuGlassButtonSurface` | P2 阅读器顶/底栏按钮 | Phase4 浮层 R2 / 阅读器族 | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.29 | `ReadMenuSlider` | P2 阅读器进度/亮度/字号 | Phase4 浮层 R2 / 阅读器族（alpha≤30% 预览） | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.2A | `ReaderBookSheet`（三 Tab） | P2 阅读器目录/书签/高亮 | Phase4 浮层 R2 演进（敬畏 BookTocBookmarkSheet 基座） | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.2B | `ReaderMoreActionsSheet` | P2 阅读器更多操作 | Phase4 浮层 R2 / 阅读器族 | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.2C | `ReaderViewport` | P2 正文尺寸接缝 | Phase4 正文零改动桥接 | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.2D | `TextActionSelectionMenu` | P2 阅读器选区工具条 | Phase4 浮层 / 替换 TextActionMenu | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.2E | `HighlightStyleDialog`（存量升级） | P8 高亮色盘 chooser | Phase4 高亮选色 / 存量升级 | ✅ 已建（2026-08-16，编译通过；Compose 化升级，保留 StyleHost 桥接与 8 通道+线型+字体业务） |
| ✅ 12.2F | `ListCard`（含 BookListCardMetrics） | S2 列表页条目 | 公共组件库三期 / 列表工具（legado-archive） | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.30 | `ListScaffold<T>`/`ListUiState<T>` | S2 列表通用模板 | 公共组件库三期 / 325506 | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.31 | `ModernActionPopup` | 全仓菜单入口替代 | 公共组件库三期 / 菜单族（legado-archive） | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.32 | ComposeDialog 家族 8 类 | S6 Dialog 族扩展 | 公共组件库三期 / legado-archive | ✅ 已建（2026-08-16，编译通过；TextInput/SingleChoice/ActionList 三子类落地，余 5 子类按需补） |
| ✅ 12.33 | `BaseComposeDialogFragment` 基类 | S6 Dialog 统一宿主 | 公共组件库三期 / Legado_Max | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.34 | `AppConfirmDialog` | S6 确认弹窗（14 处） | 公共组件库三期 / Legado_Max | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.35 | `MultiSelectDialog` | S6 多选弹窗 | 公共组件库三期 / Legado_Max | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.36 | `AppAlertDialog`/`AppModalBottomSheet` 双引擎 | S6 弹窗容器 | 公共组件库三期 / 325506 | ✅ 已建（2026-08-16，编译通过；`AppAlertDialog` 含 show/data 双变体+data 缓存末值退出动画+18dp 圆角；`AppModalBottomSheet` 已存在。本项目无 Miuix 依赖，双引擎退化为单 M3 引擎，入口已收敛便于未来接引擎） |
| ✅ 12.37 | `AlertBuilder<D>` DSL | S6 Confirm/Select 统一入口 | 公共组件库三期 / 325506 | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.38 | Sheet 内多级导航（ManageScreen sealed） | S6 弹窗多级页面 | 公共组件库三期 / Legado_Max | ✅ 已建（2026-08-16，编译通过；通用化 ManageScreenHost/ManageScreenHeader + sealed ManageScreen(depth)） |
| ✅ 12.39 | `HeatmapCalendar` | P4 我的页阅读热力图 | 枝叶 P2 我的页 / Legado_Max | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.3A | `SummaryCard`/`BookStackView` | P4 我的页成就卡 | 枝叶 P2 我的页 / Legado_Max | ❌ 已删除（2026-08-16 孤儿清理，About 页私有卡已注释登记） |
| ✅ 12.3B | `SourceFilterRule` | 搜索过滤 | 枝叶 P3 搜索 / huajideshutiao | ✅ 已建（2026-08-16，编译通过） |
| ✅ 12.3C | `AssistChip` | P5 书源条目登录态 | S2 支干 P5 / MoRealm F2 | ✅ 已建（2026-08-16，编译通过）+ P5 §1.1 |

### 轻量页 task 精确映射表（12.4x 系列 · 2026-08-13 收编 P1/P2/P3 阶段号 → 精确 task 号）
> **背景**：ui-standards §10.3 矩阵中轻量文档 task 对应此前标「P1/P2/P3 阶段号」，未到单页级，后续 AI 无法定位具体任务。本表将每个标 P1/P2/P3 的轻量文档收编为精确 `12.4x` 子任务号，并映射到对应真机验证子任务（P2→V-8 / P3→V-9 / P1→V-7）。**✅ 2026-08-13 已完成收编**：35 项全部入表，§10.3 矩阵 task 列已同步精确化。开发某轻量页前先在本表锁定 12.4x 号，完成后回填本表勾选。

| task | 页面文档 | 页面（Activity/Fragment） | 骨架 | 阶段 | 真机 task |
|------|---------|--------------------------|------|------|-----------|
| ✅ 12.40 | L-D4 | RssArticles/RssSort | S2 | P1 | V-7 |
| ✅ 12.41 | L-B8 | BookshelfManageActivity | S2 | P2 | V-8 |
| ✅ 12.42 | L-B10 | CacheActivity | S2 | P2 | V-8 |
| ✅ 12.43 | L-B12 | ReadMangaActivity | S5 | P2 | V-8 |
| ✅ 12.44 | L-B13 | AudioPlayActivity | S5 | P2 | V-8 |
| ✅ 12.45 | L-C3 | BookSourceDebugActivity | S3 | P2 | V-8 |
| ✅ 12.46 | L-C4 | ReplaceRule/ReplaceEdit | S2+S3 | P2 | V-8 |
| ✅ 12.47 | L-C5 | HighlightRuleActivity | S2 | P2 | V-8 |
| ✅ 12.48 | L-C13 | SourceLoginActivity | S6 | P2 | V-8 |
| ✅ 12.49 | L-C15 | ImageGallery/ImageDetail | S5 | P2 | V-8 |
| ✅ 12.4A | L-D6 | ReadRssActivity | S4 | P2 | V-8 |
| ✅ 12.4B | L-D9 | VideoPlayerActivity | S5 | P2 | V-8 | **视频设置弹框深化（2026-08-16）**：`VideoSettingsPanel`（BottomSheet）+ `SettingsDialog`（Dialog 壳）均 Compose 化，共享 `VideoSettingsPanelContent` 组件（全分区 SettingsToggleRow/SettingsClickRow/SingleChoiceDialog/PanelButton），布局精简为 ComposeView 宿主 |
| ✅ 12.4C | L-E1 | BackupConfigFragment | S2 | P2 | V-8 |
| ✅ 12.4D | L-E4 | OtherConfigFragment | S2 | P2 | V-8 |
| ✅ 12.4E | L-F6 | VideoFragment | S5 | P2 | V-8 |
| ✅ 12.4F | L-B5 | AllBookmarkActivity | S2 | P3 | V-9 |
| ✅ 12.50 | L-B7 | BookInfoEditActivity | S3 | P3 | V-9 |
| ✅ 12.51 | L-B9 | ImportBook/RemoteBook | S2 | P3 | V-9 |
| ✅ 12.52 | L-B14 | ExploreShowActivity | S2 | P3 | V-9 |
| ✅ 12.53 | L-B15 | StorageManageActivity | S2 | P3 | V-9 |
| ✅ 12.54 | L-B16 | TxtTocRuleActivity | S2 | P3 | V-9 |
| ✅ 12.55 | L-C6 | DictRuleActivity | S2+S3 | P3 | V-9 |
| ✅ 12.56 | L-C9 | FileManage/HandleFile | S2+S6 | P3 | V-9 |
| ✅ 12.57 | L-C10 | DownloadManageActivity | S2 | P3 | V-9 |
| ✅ 12.58 | L-C11 | UrlRecordActivity | S2 | P3 | V-9 |
| ✅ 12.59 | L-C12 | RecycleBinActivity | S2 | P3 | V-9 |
| ✅ 12.5A | L-C16 | AutoTask/AutoTaskEdit | S2+S3 | P3 | V-9 |
| ✅ 12.5B | L-C17 | WelcomeActivity | S6 | P3 | V-9 |
| ✅ 12.5C | L-C20 | About/AboutFragment/ReadRecord | S2 | P3 | V-9 |
| ✅ 12.5D | L-D3 | RssSourceDebugActivity | S3 | P3 | V-9 |
| ✅ 12.5E | L-D7 | RssFavoritesActivity | S2 | P3 | V-9 |
| ✅ 12.5F | L-D8 | RuleSubActivity | S2+S6 | P3 | V-9 |
| ✅ 12.60 | L-E3 | CoverConfigFragment | S2 | P3 | V-9 |
| ✅ 12.61 | L-E5 | PreciseManageFragment | S2 | P3 | V-9 |
| ✅ 12.62 | L-E6 | WelcomeConfigFragment | S2 | P3 | V-9 |

> **矩阵中已精确编号的轻量文档**（无需 12.4x）：A5→12.16j、A7→12.16m、A8→12.16o、B2/B3/B4/B11/D5/E2→12.16p、C1→12.16q/r、C2/D2→12.16n、C19→12.16k、D1→12.16l、B6→12.23 ✅。**N 不迁移页**（C7/C8/C14/C18/F4/F5/A6）无 task。

- [x] **12.6x N 不迁移页壳层 Compose 化（2026-08-16，本窗口）**——C7/C8/C14 三个带顶栏 Activity 壳层化完成，内核（sora 编辑器/WebView 池/相机扫码）保留 View（AD-02 红线）。**C8 WebViewActivity**：`activity_web_view.xml` title_bar→compose_top_bar；initComposeTopBar（GlassTopAppBar：返回/刷新/完成常驻图标 + AppDropdownMenu 溢出：浏览器打开/复制URL/全屏/网页日志(checked)/禁用源/删除源，sourceOrigin 非空才显源操作）；删除 onCompatCreate/onPrepare/onCompatOptionsItemSelected 三方法；title/subtitle 改 mutableStateOf 驱动（网页标题动态回填）；删死资源 `menu/web_view.xml`。**C7 CodeEditActivity**：`activity_code_edit.xml` title_bar→compose_top_bar；GlassTopAppBar（返回/搜索/保存常驻图标，保存按钮随 writable 显隐 + AppDropdownMenu：格式化/更换主题/配置设置/自动换行(checked)/日志）；删除旧菜单三方法 + menuSaveBtn；删死资源 `menu/code_edit_activity.xml`；修复 setSearchOptions/finish 误并 + 重复 finish 清理 + buildList 内 this 冲突（`this@CodeEditActivity`）。**C14 QrCodeActivity**：`activity_qrcode_capture.xml` title_bar→compose_top_bar；GlassTopAppBar（返回 + 相册选图常驻图标）；删除旧菜单两方法；删死资源 `menu/qr_code_scan.xml`。**F4 WebViewLoginFragment/F5 QrCodeFragment** 为无顶栏 Fragment（纯 WebView/相机视图），**C18 association 系**为透明窗 Dialog 展示、**A6 BooksFragment** 已无引用，四者无壳可换，维持 N 不迁移结论。**编译验证**：`compileAppDebugKotlin` BUILD SUCCESSFUL（EXIT=0；修复 LegadoTheme 包名 io.legado.app.ui.theme、String? 赋值 orEmpty()）。**updateLog**：追加「网页浏览页/代码编辑页/扫码页顶栏界面升级」。**FR-11 门禁**：交 V-11 统一真机验证（用户指令先不真机，编译通过即推进）。

- [x] **12.6y 订阅源编辑页壳层 Compose 化（2026-08-16，本窗口补遗漏）**——用户指出「订阅源编辑页还没处理」：RssSourceEditActivity 此前漏改，仍为纯 View（TitleBar + 原生 TabLayout + RecyclerView 菜单体系）。本次参照同型 BookSourceEditActivity 完成壳层化：`activity_rss_source_edit.xml` title_bar→compose_top_bar；initComposeTopBar（GlassTopAppBar：返回 + MoreVert 溢出下拉）；buildMenuActions 复刻原 source_edit.xml 全部 14 项（编辑内容/保存/调试源/登录[loginUrl 非空才显]/设置源变量/清除cookie/自动补全(checked)/复制源/粘贴源/扫码导入/分享/二维码分享/日志/帮助），登录条件判断从 onMenuOpened 迁移到 buildMenuActions 内 getRssSource().loginUrl；删除 onCompatCreateOptionsMenu/onMenuOpened/onCompatOptionsItemSelected 三方法；TabLayout+RecyclerView 内核保留 View（AD-02 红线，S3 表单页同型不动内核）；删死资源 `menu/source_edit.xml`（book/rss 两编辑页均已 Compose 菜单化）。**编译验证**：`compileAppDebugKotlin` BUILD SUCCESSFUL。**FR-11 门禁**：交 V-11 统一真机验证。

### 真机验证子任务（2026-08-12 用户 UI 反馈修复，APK legado_miss_app_3.26.081222）
> 用户反馈 4 项 UI 问题的修复，逐项真机（MEmu 21503 测试包 io.legado.miss.app.debug）验证。SOP 见 ai_tests/docs/fixed_test_workflow.md。

- [ ] V-1 底部导航：PillNavigationBar 去掉悬浮半透明白色胶囊 → surface 背景+顶部细分割线；四项 Tab weight 等分均分；切换仅柔和颜色过渡（无整体大缩放）；选中项仅轻量圆形底色 → 验证结果：无"名字下方白条"、四项均匀、点击动画柔和、非选中无底色
- [ ] V-2 我的页：ProfileScreen3Level 去掉 Scaffold 重复系统栏 inset → 直接 Box 布局 → 验证结果：顶部/底部留白明显减少、统计卡与设置列表紧贴
- [ ] V-3 详情页按钮：换源/设置分组/查看目录 AccentBgTextView 圆角 2→6dp、padding 5→10dp、字号 13→14sp → 验证结果：三按钮样式统一圆润、视觉协调
- [ ] V-4 书架初始化：loading 骨架屏按布局类型动态匹配（layout≥2 用 ShelfGridSkeleton，否则 ShelfListSkeleton）→ 验证结果：打开书架不再"先画双列再刷单列"闪变
- [ ] V-5 全量回归：logcat 零 FATAL/ANR，书架/发现/订阅/我的四 Tab 切换正常

### 后续优化统一真机验证子任务（Phase4-6 实施完成后统一验证，禁止逐项插入打断实施）
> 用户指令（2026-08-12）：所有优化实施工作全部完成后，再统一真机验证，不现在逐项验证。

- [ ] V-6 阅读器浮层 Sheet 化（Phase4 PR-4.1/4.2）：目录→BookTocBookmarkSheet（目录+书签双 Tab）、高亮选色等次要浮层 BatchDialog→BottomSheet → 验证：浮层不遮挡正文、正文读翻页性能无回归、手势冒烟
- [ ] V-7 支干 P1 样板页（Phase6 · S2-S6）：S1 MainActivity 已接入 PillNavigationBar（bottomNavigationView.setContent，2026-08-12 已确认）；S2 BookSourceActivity / S3 BookSourceEditActivity / S4 BookInfoActivity / S5 阅读器浮层 / S6 登录·导入 Dialog 族 Compose 化 → 验证：样板页功能点全过 + 回执模板示范（FR-11）
- [ ] V-8 枝叶 P2 次优高频页（Phase6）：Explore/Rss/Search/Toc/BookSourceDebug/Replace/Highlight/SourceLogin/BookManage/Cache/ReadManga/Audio/ImageGallery/Video/Config 3 子页（Backup/Theme/Other）照抄样板 Compose 化 → 验证：同型页骨架一致 + 真机覆盖 + 回执
- [ ] V-9 枝叶 P3 长尾页（Phase6）：其余所有 View 页（导入/存储/文件/下载/记录/回收站/自动任务/词典/TxtToc/BookInfoEdit/Welcome/About/ReadRecord/URL记录/SS阅读）全量 Compose → 验证：全量页面 Compose + 巡检 0 私有重复 + 回执
- [x] **S1-S4 规格一致性巡检（Phase5 前置，2026-08-13）**：对已接线 S1(S2)/S2(S4)/S3(S6)/S4(Phase4) 骨架页做全局规格一致性巡检（对照 §3.4 真值表五维+硬编码检查）。**审计 6 项**（audit-wired-components.md）：S1 PillNavigationBar ✅ / S2 GroupHeader+EmptyState+卡18dp ✅（1处定案违例已修复）/ S3 BookSourceEdit（新增审计）✅ 全部符合 / S4 GlassTopAppBar ✅（1处定案违例已修复）/ Phase4 浮层功能✅（真值已补）/ S6 ImportSourceSheet ✅（1处定案违例已修复）。**核心发现**：audit 文档此前定案 3 处违例均未落地，本次巡检复核并全部修复（SourceCover 24sp→displaySmall / GlassTopAppBar 补 titleMedium / ImportItemRow padding 16/12），BUILD SUCCESSFUL。**经验沉淀**：定案必须当次会话内同步落地改码，避免纸面定案。**V-10 全量巡检仍待做**（组件验收矩阵全 App 扫码）。
- [x] V-10 一致性巡检（Phase5 PR-5.1/5.2）：组件验收矩阵全 App 扫码、对齐调色板/圆角/间距 → 验证：0 处页面私有重复组件实现、公共 token 命中率 100%。**进度 2026-08-16**：审计报告 `audit-v10-consistency.md` 已完成全 App 扫码（55 组件文件/31 Screen/87 圆角使用点/30+ 色点），修复进度登记于其 §7——**P0（E1 BadgeDot/E5 SettingsSection/E6 SummaryCard/E7 FormatUtils）✅ 已完成**、**P1（E4 SettingsSelectableRow/E2 TagChip/E3 SettingsClickRow trailing）✅ 已完成**、**P2（AppShapes 圆角回迁 components 10 文件+debug 7 页+页面层，共 81 处）✅ 已完成**；**P3 颜色/间距收敛 ✅ 已完成（2026-08-16，并行子代理执行+主代理复核：Download/UrlRecord/CurlTest/RegexTest/PingTest/ExploreShow/Bookshelf 遮罩约 20 处硬编码语义色全部收敛至 colorScheme 语义槽位，剩余 Color 均为已登记豁免/主题派生源头/合理内容色）**；**孤儿组件清理 ✅ 已完成（2026-08-16）：删除 SummaryCard/ThemedSnackbarHost/SplicedColumnGroup/ManageScreenSheet，VerticalScrollbar 接线 DownloadManageScreen，ListScaffold 保留模板待用**。P0-P3+孤儿清理全部完成，V-10 巡检（审计+修复）统一勾选。
- [ ] V-11 全量回归（Phase6 收尾）：logcat 零 FATAL/ANR，主干/支干/枝叶所有页面核心功能一个不漏，N 不迁移页壳换 Compose 浮层正常
- [ ] V-12 S6 Import 系列样板（Phase6 · ImportSourceSheet，2026-08-13 完成）：**7 个 Import Dialog 全部升级为 Compose 底部抽屉**（订阅源样板+RssSource 接线 12.26，书源/替换净化/净化词/目录规则/主题/在线朗读 6 枝叶 12.26b）——验证：弹窗渲染（标题/菜单/列表/底部三按钮）、逐项勾选/全选/反选、备注展开（目录规则=example 常显）、新增-更新-已存在状态徽标、编辑源回写、导入执行落库、取消关闭、各菜单（书源 7 项/替换净化 2 项）勾选态
- [x] ✅ **12.47 L-C5 HighlightRuleActivity 枝叶 Compose 化完成（2026-08-14）**：全 Compose 页面 `HighlightRuleScreen.kt`（GlassTopAppBar 顶栏 + SettingsSearchBar 搜索过滤 + LazyColumn 规则列表 + EmptyStatePlaceholder 空态 + AppMenuSheet 置顶/置底/删除）+ `HighlightRuleActivity` ComposeView 混合接线（activity_highlight_rule.xml 单容器）。VM/数据逻辑零改动，Dialog 族（编辑/分组管理/预设/恢复默认/导入/导出）全量保留无功能删减（红线合规）；strings 新增 8 key（en+zh）；updateLog 已追加。**门禁**：assembleAppDebug BUILD SUCCESSFUL；GetDiagnostics 0 错误；grep 无 Log.d/e 残留。**FR-11 真机验证归 V-8 统一执行**（V-8 待办清单已含 Highlight）

- [x] ✅ **12.4F L-B5 AllBookmarkActivity 枝叶 Compose 化完成（2026-08-15）**：全 Compose 页面 `AllBookmarkScreen.kt`（GlassTopAppBar 顶栏 + 导出 JSON/MD 下拉菜单 + 按书分组的 LazyColumn 列表 + GroupHeader 折叠/展开 + EmptyStatePlaceholder 空态 + BookmarkItem 点击跳读/长按编辑）+ `AllBookmarkActivity` ComposeView 混合接线（activity_all_bookmark.xml 单容器）。数据经 `flowAll()` 桥接 composeBookmarks，VM 导出逻辑（exportBookmark/exportBookmarkMd）零改动，BookmarkDialog 全量保留无功能删减（红线合规）；strings 新增 4 key（all_bookmark/empty_title/empty_subtitle 等，en+zh）；废弃 View 文件（BookmarkAdapter/BookmarkDecoration/item_bookmark.xml）已清理。**门禁**：assembleAppDebug BUILD SUCCESSFUL；grep 无 Log.d/e 残留。**FR-11 真机验证归 V-9 统一执行**（V-9 待办清单已含 AllBookmark）

- [x] ✅ **12.50 L-B7 BookInfoEditActivity 枝叶 Compose 化完成（2026-08-15）**：全 Compose 页面 `BookInfoEditScreen.kt`（GlassTopAppBar 顶栏 + 保存下拉菜单 + SettingsCard 分组字段：封面预览 AndroidView(CoverImageView)/书名/作者/类型 Spinner(DropdownMenu)/封面地址/封面操作三按钮/简介）+ `BookInfoEditActivity` ComposeView 混合接线（activity_book_info_edit.xml 单容器）。保存（BookHelp.updateCacheFolder + 类型/封面/简介写回）与换封面三途径（ChangeCoverDialog/本地 selectCover/刷新 tvRefreshCover）逻辑保留 Activity，View 状态由 Screen 上抛；旧菜单 menu/book_info_edit.xml 已删除（grep 确认无引用）；i18n 复用既有 key 无新增。**门禁**：assembleAppDebug BUILD SUCCESSFUL（修复 12.4F 误删 item_bookmark.xml 导致的 TOC BookmarkAdapter 编译回归 + 漏 import VMBaseActivity 级联错误）；grep 无 Log.d/e 残留。**FR-11 真机验证归 V-9 统一执行**（V-9 待办清单已含 BookInfoEdit）

- [x] ✅ **12.51 L-B9 ImportBook/RemoteBook 枝叶 Compose 化完成（2026-08-15）**：通用内容区 `ImportBookScreen.kt`（GlassTopAppBar 顶栏 + 更多下拉菜单 + 搜索栏 + 路径栏(返回上级) + 列表 + 空态/加载条）+ `ImportBookActivity`/`RemoteBookActivity` ComposeView 混合接线（activity_import_book.xml = ComposeView + View SelectActionBar 多选底栏）。本地导入菜单 6 项（选择文件夹/按名称/大小/时间排序/智能扫描/导入文件名设置），远程导入菜单 6 项（刷新/按名称/更新时间排序/服务器配置/帮助/日志），均 AppDropdownMenu 数据驱动；多选/全选/反选/删除选中/添加书架 SelectActionBar 全保留；BaseImportBookActivity 移除 SearchView 依赖。**旧文件删除**：ImportBookAdapter/RemoteBookAdapter/item_import_book.xml/menu_import_book.xml/menu_book_remote.xml（grep 确认 0 残留引用），menu_import_book_sel.xml 保留（多选菜单在用）。i18n：PathBar 返回上级→go_back 资源 + 新增 re_add_to_bookshelf（en+zh）。**门禁**：assembleAppDebug BUILD SUCCESSFUL（修复扩展图标全限定名无法解析 + Adapter 接口 override 失效 2 类编译错误）；grep 无 Log.d/e 残留；GetDiagnostics 0 错误。**FR-11 真机验证归 V-9 统一执行**（V-9 待办清单已含 ImportBook/RemoteBook）

- [x] ✅ **12.54 L-B16 TxtTocRuleActivity 枝叶 Compose 化完成（2026-08-15）**：全 Compose 页面 `TxtTocRuleScreen.kt`（GlassTopAppBar 顶栏 + 顶栏菜单 6 项 AppDropdownMenu：添加/导入本地/在线导入/二维码导入/导入默认/帮助 + 列表项 CheckBox/名称/示例/启用开关/编辑/更多/拖拽手柄 + 滑选多选（多选模式 pointerInput 长按拖动批量勾选）+ 长按拖拽排序（DragHandle + detectDragGesturesAfterLongPress + 本地重排/松手提交）+ 底部批量操作栏 SelectionActionBar：全选/反选/删除/选择菜单（启用/停用/导出）+ 空态 EmptyStatePlaceholder）+ `TxtTocRuleActivity` ComposeView 混合接线（activity_txt_toc_rule.xml 单容器）。VM 逻辑零改动，编辑对话框/导入（本地/在线/二维码/默认）/帮助/导出/置顶置底逻辑保留 Activity，Screen 上抛事件；选择状态 Activity 持 selectedIds 桥接。**旧文件删除**：TxtTocRuleAdapter/item_txt_toc_rule.xml/menu txt_toc_rule_sel/item（grep 确认 0 残留引用），menu/txt_toc_rule.xml 保留（TxtTocRuleDialog 在用）。i18n：复用既有 key（txt_toc_rule/delete/enable/select_all_count/select_cancel_count/revert_selection/to_top/to_bottom 等）无新增。**门禁**：assembleAppDebug --rerun-tasks BUILD SUCCESSFUL（仅预存 bundleOf/systemUiVisibility 等历史警告）；grep 无 Log.d/e 残留；GetDiagnostics 0 错误。**FR-11 真机验证归 V-9 统一执行**（V-9 待办清单已含 TxtTocRule）

- [x] ✅ **12.55 L-C6 DictRuleActivity 枝叶 Compose 化完成（2026-08-15）**：全 Compose 页面 `DictRuleScreen.kt`（GlassTopAppBar 顶栏 + 顶栏菜单 6 项 AppDropdownMenu：添加/导入本地/在线导入/二维码导入/导入默认/帮助 + 列表项 CheckBox/名称/启用开关/编辑/删除/拖拽手柄 + 滑选多选（选择模式常驻 pointerInput 长按拖动批量勾选，对齐原版 activeSlideSelect）+ 长按拖拽排序（DragHandle + detectDragGesturesAfterLongPress + 本地重排/松手提交）+ 底部批量操作栏 SelectionActionBar：全选/反选/删除/选择菜单（启用/停用/导出）+ 空态 EmptyStatePlaceholder）+ `DictRuleActivity` ComposeView 混合接线（activity_dict_rule.xml 单容器）。VM 逻辑零改动，编辑全屏对话框/导入（本地/在线/二维码/默认）/帮助/导出/删除确认逻辑保留 Activity，Screen 上抛事件；选择状态 Activity 持 selectedNames（String 主键）桥接；拖拽排序提交按新顺序重排 sortNumber 覆盖原 swap+upOrder 语义。**旧文件删除**：DictRuleAdapter/item_dict_rule.xml/menu/dict_rule.xml/menu/dict_rule_sel.xml（grep 确认 0 残留引用），menu/dict_rule_edit.xml 保留（DictRuleEditDialog 在用）。i18n：复用既有 key（dict_rule/delete/enable/edit/select_all_count/select_cancel_count/revert_selection/enable_selection/disable_selection/export_selection 等）无新增。**门禁**：assembleAppDebug BUILD SUCCESSFUL（仅预存 bundleOf/systemUiVisibility 等历史警告）；grep 无 Log.d/e 残留；GetDiagnostics 0 错误。**FR-11 真机验证归 V-9 统一执行**（V-9 待办清单已含 DictRule）

- [x] ✅ **12.56 L-C9 FileManage 枝叶 Compose 化完成（2026-08-15）**：全 Compose 页面 `FileManageScreen.kt`（GlassTopAppBar 顶栏（返回 + 标题）+ 搜索过滤 OutlinedTextField + 路径导航条 PathBar（root + 逐级目录 LazyRow 点击跳转，当前级 primary 高亮）+ 文件列表 LazyColumn（上级 .. / 文件夹 Folder 图标 / 文件 Description 图标）+ 长按删除（AppDropdownMenu 删除项，仅文件夹/文件，上级 .. 不响应）+ 空态 EmptyStatePlaceholder + 加载 LinearProgressIndicator）+ `FileManageActivity` ComposeView 混合接线（activity_file_manage.xml 单容器）。VM 逻辑零改动（upFiles/delFile/rootDoc/subDocs 保留），返回键回上级/根目录退出/打开文件（FileProvider + openFileUri）/进入目录/删除文件逻辑保留 Activity，Screen 上抛事件；搜索过滤 Activity 侧按名称 contains 过滤（上级 .. 常驻）。**HandleFileActivity 保持 View**（透明壳 Activity 无自有 UI，仅按 mode 分发系统选择器/FilePickerDialog/手动输入对话框，S6 弹窗体系非本页自有布局，不迁移）。**旧文件删除**：item_file.xml/menu/file_long_click.xml（grep 确认 0 残留引用），item_path_picker.xml 保留（FilePickerDialog 在用）。i18n：新增 root（en:Root/zh:根目录）双语，其余复用既有 key（file_manage/screen/delete/go_back/empty）无新增。**门禁**：assembleAppDebug BUILD SUCCESSFUL；grep 无 Log.d/e 残留；GetDiagnostics 0 错误。**FR-11 真机验证归 V-9 统一执行**（V-9 待办清单已含 FileManage）

- [x] ✅ **12.57 L-C10 DownloadManageActivity 枝叶 Compose 化完成（2026-08-15）**：全 Compose 页面 `DownloadManageScreen.kt`（GlassTopAppBar 顶栏（返回 + 更多下拉菜单「清除已完成」）+ PrimaryScrollableTabRow 5 页签（全部/下载中/已暂停/已完成/失败）+ 任务列表 LazyColumn（文件名+状态色/运行进度条+大小/来源 URL，已完成行尾更多图标提示）+ 点击任务 AppMenuSheet 按状态弹出操作菜单（运行/等待→删除；暂停/失败→重试+删除；完成→打开文件+复制路径+删除）+ 删除任务/清除已完成 ConfirmDialog 确认 + 空态 EmptyStatePlaceholder + 骨架屏 ShelfListSkeleton）+ `DownloadManageActivity` ComposeView 混合接线（activity_download_manage.xml 单容器）。轮询/过滤/排序/取消/重试/打开文件/清除已完成逻辑保留 Activity（500ms 单循环轮询 DownloadState.queryAllTaskStatus，修复原实现切换 Tab 重复启动轮询协程导致多协程并发轮询的问题），Screen 上抛事件；Dialog 族（任务操作菜单/删除确认/清除确认）Compose 化保留。**旧文件删除**：DownloadTaskAdapter/item_download_task.xml/menu/download_manage.xml（grep 确认 0 残留引用）。i18n：新增 clear_completed_confirm（en:Clear completed and failed download tasks?/zh:确定清除已完成和失败的下载任务？）双语，其余复用既有 key（download_manage/download_tab_*/download_delete_task/download_retry/download_open_file/download_copy_path/download_empty/wait_download/downloading/pause/download_success/download_error 等）无新增。**门禁**：assembleAppDebug BUILD SUCCESSFUL（顺带修复 12.56 遗留编译错误：FileManageActivity 漏 VMBaseActivity/R 导入 + FileManageScreen ArrowUpward 图标 automirrored→filled）；grep 无 Log.d/e 残留；GetDiagnostics 0 错误。**FR-11 真机验证归 V-9 统一执行**（V-9 待办清单已含 DownloadManage）

- [x] ✅ **12.58 L-C11 UrlRecordActivity 枝叶 Compose 化完成（2026-08-15）**：全 Compose 页面 `UrlRecordScreen.kt`（GlassTopAppBar 顶栏（返回 + 更多下拉菜单 5 项：记录开关勾选态/四维过滤/清除 7 天/清除 30 天/清除全部）+ SettingsSearchBar 搜索框 + 列表 LazyColumn（method 色标 GET 蓝/POST 紫/status 2xx 绿/4xx 橙/错误红 + 耗时 + 时间 + URL + 域名 + 书源 primary 色）+ 空态 EmptyStatePlaceholder + 骨架屏 ShelfListSkeleton + 清除确认 ConfirmDialog）+ `UrlRecordActivity` ComposeView 混合接线（activity_url_record.xml 单容器）。搜索/四维过滤（domain/sourceName/method/status + 清除过滤）/清除 7 天/30 天/全部/详情对话框（View alert 保留，显示详情 + 复制 URL）逻辑保留 Activity；Screen 上抛事件；详情 alert 对话框保留 View 体系（与 12.55/12.57 一致的 ComposeView 壳层混合方案）。**旧文件删除**：item_url_record.xml/menu/url_record.xml（grep 确认 0 残留引用）。i18n：复用既有 key（url_record/record_url_switch/url_record_filter/url_record_filter_*/clear_7_days_ago/clear_30_days_ago/clear_all_records/url_record_detail/url_record_detail_message/url_record_error/url_record_empty/just_now/minutes_ago/hours_ago/days_ago/search/clear/clear_filter/filter_success/filter_failed/sure_del/copy_url/ok/cancel 等）无新增。**门禁**：assembleAppDebug --rerun-tasks BUILD SUCCESSFUL（77 executed，0 error）；grep 无 Log.d/e 残留；GetDiagnostics 0 错误；APK `legado_miss_app_3.26.081516.apk` 已更新。**FR-11 真机验证归 V-9 统一执行**（V-9 待办清单已含 UrlRecord）

- [x] ✅ **12.59 L-C12 RecycleBinActivity 枝叶 Compose 化完成（2026-08-15）**：全 Compose 页面 `RecycleBinScreen.kt`（GlassTopAppBar 顶栏（返回 + 更多下拉菜单 2 项：清空回收站/帮助）+ 列表 LazyColumn（名称+类型+删除时间）+ 滑选多选（pointerInput 长按拖动批量勾选）+ 批量操作栏 RecycleBinActionBar（全选/反选/批量恢复/批量删除）+ 单个恢复/删除 + 恢复冲突覆盖确认 ConfirmDialog + 清空确认 ConfirmDialog + 空态 EmptyStatePlaceholder + 骨架屏 ShelfListSkeleton）+ `RecycleBinActivity` ComposeView 混合接线（activity_recycle_bin.xml 单容器）。数据观察（sourceRecycleBinDao.flowAll）/选择状态（selectedIds）/恢复冲突检测（SourceRecycleBinHelp.hasConflict）/恢复/删除/清空逻辑保留 Activity；Screen 上抛事件。**旧文件删除**：RecycleBinAdapter/item_recycle_bin.xml/menu/recycle_bin.xml/menu/recycle_bin_sel.xml（grep 确认 0 残留引用）。i18n：复用既有 key（recycle_bin/recycle_bin_restore/recycle_bin_delete/recycle_bin_delete_msg/recycle_bin_delete_selection_msg/recycle_bin_empty/recycle_bin_empty_msg/recycle_bin_restore_conflict/select_all/revert_selection/draw/ok/cancel/help 等）无新增。**门禁**：assembleAppDebug BUILD SUCCESSFUL；grep 无 Log.d/e 残留；GetDiagnostics 0 错误。**FR-11 真机验证归 V-9 统一执行**（V-9 待办清单已含 RecycleBin）

- [x] ✅ **12.5A L-C16 AutoTask/AutoTaskEdit 枝叶 Compose 化完成（2026-08-15）**：1）列表页全 Compose `AutoTaskScreen.kt`（GlassTopAppBar 顶栏（返回 + 更多下拉菜单 4 项：添加任务/导入本地/在线导入/日志）+ SettingsSearchBar 搜索框 + 列表 LazyColumn（任务名/启用 Switch/摘要 Cron+状态：最后运行/最后错误/未运行）+ 滑选多选（pointerInput 长按拖动批量勾选）+ 长按拖拽排序（DragHandle + detectDragGesturesAfterLongPress + 本地重排/松手提交）+ 批量操作栏 AutoTaskSelectionActionBar（全选/反选/批量设置 Cron/批量启用/批量停用/导出所选/批量删除）+ 空态 EmptyStatePlaceholder + 骨架屏 ShelfListSkeleton）；2）`AutoTaskActivity` ComposeView 混合接线（activity_auto_task.xml 单容器），数据观察（rulesFlow）/搜索过滤/选择状态（selectedIds）/拖拽排序提交（saveOrder）/导入（本地 ImportAutoTaskDialog + 在线 DialogEditText）/批量 Cron/批量启停/导出所选/删除确认逻辑保留 Activity；3）编辑页全 Compose `AutoTaskEditScreen.kt`（GlassTopAppBar 顶栏（返回 + 更多下拉菜单 6 项：保存/调试/登录/复制源码/粘贴源码/帮助）+ SettingsCard 分组字段：启用+启用 Cookie 开关行 / 名称 + 执行频率三选一（每天/每小时/自定义，自定义档才显示 Cron 输入框）+ 备注 / 脚本/请求头/JS 库/并发率 / 登录 URL/登录 UI/登录检查 JS）；4）`AutoTaskEditActivity` ComposeView 混合接线（activity_auto_task_edit.xml 单容器），构建（buildTask）/保存校验（名称非空/自定义 Cron 合法/脚本非空）/未保存拦截（finish 覆写对比 draft）/登录（SourceLoginActivity autoTask 类型）/复制粘贴源码/帮助逻辑保留 Activity。**旧文件删除**：AutoTaskAdapter/item_auto_task.xml/menu/auto_task.xml/menu/auto_task_item.xml/menu/auto_task_sel.xml/menu/auto_task_edit.xml（grep 确认 0 残留引用）。i18n：复用既有 key（auto_task_manage/auto_task_add/auto_task_edit/auto_task_delete/auto_task_batch_cron/auto_task_cron_frequency/auto_task_cron/auto_task_cookie_jar/auto_task_comment/auto_task_script/auto_task_header/auto_task_jslib/auto_task_concurrent_rate/auto_task_last_run/auto_task_last_error/auto_task_not_run/auto_task_item_summary/auto_task_name_required/auto_task_script_empty/auto_task_cron_invalid/auto_task_no_task/auto_task_debug_pending/auto_task_help_missing/enable_selection/disable_selection/export_selection 等）无新增。**门禁**：assembleAppDebug BUILD SUCCESSFUL（顺带修复 12.59 遗留编译错误：activity_recycle_bin.xml 未替换为 ComposeView 壳层导致 binding.composeHost 解析失败，已替换为单容器）；grep 无 Log.d/e 残留；GetDiagnostics 0 错误；APK `legado_miss_app_3.26.081517.apk` 已更新。**FR-11 真机验证归 V-9 统一执行**（V-9 待办清单已含 AutoTask/AutoTaskEdit）

- [x] ✅ **12.5B L-C17 WelcomeActivity 枝叶 Compose 化完成（2026-08-15）**：全 Compose 展示区 `WelcomeScreen.kt`（标题区：左侧主题色竖线 6dp+64dp + 主标题 49sp + 副标题 16sp；书籍图标 icon_read_book 120dp ColorFilter.tint(primary) contentScale Fit；底部标语 16sp letterSpacing 1.6sp；四元素显隐由宿主状态驱动，S6 弹窗/展示族范式）+ `WelcomeActivity` ComposeView 混合接线（activity_welcome.xml 单容器 ComposeView）。Activity 保留：欢迎图背景解码（customWelcome + welcomeImage(Dark)，.9.png→decodeNinePatchDrawable / 普通位图→decodeBitmap+toDrawable）、FLAG_ACTIVITY_BROUGHT_TO_FRONT 防重复启动、welcomeShowTime 显示时长（0 直接跳转）、startMainActivity + defaultToRead 直达上次阅读、文字/图标显隐（日/夜两套 AppConfig.welcomeShowText(Dark)/welcomeShowIcon(Dark)）经 mutableStateOf 桥接驱动 Compose 重组。**旧布局清理**：原 activity_welcome.xml 硬编码乱码文案（标题/副标题/标语）+ AccentTextView/ImageView 布局已整体替换为 ComposeView 单容器。i18n：新增 welcome_title/welcome_subtitle/welcome_slogan 三 key（en：Read/Enjoy quality time/Read thousands of stories；zh：阅读/享受美好时光/品读万千故事）双语，替换原硬编码乱码。Launcher1-7 多图标入口类保留不变。**门禁**：assembleAppDebug BUILD SUCCESSFUL（APK `legado_miss_app_3.26.081517.apk` 已更新）；grep 无 Log.d/e 残留；GetDiagnostics 0 错误。**FR-11 真机验证归 V-9 统一执行**（V-9 待办清单已含 Welcome）

## AOAdapt 日志

- [x] 首轮审查反馈"深度不足" → 产出深度版 forks-deep-dive.md（逐源码核验）+ design.md 新增 AD-05~AD-14 + 用户旅程状态机
- [x] 用户点名四仓（HapeLee/legados/Rimchars/legado_NG）+鸿蒙版 → 产出 four-fork-deep-dive.md、P4 重写为鸿蒙三级布局、AD-15/16
- [x] 用户点名 morealm-reader（纯 Compose 现代工程标杆）→ 克隆+逐源码拆解 → morealm-deep-dive.md + AD-17/18
- [x] 用户质疑是否「整体前端视角」→ 新增 frontend-synthesis.md（五仓收敛+不裁剪红线清单 A~D）
- [x] 用户确认「全部补齐再开工」→ 新增 implementation-spec.md（17 组件签名+主题映射+文件锚点+PR/KPI+themeConfig 格式封口）
- [x] 用户确认「设计具备开发支撑」→ 设计阶段定稿 ✅，README/INDEX 状态清零，UI 重构设计闭环
- [x] 用户确认「进入实施 Phase 0」→ 建仓 19 组件文件（比 spec 17 多 2：BadgeDotPlain 由 BadgeDot 合并、BookTocBookmarkSheet 从 Book 参数改为 title 列表注入以解耦）；AOAdapt: 组件数量 17→19 为拆 Modifier 独立文件所致，签名与 spec 2.1~2.6 一致
- [x] 用户确认「继续 Phase 1」→ PR-1.1 已在 P0 落盘；PR-1.2 替换 LegadoTheme lerp → toM3Scheme（remember 缓存）；themeConfig.json 一字不改；AOAdapt: LegadoTheme 从此无手写 ColorScheme，色板推导单一入口
- [x] 用户确认「继续 Phase 2 我的页」→ 整页 Compose 化落地；AOAdapt: (a) Project 首次 Fragment 内嵌 ComposeView（无先例，用 AndroidView 壳改为 addView ComposeView 进 pre_fragment 容器，O 已实现）；(b) material-icons 1.7.8 图标名变更（Books→LibraryBooks）暴露，须编译验证图标；(c) appDb 统计全同步查询且无 Flow 预览，produceState(IO) 包装；(d) ConfigTag 真实包 ui.config 非 constant，探针修正；(e) BookSourceDao 计数接口 allCount() 非属性
- [x] 用户确认「真机验证 Phase 2」→ MEmu 实测发现我的页永久 loading（produceState + LaunchedEffect 双状态死锁）→ 修复为单 produceState 委托属性 + if stats==null；AOAdapt: Compose 异步取数避免 produceState 与 LaunchedEffect 写同一状态；uiautomator2 元素 .bounds() 是方法；switch 需点中心坐标
- [x] 用户确认「进入 Phase 3 书架」→ 全量 Compose 化书架落地；AOAdapt 关键教训：(f) `RemoveAllViews+addView(ComposeView)` 在 ViewPager/RecyclerViewAtPager2 容器不测量（0 尺寸空渲染）→ 直接改 fragment XML 容器元素为 ComposeView + `binding.x.setContent{}` 纯方案根除；(g) compose-ui 1.9.2 无 setViewTreeLifecycleOwner（弃用，MyFragment 真机已证 addView 方案可脱离）；(h) glide-compose 不引入，AndroidView+BookCover.load 免新依赖；(i) material3 Tab 无 onLongClick 参数须 combinedClickable 包 Modifier；(j) AppConfig.getBookSortByGroupId 是 suspend（withContext(IO)）必须 launch 内调；(k) Kotlin 顶层扩展 import 不带类名前缀（BookExtensions.isLocal 错 → help.book.isLocal）
- [ ] 后续任务过程记录 Action / Observation / Adapt（实施阶段立项后另行跟踪）

## 12.x AOAdapt 日志（设计文档完善 v2，2026-08-11）

- [x] 用户反馈「学 5 个开源前端改造效果仍差」→ 反省根因：学的是碎片组件/页面，未沉淀成「页面骨架 + 组件使用标准 + token 引用」可执行规则；KPI 缺统一性验收；文档半完成态 → 本轮以 ui-standards + pages-inventory 双权威文档落地
- [x] 用户明确「前端全部 Compose + 工程级规范 + 核心功能一个不漏」→ AD-19~22 + spec FR-8~12 + 全量 84 页核对表；AOAdapt: 全量核对表基于 4 路源码探针而非猜测，N 不迁移清单（正文/CodeEdit/WebView/扫码/透明窗）单独封口
- [x] 用户确认 Compose 边界「正文保留原生 View，其余全 Compose」→ AD-20 路线图维持 AD-02；「全部 Compose」= 页面壳/浮层/列表全 Compose，内核被 AndroidView 桥包围
- [x] 用户确认「真机功能点覆盖测试纳入计划随迁移逐页执行」→ 第 13 节门禁 + FR-11 + ui-standards §7 检查清单第 8 项

## 14. 实施者深度复盘补证 v3（2026-08-11 用户要求「深度思考 UI 开发是否符合当初的设计」，仅复盘不改码）

> 用户观察：书架 tab 曾被感知为「黑色竖排」（真机实测横排亮紫正常，判定为用户误记/旧包记忆，已搁置）。随后要求以 `ui-standards.md` 为判据深度复盘 Phase0-3 是否符合设计。结论：**方向符合、存在 5 项设计符合性欠账**，全部登记如下。其中 ①④ 与 v2 的 11.9/AD-21 重合（v2 已列待办），②③⑤ 为本轮补证新增。

- [x] 14.1 复盘结论 A · 符合项：主题单源（AD-18，Fragment setContent 均包 LegadoTheme）、受控组件（ui-standards §4，BookshelfScreen 无状态+回调）、我的页三级（AD-16 入口零删除）、书架渲染矩阵（implementation-spec P3：showBookname 0/1/2、进度贴底、toTimeAgo、getDisplayCover）、红线（正文零改动/themeConfig 只读/每书独立配色）全部兑现 ✅
- [x] 14.2 复盘结论 B · ①私有组件复制公共能力（最严重，KPI 违反）：`BookshelfItems.kt:67-91` UnreadBadge 复制 `BadgeDot` 能力（均 99+ 数字角标），且私有版无 `badgeTextBright` 亮度自适应/纯圆点模式，还硬编码 Color.White 违反「禁硬编码色」。ui-standards §3.2 明文点名 UnreadBadge。**→ 已并入 11.9a 收敛为 BadgeDot**（2026-08-11 落地，见 11.9）
- [x] 14.3 复盘结论 C · ②三态未达标（ui-standards §5 违反）：书架 loading 用 CircularProgressIndicator（应 ShelfGridSkeleton）；空态只有单行 Text（应「图标48dp+主文案+次文案+操作按钮」统一占位）；**无错误态**（books 拉取失败直接当空态，应「占位+重试」）。**→ 已并入 11.9b + 本轮实施补全**（2026-08-11 落地，见 11.9）
- [x] 14.4 复盘结论 D · ③栅格越档（ui-standards §1.3/Design Pillar 违反）：BookshelfScreen 出现 6dp（bookname top、showBookname==2 文案 6/4）、10dp（list padding vertical、封面 10dp）等 4dp 半档，须归档到 4/8/12/16/24/32 档位。**→ 本轮实施归档**：bookname top 6→8、showBookname==2 文案 6/4→8/4、list padding vertical 10→8、Column start 14→16、Tab edgePadding 6→8（16 的 grid contentPadding/12 spacedBy 已合规）
- [x] 14.5 复盘结论 E · ④孤儿组件/接线：PillNavigationBar（MainActivity 仍 BottomNavigationView）、GlassTopAppBar、AppModalBottomSheet、BookTocBookmarkSheet、ShelfGridSkeleton、SettingsSearchBar 等 12/19 孤儿。已由 v2 §3.1 接线计划覆盖（P1/P4/P5 执行），纳入 Phase4/5 任务。**→ ✅ 已完成（2026-08-16 孤儿清理）**：删除 SummaryCard/ThemedSnackbarHost/SplicedColumnGroup/ManageScreenSheet 4 个无消费场景孤儿组件；VerticalScrollbar 已接线 DownloadManageScreen（下载管理页长列表滚动指示条）；其余孤儿均已接线（PillNavigationBar→MainActivity、GlassTopAppBar→55 页、SettingsSearchBar/AppModalBottomSheet/BookTocBookmarkSheet/ShelfGridSkeleton/SwipeActionContainer→BookSourceItems/BadgeDot）；ListScaffold 保留模板待用（task 12.30）。剩余孤儿 0/19
- [ ] 14.6 复盘结论 F · ⑤设计文档自身 bug：接线计划在同一文档两套 Phase 编号（ui-standards §3.1 标「阶段 P1/P0 补」vs frontend-synthesis/implementation-spec 实际 Phase2-5）→ v2 已修复 implementation-spec Phase4/5 标号重复，但 ui-standards §3.1「阶段」列与 frontend-synthesis 的 Phase 语义仍不一致，登记待统一（随 Phase4 开工时修订）
- [x] 14.7 复盘结论 G · ⑥GeneratedCover 8 色硬编码（BookshelfItems:98-105）属「封面视觉定色」豁免场景，须在 ui-standards §1.1 例外登记，否则 KPI「非内核硬编码色=0」无法达标。**→ 本轮登记豁免**
- [ ] 14.8 真机复核：书架三态/栅格归档后的渲染与功能点用例在 MEmu 复核（ai_tests 框架，L1 + 书架专项 L2）

## AOAdapt 日志（实施者复盘 v3，2026-08-11）

- [x] 用户「深度思考 UI 开发是否符合当初的设计，先别动源码」→ 以 ui-standards/implementation-spec/frontend-synthesis 为判据 5 证复核（BookshelfScreen 渲染矩阵/UnreadBadge vs BadgeDot/Fragment 主题包裹/硬编码色 grep 8 处/MainActivity 导航）；AOAdapt: 复盘发现 v2 已覆盖大部分（11.9/AD-21/Phase 编号修复），新增三态错误态缺失、栅格越档、GeneratedCover 豁免 3 项
- [x] 用户「先优化完善设计文档，按新要求重新考虑后再实施」→ 本第 14 节落档 + 后续 section 11.9 实施补全集（UnreadBadge 收敛/Skeleton 接线/三态占位/栅格归档/豁免登记）

## 备注

- 设计阶段（1-7）：纯文档，`src/` 全程不动。
- 实施阶段（8+）：动 `ui/widget/components/` 建仓、后续 Phase 动 `ui/theme/` 或页面 Compose 迁移。
