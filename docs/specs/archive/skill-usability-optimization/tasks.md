# Tasks: Skill 三件套使用者体验优化方案

> **格式说明**：`- [ ] X.Y` 未完成 / `- [x] X.Y ✅ YYYY-MM-DD` 已完成
> **优先级**：P0（必须完成） > P1（应该完成） > P2（建议完成）

---

## 方向 1：精简 SKILL.md（P0）

### 1.1 source-creator SKILL.md 精简（514→300行）

- [ ] 1.1.1 精简陷阱速查表：保留 Top 10 高频陷阱（#1/#5/#11/#14/#17/#37/#47/#58/#62/#79），完整 79 条指向 references/troubleshooting/ → 验证：行数 ≤ 20
- [ ] 1.1.2 精简 JVM 测试基础设施：只保留"JVM 可选，降级 Python"核心描述，详情指向 references/jvm-infrastructure.md → 验证：行数 ≤ 10
- [ ] 1.1.3 精简 5 阶段工作流：每阶段精简为"输入→动作→输出→完成标志"，详情指向 references/ → 验证：行数 ≤ 80
- [ ] 1.1.4 精简代码进化机制：只保留触发条件和流程概要 → 验证：行数 ≤ 10
- [ ] 1.1.5 精简辅助工具列表：只保留 obstacle_resolver 和 auto_fixer，其余 9 个标记为"高级工具，详见 references/" → 验证：行数 ≤ 10
- [ ] 1.1.6 精简参考文档索引：合并相似条目 → 验证：行数 ≤ 30
- [ ] 1.1.7 验证：source-creator SKILL.md 总行数 ≤ 300 → 验证：`wc -l SKILL.md`

### 1.2 skill-auditor SKILL.md 精简（1257→500行）

- [ ] 1.2.1 将 42 项检查清单详细说明移动到 references/audit-checklist.md → 验证：文件存在且内容完整
- [ ] 1.2.2 将 8 维度详细检查方法移动到 references/audit-dimensions.md → 验证：文件存在且内容完整
- [ ] 1.2.3 精简 8 维度审查框架：每维度精简为"目标+检查方法+通过标准" → 验证：行数 ≤ 150
- [ ] 1.2.4 精简 L1/L2/L3 检查清单：保留分层结构，精简描述 → 验证：行数 ≤ 50
- [ ] 1.2.5 精简审查执行流程：精简为流程图 + 关键步骤 → 验证：行数 ≤ 40
- [ ] 1.2.6 精简审查报告格式：保留 3 部分结构，精简模板 → 验证：行数 ≤ 30
- [ ] 1.2.7 精简历史审查经验：只保留 Top 10，完整 25 条指向附录 → 验证：行数 ≤ 30
- [ ] 1.2.8 验证：skill-auditor SKILL.md 总行数 ≤ 500 → 验证：`wc -l SKILL.md`

### 1.3 workflow-auditor SKILL.md 精简（153→120行）

- [ ] 1.3.1 精简审计流程描述 → 验证：行数 ≤ 120

---

## 方向 2：分级工作流（P0）

### 2.1 快速路径实现

- [ ] 2.1.1 在 source-creator SKILL.md 中新增"分级工作流"章节 → 验证：章节存在
- [ ] 2.1.2 定义快速路径触发条件（CMS 类型/静态 HTML/用户要求） → 验证：条件明确
- [ ] 2.1.3 定义快速路径流程（2 步：构建规则 + verify-source.py） → 验证：流程清晰
- [ ] 2.1.4 定义快速路径完成标志：`[QUICK_PATH_COMPLETE]` → 验证：标志定义
- [ ] 2.1.5 定义路径选择决策树 → 验证：决策树无歧义

### 2.2 标准路径定义

- [ ] 2.2.1 定义标准路径流程（精简 5 阶段：Phase 1 快速搜索 + Phase 2 + Phase 3 + Phase 5） → 验证：流程清晰
- [ ] 2.2.2 定义标准路径与完整路径的切换条件 → 验证：切换条件明确

---

## 方向 3：统一 basic-memory 降级策略（P0）

### 3.1 统一降级流程

- [ ] 3.1.1 定义统一降级四步流程：检测→告知→替代→标记 → 验证：四步完整
- [ ] 3.1.2 在 source-creator SKILL.md 中更新降级路径描述 → 验证：与统一流程一致
- [ ] 3.1.3 在 workflow-auditor SKILL.md 中更新降级路径描述 → 验证：与统一流程一致
- [ ] 3.1.4 在 skill-auditor SKILL.md 中更新降级路径描述 → 验证：与统一流程一致

### 3.2 降级模式功能边界

- [ ] 3.2.1 定义 source-creator 降级模式功能边界（跳过 Phase 1，标注"无经验参考"） → 验证：边界明确
- [ ] 3.2.2 定义 workflow-auditor 降级模式功能边界（4 项检查可用） → 验证：边界明确
- [ ] 3.2.3 定义 skill-auditor 降级模式功能边界（7 维度 35 项可用） → 验证：边界明确

---

## 方向 4：消除"实现状态"歧义（P0）

### 4.1 移除"设计中"标记

- [ ] 4.1.1 移除 source-creator Phase 2 预校验"实现状态：设计中"标记 → 验证：Grep "设计中" 为 0
- [ ] 4.1.2 移除 source-creator Phase 3 JVM 降级"实现状态：设计中"标记 → 验证：Grep "设计中" 为 0
- [ ] 4.1.3 移除 source-creator Phase 3 错误诊断"实现状态：部分实现"标记 → 验证：Grep "部分实现" 为 0
- [ ] 4.1.4 移除 source-creator Phase 5 经验反哺"实现状态：部分实现"标记 → 验证：Grep "部分实现" 为 0
- [ ] 4.1.5 验证：source-creator SKILL.md 中无"实现状态"标记 → 验证：`grep "实现状态" SKILL.md` 为 0

### 4.2 辅助工具列表清理

- [ ] 4.2.1 审查 11 个辅助工具的实际可用性 → 验证：每个工具有可用性标注
- [ ] 4.2.2 移除不可用工具的描述，只保留已实现且可用的 → 验证：工具列表与代码一致

---

## 方向 5：简化工具选择（P1）

### 5.1 source-creator 测试脚本简化

- [ ] 5.1.1 将测试脚本推荐简化为 2 个核心脚本（debug-source.py + verify-source.py） → 验证：核心脚本 ≤ 2
- [ ] 5.1.2 将其余 5 个脚本标记为"高级/可选" → 验证：分级清晰
- [ ] 5.1.3 更新脚本选择决策树为 2 选 1 → 验证：决策树简化

### 5.2 skill-auditor 检查点分级

- [ ] 5.2.1 定义核心检查 10 项（默认执行） → 验证：10 项明确
- [ ] 5.2.2 定义标准检查 +15 项（用户显式要求） → 验证：15 项明确
- [ ] 5.2.3 定义深度检查 +17 项（用户显式要求） → 验证：17 项明确
- [ ] 5.2.4 在 SKILL.md 中说明默认执行核心检查 → 验证：默认行为明确

---

## 方向 6：扩展 skill-auditor 审查范围（P1）

### 6.1 新增 workflow-auditor 审查维度

- [ ] 6.1.1 定义维度 W（workflow-auditor 质量审查）：5 个检查项 → 验证：检查项明确
- [ ] 6.1.2 在 skill-auditor SKILL.md 中新增维度 W → 验证：维度存在
- [ ] 6.1.3 在 references/audit-dimensions.md 中新增维度 W 详细说明 → 验证：详细说明存在

### 6.2 新增 skill-auditor 自身审查维度

- [ ] 6.2.1 定义维度 X（skill-auditor 自身质量审查）：5 个检查项 → 验证：检查项明确
- [ ] 6.2.2 在 skill-auditor SKILL.md 中新增维度 X → 验证：维度存在
- [ ] 6.2.3 在 references/audit-dimensions.md 中新增维度 X 详细说明 → 验证：详细说明存在

---

## 方向 7：增强 workflow-auditor 独立价值（P1）

### 7.1 增加实际验证能力

- [ ] 7.1.1 在 workflow-auditor SKILL.md 中新增"步骤 3.5：实际验证" → 验证：步骤存在
- [ ] 7.1.2 定义实际验证内容：检查 output 目录 + 执行 verify-source.py → 验证：内容明确

### 7.2 减少 basic-memory 依赖

- [ ] 7.2.1 分析 8 项检查的 basic-memory 依赖 → 验证：依赖分析完成
- [ ] 7.2.2 为每项依赖 basic-memory 的检查定义降级替代方案 → 验证：替代方案明确
- [ ] 7.2.3 验证：降级模式下 4 项检查可用 → 验证：可用项 ≥ 4

### 7.3 增强审计报告

- [ ] 7.3.1 在审计报告格式中新增"错误原因"列 → 验证：列存在
- [ ] 7.3.2 在审计报告格式中新增"修复建议"列 → 验证：列存在

---

## 方向 8：统一触发词和上下文传递（P1）

### 8.1 简化触发词表

- [ ] 8.1.1 将触发词从 12 个简化为 5 个 → 验证：触发词 ≤ 5
- [ ] 8.1.2 在 AGENTS.md 中更新触发词表 → 验证：AGENTS.md 一致
- [ ] 8.1.3 在三个 skill 的 SKILL.md 中更新触发条件 → 验证：三方一致

### 8.2 上下文传递标准模板

- [ ] 8.2.1 定义上下文传递标准模板（2 必填 + 1 可选） → 验证：模板明确
- [ ] 8.2.2 定义自动推断字段（task_type / execution_logs） → 验证：推断逻辑明确
- [ ] 8.2.3 在 source-creator SKILL.md 中更新上下文传递说明 → 验证：说明一致
- [ ] 8.2.4 在 workflow-auditor SKILL.md 中更新上下文接收说明 → 验证：说明一致

---

## 验证任务（P0）

### V1：整体一致性验证

- [ ] V1.1 验证三个 SKILL.md 的行数达标（source ≤ 300, skill-auditor ≤ 500, workflow ≤ 120） → 验证：`wc -l` 结果达标
- [ ] V1.2 验证三个 SKILL.md 中无"实现状态"标记 → 验证：Grep 结果为 0
- [ ] V1.3 验证三个 SKILL.md 的触发词与 AGENTS.md 一致 → 验证：三方对比一致
- [ ] V1.4 验证三个 SKILL.md 的降级路径格式统一 → 验证：格式一致
- [ ] V1.5 验证 source-creator 分级工作流决策树无歧义 → 验证：决策树清晰
- [ ] V1.6 验证 skill-auditor 审查范围覆盖三个 skill → 验证：范围包含三个
- [ ] V1.7 验证 workflow-auditor 降级模式下 4 项检查可用 → 验证：可用项 ≥ 4
