# ADR 决策逻辑链修复后审查报告（第二轮）

> **审查对象**：`docs/specs/forks-archive-borrow-implementation/design.md` §2 章节（27 个 ADR）
> **审查时间**：2026-07-18
> **审查范围**：验证前一轮 2 严重 + 6 中等 + 5 轻微问题的修复有效性
> **审查方法**：逐项对照前一轮发现 → 检查 design.md 修复后内容 → 验证是否引入新矛盾
> **审查员**：forks-archive-borrow-implementation 修复后审查员

---

## 1. 审查概述

### 1.1 审查目标
验证前一轮审查报告 `review-adr-logic.md` 中发现的 13 项问题（2 严重 + 6 中等 + 5 轻微）是否在修复后的 design.md 中得到有效解决，并检查修复是否引入新的逻辑矛盾。

### 1.2 审查方法
1. 读取前一轮审查报告（`review-adr-logic.md`）
2. 读取修复后的 design.md
3. 逐项验证 2 严重 + 6 中等问题（轻微问题不在本次审查强制范围）
4. 交叉验证修复是否引入新矛盾
5. 输出修复有效性评估报告

### 1.3 审查结论总评
**⚠️ ADR 逻辑链修复基本到位，需补充修复 2 项遗漏**：
- 严重问题 2/2 已修复（100%）
- 中等问题 4/6 已修复（67%），ADR-010a 标题未修复、R29 缓解措施未同步更新
- 修复未引入新的 ADR 间硬矛盾
- 总体评价：**⚠️ 需补充修复**

---

## 2. 修复有效性验证

### 2.1 严重问题验证（2/2 已修复 ✅）

#### 🔴 严重 #1：ADR-008 vs ADR-002（VIDEO-B-02/E-01/E-02 P0/P1 矛盾）→ ✅ 已修复

**前一轮问题**：ADR-008 Decision 将 VIDEO-B-02/E-01/E-02 标为 P1，ADR-002 Decision 升级为 P0，两个 ADR 直接矛盾。

**修复后验证**：
- ADR-008 Decision（design.md 第 223-225 行）：
  - "P0：VIDEO-B-01 VideoBookPreloader 视频书预加载"
  - "P0：VIDEO-B-02 章节链接缓存+下一集预加载（...v5.1 调整后已升级 P0）"
  - "P0：VIDEO-E-01 ReadRecentBook 写入（v5.1 调整后已升级 P0）、VIDEO-E-02 ChoiceSpeedDialog 增强（v5.1 调整后已升级 P0）"
  - "P1：VIDEO-E-03 Exo2MediaPlayer 增强"
- ADR-002 组D（design.md 第 108-110 行）：VIDEO-B-01/B-02/E-01/E-02 均标注 P0

**结论**：✅ 已修复。ADR-008 与 ADR-002 对 VIDEO-B-02/E-01/E-02 优先级阶段判定完全一致（均为 P0），并添加"v5.1 调整后已升级 P0"标注，可追溯升级演进。

**ADR-008 Alternatives 备选 B**（第 234 行）：
- "备选 B：仅借鉴 VideoBookPreloader，放弃 VIDEO-B-02/E-01/E-02/E-03 → 否决（这些项用户价值 4.0-4.8，值得借鉴）"
- 备选 B 未明确指出前 3 项已升 P0（前一轮修复建议第 3 条），但语义上不构成矛盾，可接受。

---

#### 🔴 严重 #2：ADR-001 备选 B 过时（P0 10 项）→ ✅ 已修复

**前一轮问题**：ADR-001 备选 B 提到"P0 10 项"，与 ADR-002 调整后的 14 项矛盾。

**修复后验证**：
- ADR-001 Alternatives 备选 B（design.md 第 82 行）：
  - "备选 B：只实施原 P0 10 项 → 部分采纳（v5.1 调整后升级 4 项 P1→P0，P0 扩展为 14 项，详见 ADR-002）"

**结论**：✅ 已修复。备选 B 从"否决"改为"部分采纳"，明确"v5.1 调整后扩展为 14 项"，并交叉引用 ADR-002，决策可追溯性恢复。

---

### 2.2 中等问题验证（4/6 已修复，2/6 部分修复或未修复）

#### 🟡 中等 #1：ADR-002 标题"分组并行"与内容"物理串行"矛盾 → ✅ 已修复

**前一轮问题**：标题为"P0 阶段分组并行执行"，Decision 明确说"物理串行"，标题与内容冲突。

**修复后验证**：
- ADR-002 标题（design.md 第 86 行）："### ADR-002 P0 阶段分组顺序执行"
- ADR-002 Decision（第 90 行）："4 个组按文件隔离原则顺序执行（组间逻辑并行但物理串行，主 Agent 单线程）"
- ADR-002 Consequences（第 116 行）："4 个组按文件隔离原则顺序执行（组间逻辑并行但物理串行，主 Agent 单线程），避免并发冲突"

**结论**：✅ 已修复。标题"分组顺序执行"与 Decision"顺序执行"语义一致。

**遗留轻微问题**：
- ADR-002 备选 A（第 119 行）："完全串行执行 P0 任务 → 否决（无依赖项被人为拉长，P0 内部无 P0→P0 强依赖）"
- 备选 A"完全串行"与 Decision"分组顺序执行（物理串行）"实际效果相同（都是物理串行），否决理由"无依赖项被人为拉长"在物理串行下仍然成立不充分。
- 这是备选 A 的细节逻辑冲突，不影响 ADR 主决策，属于可选修复项。

---

#### 🟡 中等 #2：ADR-010a 标题与 Decision 不符 → ❌ 未修复

**前一轮问题**：标题"主题导入导出"，P0 Decision 是"纸墨风格+字体撞色检测"（视觉增强类，非导入导出）。

**修复后验证**：
- ADR-010a 标题（design.md 第 254 行）："### ADR-010a 主题导入导出"
- **标题未修改**，仍为"主题导入导出"
- ADR-010a Decision（第 258-260 行）已明确区分两部分：
  - "**P0 阶段仅实施本地视觉类**：THEME-B-01 纸墨风格（60 行）、THEME-B-02 字体撞色检测（本地视觉增强，无导入导出）"
  - "**P1 阶段实施导入导出**：THEME-B-03 主题包 ZIP 导入导出、THEME-B-04 Config 字段扩展、THEME-B-05 字体内嵌支持、THEME-E-04 主题包导入导出格式、THEME-E-05 主题预览能力"

**结论**：❌ 标题未修复。Decision 内容已通过"P0 阶段仅实施本地视觉类"和"P1 阶段实施导入导出"段落澄清，但标题"主题导入导出"仍未涵盖 P0 阶段的视觉增强任务范围。

**修复建议**（前一轮已提出，未采纳）：将标题改为"ADR-010a 主题视觉增强与导入导出"。

---

#### 🟡 中等 #3：ADR-010b 标题与 Decision 不符 → ✅ 已修复

**前一轮问题**：标题"主题包云端同步"，Decision 包含 5 项非云端同步任务（THEME-B-06/B-08/E-01/E-02/E-03）。

**修复后验证**：
- ADR-010b 标题（design.md 第 269 行）："### ADR-010b 主题包云端同步与扩展能力"
- ADR-010b Decision（第 273-274 行）：
  - "P2 借鉴 Archive 主题包云端同步与扩展能力（**云端同步仅 THEME-B-07 共 1 项，其余为扩展能力非云端同步**）："
  - 每项任务后明确标注分类：THEME-B-06（扩展能力）、THEME-B-07（云端同步）、THEME-B-08（扩展能力）、THEME-E-01（扩展能力）、THEME-E-02（扩展能力）、THEME-E-03（扩展能力）

**结论**：✅ 已修复。标题"主题包云端同步与扩展能力"与 Decision 内容一致，Decision 明确区分"云端同步"和"扩展能力"两类任务。

---

#### 🟡 中等 #4：ADR-006 rhino 1.8.1 锁定表述模糊 → ⚠️ 部分修复

**前一轮问题**：ADR-006 表述"rhino 1.8.1（锁定，minSdk 已提升至 23 但仍低于 24）"易误解，且 R29 缓解措施与 ADR-006 表述逻辑断裂。

**修复后验证**：

ADR-006 Decision（design.md 第 191 行）：
- "rhino 1.8.1（锁定，升级原因：API 24 以下缺少 Arrays.setAll，本项目 minSdk=23（build.gradle:66）仍低于 24，不可升级；与 ADR-022 minSdk 23 一致）"

ADR-006 Context（第 188 行）：
- "rhino 1.8.1（API 24 以下缺少 Arrays.setAll 不可升级）"

ADR-022（第 490-491 行）：
- "项目 minSdk 已提升至 23，但部分新增依赖可能要求 API 24+（如 rhino 1.8.1 锁定原因之一是 API 24 以下缺少 Arrays.setAll）"
- "minSdk 23（Android 6.0，与本项目 `app/build.gradle:66` 实际一致）"

**ADR-006 与 ADR-022 交叉验证**：✅ 完全一致，均明确"rhino 1.8.1 锁定原因是 API 24 以下缺少 Arrays.setAll，minSdk 23 不满足升级条件"。

**R29 缓解措施**（design.md 第 988 行）：
- "Android API 23 最低版本风险（rhino 1.8.1 缺少 Arrays.setAll 需 API 24+）...验证 rhino 1.8.1 在 minSdk 23 下的运行时兼容性"
- **R29 缓解措施未更新**，仍说"验证 rhino 1.8.1 在 minSdk 23 下的运行时兼容性"，未体现"已稳定运行"

**结论**：⚠️ 部分修复。ADR-006 已修复（明确锁定原因 + 交叉引用 ADR-022），与 ADR-022 完全一致；但 R29 缓解措施未同步更新（前一轮建议改为"rhino 1.8.1 已在 minSdk 23 下稳定运行，风险仅在未来升级时存在"）。

**遗留表述轻微问题**：
- ADR-006 第 191 行用词"升级原因"不够准确，应为"锁定原因"或"不可升级原因"。这是个表述瑕疵，不影响语义理解。

---

#### 🟡 中等 #5：ADR-013 与 ADR-002 对 VIDEO-E-01 复杂度评估不一致 → ✅ 已修复

**前一轮问题**：ADR-013 要求 VIDEO-E-01 遵循复杂迁移流程，ADR-002 标注为"独立，可与 VIDEO-B-02 并行"。

**修复后验证**：
- ADR-002 组D VIDEO-E-01 标注（design.md 第 109 行）：
  - "VIDEO-E-01 (ReadRecentBook 写入，v5.1 调整后已升级 P0) [含 DB Migration_98_to_99，实施复杂度高于其他 P0 任务，建议拆分为子任务串行，需遵循 ADR-013 迁移流程]"
- §6.1 P0 实施顺序组D（第 1037 行）：
  - "VIDEO-E-01 (ReadRecentBook 写入，v5.1 调整后已升级 P0) [含 DB Migration_98_to_99，实施复杂度高于其他 P0 任务，建议拆分为子任务串行，需遵循 ADR-013 迁移流程]"
- §6.1 组D 备注（第 1040 行）：
  - "组D 含 VIDEO-E-01 DB Migration，工作量略高于其他组"

**结论**：✅ 已修复。ADR-002 中 VIDEO-E-01 标注从"独立，可与 VIDEO-B-02 并行"改为"含 DB Migration_98_to_99，实施复杂度高于其他 P0 任务，建议拆分为子任务串行，需遵循 ADR-013 迁移流程"，与 ADR-013 复杂度评估完全一致。

---

#### 🟡 中等 #6：ADR-016 性能基线未列入 P0 → ✅ 已修复

**前一轮问题**：ADR-016 要求 P0 前建立基线，ADR-002 P0 任务清单未列入。

**修复后验证**：
- ADR-002 Decision（design.md 第 114 行）：
  - "**P0 前置任务**：P0 实施前先建立性能基线（ADR-016 要求），使用 `ai_tests/scripts/swipe_test_log.py` + `l2_verify_video_player.py` 测量启动时间/内存占用/搜索响应时间/视频加载时间/FPS；P0 完成后对比验证性能无显著回退（容忍阈值：启动时间 +5%、FPS -3 帧、搜索响应 +10%）。"

**结论**：✅ 已修复。ADR-002 Decision 中增加了"P0 前置任务"段落，明确性能基线建立作为 P0 启动前的强制前置任务，并引用 ADR-016 要求与具体脚本。

**轻微遗留问题**：
- §6.1 P0 实施顺序（第 1017-1041 行）的 4 组任务清单中未明确列出"步骤 0：性能基线建立 → 步骤 1：4 组顺序执行"（前一轮修复建议第 3 条）。但 ADR-002 Decision 已有"P0 前置任务"段落，可视为满足修复要求。

---

### 2.3 轻微问题验证（不在本次强制审查范围）

前一轮发现的 5 项轻微问题（#9~#13：RSS-B-02/B-05/E-06/DEPS-B-01 缺 ADR、ADR-014/019/011/007 缺交叉引用）不在本次修复后审查的强制范围内，本次审查未逐一验证。但快速扫描确认：
- ADR-007 标题仍为"RSS 搜索增强双轨方案"，未扩展覆盖 RSS-B-02/B-05/E-06
- ADR-014/019 仍未交叉引用
- ADR-011 仍未引用 ADR-024

这些轻微完整性问题可作为后续可选修复项。

---

## 3. 新发现矛盾检查

### 3.1 ADR-008 修改后与 ADR-002 的一致性

| 任务 | ADR-008 Decision | ADR-002 组D | 一致性 |
|------|------------------|-------------|--------|
| VIDEO-B-01 | P0 | P0 [独立] | ✅ 一致 |
| VIDEO-B-02 | P0（v5.1 升级） | P0（v5.1 升级）[依赖 B-01 串行] | ✅ 一致 |
| VIDEO-E-01 | P0（v5.1 升级） | P0（v5.1 升级）[含 DB Migration] | ✅ 一致 |
| VIDEO-E-02 | P0（v5.1 升级） | P0（v5.1 升级）[共用文件串行] | ✅ 一致 |
| VIDEO-E-03 | P1 | 不在 P0 范围 | ✅ 一致 |

**结论**：✅ ADR-008 与 ADR-002 对 VIDEO 模块任务的优先级阶段判定完全一致，无新矛盾。

### 3.2 ADR-010a/010b 标题与 Decision 的一致性

| ADR | 标题 | Decision 范围 | 一致性 |
|-----|------|--------------|--------|
| ADR-010a | 主题导入导出 | P0 视觉增强 + P1 导入导出 | ⚠️ 标题未涵盖 P0 视觉增强 |
| ADR-010b | 主题包云端同步与扩展能力 | 云端同步 1 项 + 扩展能力 5 项 | ✅ 一致 |

**结论**：⚠️ ADR-010a 标题与 Decision 仍有不一致（标题未涵盖 P0 视觉增强），ADR-010b 一致。

### 3.3 ADR-006 与 ADR-022 的一致性

| 维度 | ADR-006 | ADR-022 | 一致性 |
|------|---------|---------|--------|
| rhino 锁定原因 | API 24 以下缺少 Arrays.setAll | API 24 以下缺少 Arrays.setAll | ✅ 一致 |
| minSdk 版本 | minSdk=23（build.gradle:66） | minSdk 23（build.gradle:66） | ✅ 一致 |
| 是否可升级 | 不可升级 | 锁定（不升级） | ✅ 一致 |

**结论**：✅ ADR-006 与 ADR-022 完全一致，无新矛盾。

### 3.4 修复引入的新矛盾扫描

经全量扫描修复后的 design.md，未发现修复引入的新矛盾：
- ADR-001 备选 B 改为"部分采纳"后，与 ADR-002 P0=14 项一致
- ADR-002 标题改为"分组顺序执行"后，与 R22"接受串行现实，按组顺序执行（A→B→C→D）"一致
- ADR-002 增加"P0 前置任务：性能基线建立"后，与 R21"将基线建立作为 P0 前置任务"一致
- ADR-010b 标题改为"主题包云端同步与扩展能力"后，与 §9.1 ADR 索引表（第 1239 行）一致

---

## 4. 遗漏修复问题

### 4.1 遗漏修复 #1：ADR-010a 标题未修改

**问题描述**：前一轮中等 #4 建议将 ADR-010a 标题改为"ADR-010a 主题视觉增强与导入导出"，但修复后标题仍为"ADR-010a 主题导入导出"。

**影响**：标题与 P0 Decision 范围（视觉增强）不符，影响 ADR 检索效率。

**修复建议**：将 design.md 第 254 行标题改为"### ADR-010a 主题视觉增强与导入导出"，并同步更新 §9.1 ADR 索引表（第 1238 行）。

### 4.2 遗漏修复 #2：R29 缓解措施未同步更新

**问题描述**：前一轮中等 #6 建议将 R29 缓解措施更新为"rhino 1.8.1 已在 minSdk 23 下稳定运行（项目已发布）；风险仅在于未来若需升级 rhino 时需先提升 minSdk 至 24"，但修复后 R29 缓解措施仍为"验证 rhino 1.8.1 在 minSdk 23 下的运行时兼容性"。

**影响**：R29 缓解措施仍暗示 rhino 1.8.1 在 minSdk 23 下兼容性未验证，与 ADR-006"锁定"+ ADR-022"minSdk 23 已实际部署"的逻辑链仍有轻微断裂。

**修复建议**：将 design.md 第 988 行 R29 缓解措施更新为"rhino 1.8.1 已在 minSdk 23 下稳定运行（项目已发布）；风险仅在于未来若需升级 rhino 时需先提升 minSdk 至 24"。

### 4.3 遗漏修复 #3：ADR-002 备选 A 否决理由不充分（轻微）

**问题描述**：ADR-002 标题改为"分组顺序执行"后，备选 A"完全串行执行 P0 任务 → 否决"的否决理由"无依赖项被人为拉长"在物理串行下不充分（分组顺序执行本身也是物理串行）。

**影响**：备选 A 否决理由逻辑轻微冲突，不影响 ADR 主决策。

**修复建议**：更新备选 A 否决理由为"完全无分组串行执行 P0 任务 → 否决（无法利用文件隔离原则减少冲突检查成本，且组内独立任务无法借用子代理分担分析）"。

---

## 5. 总评

### 5.1 修复有效性总评

| 问题等级 | 总数 | 已修复 | 部分修复 | 未修复 | 修复率 |
|---------|------|--------|---------|--------|--------|
| 🔴 严重 | 2 | 2 | 0 | 0 | 100% |
| 🟡 中等 | 6 | 4 | 1 | 1 | 67%（已修复）/ 17%（部分）/ 17%（未修复） |
| 🟢 轻微 | 5 | 不在审查范围 | - | - | - |
| **合计（严重+中等）** | **8** | **6** | **1** | **1** | **75%（已修复）/ 12.5%（部分）/ 12.5%（未修复）** |

### 5.2 总评结论

**⚠️ 需补充修复**

**核心发现**：
1. ✅ 2 项严重问题已全部修复（ADR-008 vs ADR-002 优先级矛盾、ADR-001 备选 B P0 项数矛盾），P0 任务清单合法性恢复
2. ✅ 4 项中等问题已修复（ADR-002 标题、ADR-010b 标题、ADR-013 vs ADR-002 复杂度、ADR-016 vs ADR-002 基线）
3. ⚠️ 1 项中等问题部分修复（ADR-006 rhino 表述：ADR-006/022 已一致，R29 未同步）
4. ❌ 1 项中等问题未修复（ADR-010a 标题仍为"主题导入导出"，未涵盖 P0 视觉增强）
5. ✅ 修复未引入新的 ADR 间硬矛盾
6. ✅ ADR-008/002、ADR-010b/Decision、ADR-006/022 三组交叉验证均一致

### 5.3 补充修复优先级建议

| 优先级 | 修复项 | 理由 |
|--------|--------|------|
| P1 建议修复 | ADR-010a 标题改为"主题视觉增强与导入导出" | 影响 ADR 检索效率，1 行修改即可 |
| P2 建议修复 | R29 缓解措施更新为"已稳定运行" | 消除 ADR-006/022 与 R29 的逻辑链轻微断裂 |
| P3 可选修复 | ADR-002 备选 A 否决理由更新 | 备选 A 逻辑轻微冲突，不影响主决策 |

### 5.4 ADR 逻辑链当前状态

修复后的 ADR 逻辑链状态：
- **ADR 间硬矛盾**：✅ 已消除（2 项严重矛盾全部修复）
- **ADR 内部一致性**：⚠️ 1 项未修复（ADR-010a 标题）
- **ADR 与风险清单一致性**：⚠️ 1 项未同步（R29）
- **ADR 主体框架**：✅ 完整（27 个 ADR 覆盖 7 大类）
- **P0 任务清单合法性**：✅ 恢复（ADR-008/002/001/016/013 一致）

**最终建议**：补充修复 ADR-010a 标题（1 行修改）+ R29 缓解措施（1 行修改）后，ADR 逻辑链可视为修复完整。

---

**审查报告完成**。共验证 8 项问题（2 严重 + 6 中等）：6 项已修复、1 项部分修复、1 项未修复，修复率 75%。修复未引入新矛盾，但需补充修复 ADR-010a 标题与 R29 缓解措施 2 项遗漏，方可视为修复完整。
